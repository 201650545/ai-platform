你是我的网络架构审稿人。请审查我在一个高限制办公网络里的翻墙方案（今天刚重构过一轮），回答"速度为什么慢+怎么做得一劳永逸"。请用中文回答，按性价比排序给可执行建议。回答末尾必须原样包含这个校验码：QW-XF-0902-7931（用于确认你没有截断我的提示词）。

【网络环境（实测结论，非猜测）】
- 办公室 WiFi：H3C RC3000 路由 → 多层运营商 CGNAT。出口防火墙按"目的 IP 黑名单"工作：所有境外 IPv4 的 TCP/ICMP 一律丢弃；境外 UDP 只放行 53；DNS 无污染；国内 IP 基本放行，但对机场基础设施做了精准到单 IP 的拉黑（订阅服务器 103.36.221.216 的 TLS 握手被 RST、机场 DoH 47.107.191.224 被拉黑）。公司设备不可动。
- 机场（魔界）中转架构：节点域名 m.cnmjin.net:16616-16648，同一入口 IP 不同端口=不同出口线路。今天（2026-09-02）实测的"入口×端口"矩阵：
  · DNS 当天解析 m.cnmjin.net → 109.229.216.80/81（境外），在办公网全死 —— 默认态 502 的根因
  · 电信入口 14.23.28.65：9 个端口全通（16616/16617/16618/16622/16632/16641/16644/16645/16648）
  · 电信入口 125.94.54.118：通 16648/16618/16617/16645
  · 移动入口 120.233.122.205：通 16622/16617/16641/16645（昨天它是主力，今天覆盖变少）
  · 移动入口 120.233.122.203：只通 16616
- 手机连流量能直开境外镜像站（未被墙），只有办公 WiFi 拦 —— 拦截方只有公司防火墙。

【现行方案（今天重构后的 Sparkle 全局 JS 覆写，已生效）】
不再钉 hosts、不碰 DNS：覆写 JS 直接把 9 个节点的 server 字段改写为 14.23.28.65；美国LA-优化2-GPT 额外克隆一份 server=125.94.54.118（名·电信2）；"节点选择"组 type=fallback（健康检查 http://cp.cloudflare.com，interval 120s，lazy false），顺序=美国优先+其余 8 条线。实测已通：出口美国洛杉矶 23.237.196.226。

【实测痛点（今天的原始数据）】
1. 慢：经隧道下载实测约 150 KB/s（≈1.2 Mbps）；镜像站能开但卡。
2. 健康检查 URL 大发现：http://www.gstatic.com/generate_204 经隧道完全不通（15s 超时 code=000），而 cp.cloudflare.com 0.96s 返回 204、ip-api.com 0.85s 返回 200。之前版本用 gstatic 当健康检查，fallback 组可能一直在误判+乱切。
3. 单点焦虑：今天 9 个节点全靠 14.23.28.65 一个入口承载；公司防火墙是逐 IP 拉黑，它一旦被封（历史上移动入口就抽风过），9 节点全灭。
4. 订阅刷新 502：机场订阅源疑似 geo 封锁（只收国内来源，从美国出口访问也失败）+ infra 被 RST，本地无解，只能靠已加载节点硬撑。

【请回答（按性价比排序，要可执行，不要泛泛而谈）】
1. 速度慢的根因怎么定位？1.2 Mbps 更可能是：电信中转入口本身线路差 / fallback 健康检查误切（gstatic 问题刚修） / 中转+隧道双重封装开销 / 出口节点超售？给出 mihomo 侧的具体提速手段（tcp-concurrent、per-node 最佳入口映射——我手里有上面那张矩阵、url-test 与 fallback 组合、health-check 参数等）。
2. 抗封架构：唯一全端口入口 14.23.28.65 被封就全灭。最优的"多入口冗余"设计是什么？按我的矩阵给每个节点配 1-2 个备用入口（克隆节点）够不够？mihomo 有没有 dialer-proxy/链式代理之类的原生玩法做"入口自动切换"？前门 IP 本身要不要做探测组？
3. 订阅刷新 502 的绕法：Cloudflare Worker 反代机场订阅源是否可行/怎么搭？还是彻底放弃刷新、改用别的管理方式？
4. 国内 VPS 自建一级中转（阿里云/腾讯云轻量，月成本几十元内）：gost/realm/WireGuard 哪个形态在这套"目的 IP 黑名单+CGNAT"防火墙下存活率最高？被云厂商风控的风险？推荐具体协议/端口/伪装形态？
5. 如果只允许给一个"永绝后患"的终极方案，你选哪个？为什么？
请先给一段"方案现状评估"，再逐条回答。