# GPT R3 终验：DSH 输出预算方案验收（2026-08-29）

- 问诊渠道：镜像 GPT vip-11.67673.live，会话自动标题「R3验收结论」
- 前置轮次：R2 终审 vip-11「DSH预算终审eriwa」（批准转向+冻结方案，遗留三项验收）
- 校验令牌：DSH-ACCEPT-R3-WIRE-65536-K7QF（GPT 第五段末原样引用，实读确认）
- 判定：通过，R3 最终验收关账；需用户拍板项：无

## 问诊包（提示词全文）

【DSH 输出预算终验轮（R3 验收）】你是本项目架构审稿人。R2 终审（你会话「DSH预算终审eriwa」）你批准转向并冻结"DSH 客户端模型条目默认预算方案"，验收条件：①主对话不再因 reasoning 耗尽 32768 预算渲染"发送继续" ②标题预算保持 1024；P0 风险=fast 组故障转移成员 65536 接受度未验证。现在三项全部关账，请给最终验收结论。

【证据包】
一、层级误判更正：R2 时"已实施"实为写错配置层——cordis.patch.yml 只作用插件模块层，llm-pi-ai 适配器运行时读 settings 层（settingsNs llm-pi-ai）。数字探针证明当时 wire 仍 32768（outputTokens=32768 整 + reason max-tokens，服务重启后依旧），由此定位根因。
二、修复落地正确层并 wire 实证：~/.dsh/settings.yaml 的 llm-pi-ai.providers.local-gateway.models[fast].maxTokens=65536（settings 热重载）。用本地透传 tap 抓 DSH 发出的真实请求体，共 5 条采样（含用户真实会话流量）：{"model":"fast","max_completion_tokens":65536,"stream":true}，turn 全部正常 completed。注意参数名是 max_completion_tokens 而非 max_tokens（这解释了此前 DSH journal 里始终看不到预算值的困惑）。
三、闭环②标题独立性：标题插件显式 maxOutputTokens=1024，materialization 只填"未设置"的值，结构上不可能被 65536 覆盖；journal 两次实证 title-llm-request maxTokens=1024。
四、P0 故障转移矩阵（每成员两个微请求：mt=16 对照验活性 + mt=65536 实验验预算，非压力测试）：xiaohongshu(主,dots3-note-prev)=200/stop 接受；openrouter(dots-3-note-preview:free)=200/stop 接受；agnes(agnes-2.5-flash)=200/stop 接受；sensetime(sensenova-6.8-flash-lite)=200/stop 接受（仅慢，>45s、90s 内完成）；zenmux(dots3-note-prev)=连接超时×3，渠道本身不可达（与预算无关）。结论：4/5 接受 65536，0 拒绝。
五、防回退：网关 sync_dsh_models 已补丁为按 id 保留条目自定义字段（原实现每次编排同步整体重建 models 会抹掉手写预算）；cordis.patch.yml 中无效的 llm-pi-ai 块已移除（session-title-llm 保留）。

【请回答】严格五段：一、总评；二、验收判定（通过/不通过+理由）；三、残余风险清单；四、后续运维建议（简短）；五、需用户拍板项（无则写"无"）。请在第五段末尾原样引用令牌 DSH-ACCEPT-R3-WIRE-65536-K7QF 以确认实读全文。

## GPT 回复（全文）

一、总评
R3 证据链完整，已从 R2 的"配置意图成立但实际写错层级"修正为"运行时正确层配置 + wire 级真实请求验证 + 独立标题预算验证 + 故障转移成员矩阵验证 + 防回退机制"。尤其关键的是，本轮不再以配置文件静态值或 journal 缺省字段作为实施证明，而是通过透传 tap 直接确认 DSH 发出的请求为 max_completion_tokens=65536；因此此前 32768 的根因、修复位置及最终生效状态已经闭环。

二、验收判定
通过，R3 最终验收关账。 理由：①主对话默认预算已由实际 wire 证明提升至 65536，5 条采样包含真实会话流量且均正常 completed，原先固定 32768 的运行时约束已解除；②标题链路继续独立保持 1024，既有结构性隔离依据，又有两次 title-llm-request maxTokens=1024 实证，未受到主模型 65536 配置污染；③R2 的 P0 未决项已经完成验证：5 个 fast 故障转移成员中，4 个当前可达成员全部接受 65536，0 个出现预算拒绝，zenmux 的失败表现为连接超时且与预算参数无关。因此，R2 规定的三项验收条件均已达到，可以冻结"DSH 客户端模型条目默认预算方案"为已验收状态。

三、残余风险清单
残余风险均不构成本轮拒收项：zenmux 当前存在渠道可达性风险，故实际 fast 故障转移能力暂为 4/5 可用，而不是完整 5/5；sensetime 虽接受 65536，但微请求延迟超过 45 秒、在 90 秒内完成，属于故障转移时的时延风险；65536 是预算上限而非"任何对话绝不会触顶"的绝对保证，极端超长 reasoning 仍理论上可能耗尽更高预算，但这已经不同于此前因客户端固定 32768 而系统性触发"发送继续"的缺陷；此外，后续若修改模型同步逻辑、settings schema 或适配器参数映射，仍可能重新引入字段覆盖或预算回退。

四、后续运维建议（简短）
保留一个低成本 wire 回归探针，升级 DSH、llm-pi-ai 或网关同步逻辑后检查 fast 请求仍为 max_completion_tokens=65536，同时抽查标题仍为 1024；把 sync_dsh_models 的"按 id 保留自定义字段"加入回归测试；zenmux 单独作为渠道健康问题处理，不要与预算验收重新耦合；sensetime 建议保留延迟监控或降低其故障转移优先级。

五、需用户拍板项
无。DSH-ACCEPT-R3-WIRE-65536-K7QF
