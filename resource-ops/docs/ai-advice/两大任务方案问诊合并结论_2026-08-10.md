# 两大任务方案 · 问诊合并结论

> 日期：2026-08-10
> 问诊对象：ChatGPT（GPT-5.6，镜像 vip-12）+ Claude（Sonnet 5 High）
> 输入：docs/ai-advice/两大任务问诊提示词_2026-08-10.md
> 原始回复：
> - [两大任务问诊回复_GPT_2026-08-10.md](./两大任务问诊回复_GPT_2026-08-10.md)
> - [两大任务问诊回复_Claude_2026-08-10.md](./两大任务问诊回复_Claude_2026-08-10.md)

---

## 一、两份问诊的共识（可信采纳）

1. **v1 方向正确**：opencli 环境、两条任务、三条登录规则、四段式汇报都保留。
2. **不拆阶段**：保留"两大任务"，不拆成「验证登录态」+「细查免费额度」两阶段（同 Agent 一次跑完，上下文闭环、避免重复授权）。但内部执行顺序固定：登录验证 → 控制台定位 → 免费权益确认 → API/开发入口 → 记录证据 → 输出。
3. **必须加「停止条件」**（防卡死，两份都强调）：遇到 OAuth 二次验证 / 手机验证码 / 强制绑卡 / onboarding 问卷，尝试 1 次即停，记待办，不无限重试。
4. **记录格式要升级**：当前字段不足，需补「验证证据 / 页面路径 / API 状态 / 是否需人工动作」。API key、token 一律只记"是否存在"，不记值、不写飞书。
5. **免费政策变化快**：每条记录必须带「验证日期/时间戳」，不能当永久事实。
6. **统一输出**：每个平台固定模板，无的字段写"未说明"，严禁编造。

## 二、差异点（需永涛拍板）

| # | 议题 | GPT 观点 | Claude 观点 |
|---|------|---------|------------|
| 1 | **海外平台范围** | 维持原 4 个（GitHub Models/Colab/Together/Mistral） | 应为 **8 个**：交接书本含 Cohere/Replicate/Stability/fal.ai，v1 悄悄砍掉没说明 |
| 2 | **已登录 7 平台待细查** | 未提及 | 验证记录里 SiliconFlow/火山Ark/阿里百炼/Groq/智谱/OpenRouter/HuggingFace 7 平台写"待细查免费额度"，v1 完全漏掉，应塞进任务 B 补充负载 |
| 3 | **记录字段对齐方案书** | 加 API/Endpoint/Key 状态/RPM/TPM/是否绑卡/是否实名 | 方案书 3.1「资源能力规格表」要 `逻辑模型/endpoint/质量等级`、「资源实例表」要 `额度/cooldown/验证指纹`——v1 模板缺 endpoint/模型名/限流/是否绑卡 |
| 4 | **并发写冲突** | 未考虑 | 两个 Agent 并发写同一份 `验证记录_2026-08-10.md` 会冲突 → 分段（Agent-1 写任务A结果、Agent-2 写任务B结果）+ 主 Agent 合并重新编号 |
| 5 | **截图证据** | 建议存 `docs/资源调研/evidence/` | 方案书强调"不传整页截图，只留哈希+脱敏摘要"，本轮先只文字摘录 |
| 6 | **可信等级字段** | 建议加：A=官方页面 / B=控制台 / C=社区 | 未提（但"双源标注"思路相近：官网价格页标注"未登录验证"） |

## 三、拟采纳的 v2 方向（合并两份建议）

- 任务 A · 海外组 + 任务 B · 国内组，**保留两大任务不拆**
- 每平台固定执行顺序 + 明确「打开什么页面 → 看什么字段 → 完成标准 → 停止条件」
- 记录模板升级为六节：①登录状态 ②免费权益 ③API 能力 ④限制 ⑤验证证据 ⑥待办
- 每平台停止条件写死：OAuth 需验证 → 停；需绑卡 → 停；手机/微信墙 → 直接记录不点登录；单平台超时（海外 5 分钟/国内 3 分钟）→ 跳下一个
- 并发写文件协调：分段写入 + 主 Agent 合并

## 四、待永涛拍板（6 项）

详见正文后由主 Agent 汇总提问。

---

*本文档由主 Agent 合并两份问诊原始回复提炼，供永涛拍板后生成 v2 方案。*
