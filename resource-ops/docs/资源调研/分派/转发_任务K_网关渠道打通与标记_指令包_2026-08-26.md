# 任务 K · 飞书表 × API 转发网关打通 · 执行指令

> 本文件是**完整执行指令**，可整体复制转发给外部执行 AI（Fast 模型）。
> 收到后按指令逐项执行，完成后按文末「汇报要求」写简短汇报。
> 目标：把飞书「免费API活动清单」表与 API 转发网关打通——在飞书表中标记每个平台是否已接入网关，输出「未接入的优质渠道清单」，供后续把好渠道补进网关。

---

## 0. 总则

### 0.1 目标（三件事）
1. **读网关**：读取 API 转发网关当前已配置的渠道与统一模型编排（只读，不改任何网关配置）
2. **标飞书**：在飞书「免费API活动清单」表新增两个字段（网关接入状态、网关渠道），按本文档 §3 的映射表逐条标记 35 个平台的接入状态
3. **出清单**：输出「未接入网关的优质渠道清单」，按免费额度大 + 稳定 + OpenAI 兼容 + 免绑卡排序，标注建议接入优先级

### 0.2 执行边界（最高优先，违反即作废）
- **只读网关**：不修改 channels.json、unified_models.json、api_state.json 等任何网关文件，不调用任何 POST/PUT 写接口，不重启网关
- **只写飞书**：仅在飞书「免费API活动清单」表新增字段并更新记录；不删除、不修改其他表
- **映射以本文档 §3 为准**：平台→渠道映射关系由本文档给出，禁止自行猜测或发明映射
- **严禁编造**：所有接入状态必须基于网关实际配置判断；拿不准的标注「待确认」，不得猜测

### 0.3 环境信息（直接使用，勿重新探测）
- **lark-cli 路径**：`C:\Users\郭永涛\.trae-cn\plugins\trae-remote-official\lark\1.0.4\bin\lark-cli.exe`（PowerShell 中需 `&` 调用；不要用规则文件里的旧路径）
- **飞书 Base Token**：`StmDbTXQWaujshs9NpIc3UFpnAc`
- **飞书表 ID**：`tblf8nQiNDLtVmWE`（表名「免费API活动清单」，35 条记录）
- **飞书表现有字段**：平台名称、类别、地区、所属公司、能力、官网链接、API接入、免费规则、免费额度、有效期、截止时间、领取方式、备注、来源、核实状态
- **网关数据目录**：`D:\项目\data\search_gateway\`（channels.json、unified_models.json 在此）
- **网关鉴权**：api_key 在 `D:\项目\data\search_gateway\api_state.json` 的 `api_key` 字段；请求头 `Authorization: Bearer <key>`
- **网关接口**（端口 3100）：
  - `GET /api/channels` → 全部渠道健康状态
  - `GET /v1/models` → 聚合各渠道可用模型
  - `GET /api/unified` → 统一模型编排
- **PowerShell JSON 传参注意**：lark-cli 的 `--json` / `--fields` 参数在 PowerShell 中直接传字符串会被吞引号，**必须**用 `@相对路径.json` 文件方式（文件放当前工作目录，用相对路径如 `@api_update.json`），或先 `Get-Content file -Raw` 校验 JSON 合法

---

## 1. 第一步：读取网关配置（只读）

### 1.1 读取渠道配置
读取 `D:\项目\data\search_gateway\channels.json`，提取：
- `keys` 对象的所有 key → 已配置密钥的渠道 ID 列表
- `custom_channels` 对象 → 自定义渠道（含 base_url、models、free_models、billing_tag）
- `hidden_channels` 数组 → 已隐藏（未启用）的渠道 ID

### 1.2 读取统一模型编排
读取 `D:\项目\data\search_gateway\unified_models.json`，提取每个统一模型组名及其 `members`（渠道→模型映射）。

### 1.3 读取网关实时状态（可选但推荐）
调用 `GET /api/channels`（带鉴权头），确认各渠道 `reachable` 状态，用于在备注中标注「渠道健康但未接入」与「渠道已接入但不可达」的区别。

---

## 2. 第二步：读取飞书表记录

用 lark-cli 读取「免费API活动清单」全部 35 条记录：

```powershell
& "C:\Users\郭永涛\.trae-cn\plugins\trae-remote-official\lark\1.0.4\bin\lark-cli.exe" base +record-list --base-token StmDbTXQWaujshs9NpIc3UFpnAc --table-id tblf8nQiNDLtVmWE --page-size 200 --as user
```

确认每条记录的「平台名称」与本文档 §3 映射表的平台名能对上。

---

## 3. 平台 → 网关渠道映射表（核心，禁止自行改动）

> 网关渠道 ID 说明：`keys` 中已配置密钥 = 已接入；`hidden_channels` = 已隐藏未启用；`custom_channels` = 自定义渠道（属于已接入）。
> 状态判定规则：
> - **已接入**：网关有该平台直连渠道且已配置密钥
> - **部分接入**：网关有该平台渠道，但只接入了部分能力（如只有对话模型，语音/生图/3D 未接入）；或该平台模型通过其他渠道间接提供
> - **未接入**：网关无该平台任何渠道/模型

| # | 飞书平台名称 | 网关渠道 | 判定 | 说明 |
|---|-------------|---------|------|------|
| 1 | 阿里云百炼 | dashscope（hidden） | 未接入 | 网关有 dashscope 渠道但处于 hidden 未启用状态 |
| 2 | 火山方舟 | ark | 已接入 | deepseek-v4-pro 统一模型组含 ark 渠道 |
| 3 | 智谱AI | zhipu（hidden）+ 间接 | 部分接入 | 直连 zhipu 渠道 hidden；但 GLM-5.2 通过 modelscope/sensetime/openrouter 间接提供 |
| 4 | 硅基流动 | siliconflow（hidden） | 未接入 | 网关有 siliconflow 渠道但 hidden 未启用 |
| 5 | 腾讯TokenHub | 无 | 未接入 | 网关无对应渠道 |
| 6 | 美团LongCat | 无 | 未接入 | 网关无对应渠道 |
| 7 | 商汤SenseNova | sensetime | 已接入 | fast/deepseek-v4-flash/glm-5.2 均含 sensetime |
| 8 | 魔搭ModelScope | modelscope | 已接入 | fast/deepseek-v4-flash/glm-5.2/deepseek-v4-pro 均含 modelscope |
| 9 | KAT-Coder | 无 | 未接入 | 网关无对应渠道 |
| 10 | 火山方舟·语音 | ark（仅对话） | 部分接入 | ark 渠道已接入但只配了对话模型，TTS/ASR 语音模型未接入 |
| 11 | 火山方舟·生图 | ark（仅对话） | 部分接入 | 生图模型（Seedream）未接入 |
| 12 | 火山方舟·3D | ark（仅对话） | 部分接入 | 3D 模型（Seed3D/Hitem3D/Hyper3D）未接入 |
| 13 | 智谱·生图 | 无 | 未接入 | CogView 生图模型未接入 |
| 14 | 腾讯TokenHub·生图/3D | 无 | 未接入 | 网关无对应渠道 |
| 15 | OpenRouter | openrouter | 已接入 | fast/glm-5.2 均含 openrouter |
| 16 | Novita AI | 无 | 未接入 | 网关无对应渠道 |
| 17 | AgentRouter | 无 | 未接入 | 网关无对应渠道 |
| 18 | NVIDIA NIM | 无 | 未接入 | 网关无对应渠道 |
| 19 | Together AI | 无 | 未接入 | 网关无对应渠道 |
| 20 | Fireworks AI | 无 | 未接入 | 网关无对应渠道 |
| 21 | Hugging Face | 无 | 未接入 | 网关无对应渠道 |
| 22 | 小米MiMo | 无 | 未接入 | B.AI 渠道有 mimo-v2.5 模型但属 B.AI 平台，非小米官方直连 |
| 23 | Ox Alpha | 无 | 未接入 | 网关曾配置 ox 模型但已删除 |
| 24 | GMI Cloud | gmi | 已接入 | minimax-m3 统一模型组含 gmi 渠道 |
| 25 | B.AI | bai | 已接入 | deepseek-v4-flash 统一模型组含 bai 渠道 |
| 26 | Cloudflare Workers AI | 无 | 未接入 | 网关无对应渠道 |
| 27 | fal.ai | 无 | 未接入 | 网关无对应渠道 |
| 28 | Google AI Studio | 无 | 未接入 | 网关无对应渠道 |
| 29 | 阶跃星辰 | 无 | 未接入 | 网关无对应渠道 |
| N1 | MiniMax Week × GMI Cloud | gmi | 已接入 | 同 GMI Cloud，minimax-m3 已接入 |
| N2 | Ox Alpha（新增） | 无 | 未接入 | 网关无对应渠道 |
| N3 | KAT-Coder-V2.5-Dev | 无 | 未接入 | 网关无对应渠道 |
| N4 | 腾讯TokenHub·Token Plan | 无 | 未接入 | 网关无对应渠道 |
| N5 | 阿里云百炼·Token Plan个人版 | dashscope（hidden） | 未接入 | 同阿里云百炼，dashscope hidden |
| N6 | 商汤SenseNova·Token Plan | sensetime | 已接入 | 同商汤SenseNova，sensetime 已接入 |

**补充说明（网关已接入但飞书表无对应平台的渠道）**：groq、cerebras、opencode、agnes、zscc、xiaohongshu、zenmux 等渠道在网关已配置，但飞书「免费API活动清单」表中没有对应平台条目。这些**不需要**在飞书表中新增记录，仅在汇报中提及即可。

---

## 4. 第三步：飞书表新增字段

在「免费API活动清单」表新增两个字段（用 lark-cli `+field-create`，字段 JSON 用文件方式传参）：

```json
[
  {"type":"select","name":"网关接入状态","multiple":false,"options":[{"name":"已接入"},{"name":"部分接入"},{"name":"未接入"}]},
  {"type":"text","name":"网关渠道","description":"网关中对应的渠道ID，多个用逗号分隔"}
]
```

- 「网关接入状态」为单选 select，选项：已接入 / 部分接入 / 未接入
- 「网关渠道」为文本，记录对应渠道 ID（如 `bai`、`gmi`、`ark`），未接入留空或写「无」

---

## 5. 第四步：批量更新飞书记录

按 §3 映射表，为 35 条记录分别写入「网关接入状态」和「网关渠道」字段。用 `+record-batch-update`，`update_records` 数组按 record_id 更新：

```powershell
& "C:\Users\郭永涛\.trae-cn\plugins\trae-remote-official\lark\1.0.4\bin\lark-cli.exe" base +record-batch-update --base-token StmDbTXQWaujshs9NpIc3UFpnAc --table-id tblf8nQiNDLtVmWE --json "@api_update.json" --as user
```

- select 字段写入选项名数组：`"网关接入状态": ["已接入"]`
- 文本字段写入字符串：`"网关渠道": "bai, gmi"`
- 单次最多 200 条，35 条一次即可；先 `Get-Content file -Raw | ConvertFrom-Json` 校验 JSON 合法再提交
- 更新后可用 `+record-list` 抽查 3-5 条确认字段已写入

---

## 6. 第五步：输出「未接入优质渠道清单」

生成一份 markdown 报告，保存到 `D:\ai-resource-hub\docs\资源调研\免费资源\API活动\未接入网关优质渠道清单_2026-08-26.md`。

### 报告结构

**6.1 未接入渠道总览表**
列出所有「未接入」+「部分接入」的平台，字段：平台名称、类别、地区、免费额度、免费规则摘要、核实状态、建议优先级。

**6.2 建议优先接入 TOP 榜（重点）**
从未接入平台中，按以下标准排序选出建议优先接入的渠道：
1. **免费额度大**（参考「免费额度」字段：7000万/2000万 Token 级优先）
2. **稳定**（核实状态为「仍有效」优先）
3. **OpenAI 兼容**（API接入 字段含「OpenAI兼容」优先）
4. **免绑卡**（领取方式不含「绑卡」优先）
5. **限时活动**（截止时间临近的优先标注 🔴）

参考排序建议（基于调研报告，Fast 可复核调整）：
- 第一梯队：硅基流动（2000万Token+12免费模型）、腾讯TokenHub（100万Tokens）、阿里云百炼（7000万Tokens）
- 第二梯队：阶跃星辰（30天免费）、Together AI（$25）、AgentRouter（$100-200+签到）、Fireworks AI（$1）
- 第三梯队：NVIDIA NIM（1000 credits）、Hugging Face（免费推理）、Novita AI（$10推荐）、Cloudflare Workers AI（每日10000 Neurons）
- 语音/生图/3D 补强：火山方舟·语音/生图/3D（ark 渠道已有，补模型即可）、智谱·生图（CogView）

**6.3 限时活动提醒**
标注截止时间临近的未接入/部分接入平台（如 Ox Alpha 8-27、GMI 9-6 等）。

**6.4 网关已接入但飞书表无条目的渠道**
列出 groq、cerebras、opencode、agnes、zscc、xiaohongshu、zenmux 等渠道，说明它们已在网关但不在飞书清单中（供用户了解全貌）。

---

## 7. 汇报要求

完成后写简短汇报（可直接粘贴给用户），包含：
1. 飞书表新增字段是否成功、35 条记录标记完成数量
2. 已接入 / 部分接入 / 未接入 各多少条
3. 「未接入优质渠道清单」报告文件路径
4. 建议优先接入的 TOP 3 渠道及理由（一句话/条）
5. 任何无法确认或异常情况

---

## 8. 注意事项

- lark-cli 用 `--as user` 身份；若报权限/scope 不足，停止并汇报，不要自行降级 bot
- 网关接口若 401，检查 api_state.json 的 api_key 是否与请求头一致
- 若飞书表记录的平台名称与 §3 映射表有出入，以飞书表实际名称为准，并在汇报中说明差异
- 全程不修改网关任何配置；只新增飞书字段和更新记录
