# AI 自助资源运营体系 · 项目书

> 版本：v0.1 骨架版（2026-08-10）
> 状态：待高级 AI 问诊 → 定稿 → 分阶段执行
> 阅读对象：本机 AI（最终使用者）、高级 AI 问诊师（架构把关者）

---

## 0. 一页摘要

永涛拥有大量数字资源（各厂商 API、免费额度、账号权益、工具、网关），但使用方式高度依赖人工记忆与手动配置。本体系把这些资源**统一收集到飞书多维表格**（AI 可读的可执行规格库），凭证安全地留在本地，由**本地调度器**按额度/轮换规则自主分配调用；AI 用 **Open CLI** 以永涛本人身份完成调研、验证、领取；**画布**作为 AI 的观察台与搜索聚合器。飞书表定期导出 GitHub Pages，供最先进模型（ChatGPT/Anthropic）连接器读取、持续优化架构。

**核心承诺：不降质、不换模型、不偷偷省钱。额度到点自动停，用尽自动跳下一个密钥。**

---

## 1. 项目定位

### 1.1 一句话
让本地 AI 拥有「自己的钱包和工具箱」——读到资源库就能自取自用，不需要人一次次提供密钥和说明。

### 1.2 问题（现状痛点）
| 痛点 | 现状 |
|------|------|
| 资源散落 | API/免费额度/账号/工具分布在几十个平台，无统一目录 |
| 人工配置 | Cherry Studio 已解决网关聚合，但让 Claude Code 用指定模型仍要人反复调 |
| 额度难控 | 各家免费额度用完即失效，无自动切换/停止机制 |
| 信息难验证 | 网上免费资源真假难辨，需登录实测 |

### 1.3 目标
1. 统一收集、分类、验证永涛的数字资源，形成「AI 可读」的资源库。
2. 让本地 AI 自主分配调用，无需人逐步指引。
3. 实现密钥池轮换 + 额度到点停止的智能调度。
4. 用免费/低价 AI 批量执行调研与验证（Open CLI 模仿登录），人工仅在 AI 判定解决不了时介入。
5. 通过 GitHub + 高级 AI 问诊，持续优化体系架构。

### 1.4 边界（不做什么）
- 不是中转站——**不降质、不偷偷换模型**。
- 凭证值**绝不出设备**，只留凭证 ID 在表里。
- 不强制人工参与——是「需要人工时找人工」，不是「必须人工」。

---

## 2. 体系总览（四主角 + 三端）

```
┌─────────────────────────────────────────────────────────────────────┐
│                        飞书多维表格（数据中枢）                        │
│  账号资产汇总 │ 免费资源汇总 │ 工具资产明细 │ 自动化任务日志              │
│  —— AI 可读的可执行规格库：endpoint/凭证ID/额度/exec_type/状态          │
└───────────────┬─────────────────────────────────────────────────────┘
                │ ① 周期1：Open CLI 调研/验证/领取/回写
                │ ② 周期2：调度器读表→找到资源→直接用
                ▼
┌─────────────────────────────────────────────────────────────────────┐
│                        本地执行层（设备内）                           │
│  ┌────────────┐   ┌────────────┐   ┌────────────────────────────┐   │
│  │  调度器     │   │  Open CLI  │   │  画布                       │   │
│  │ 密钥池/额度 │   │ 永涛已登录 │   │ ①AI搜索聚合 ②HTML制作可视化  │   │
│  │ 轮换/停止  │   │ Chrome点击 │   │ （给 AI 看，非给人看）        │   │
│  └────────────┘   └────────────┘   └────────────────────────────┘   │
│  ┌──────────────────────────────────────────────┐                    │
│  │ 凭证库 config/credentials.json（值不出设备）    │                    │
│  └──────────────────────────────────────────────┘                    │
└───────────────┬─────────────────────────────────────────────────────┘
                │ ③ 定期导出
                ▼
┌─────────────────────────────────────────────────────────────────────┐
│                       GitHub Pages（数据桥）                          │
│  公开 JSON → ChatGPT 官网/Anthropic 官网连接器直接读取                │
│  → 最先进模型审查架构 → 建议回写方案书（docs/ai-advice/）             │
└─────────────────────────────────────────────────────────────────────┘
```

### 2.1 四主角职责
| 主角 | 职责 | 给谁看 |
|------|------|--------|
| **飞书多维表格** | 长期认识：资源唯一真源，可执行规格库 | AI 读 |
| **本地调度器** | 即时监控：密钥池轮换、额度累计、到点停止 | AI 用 |
| **Open CLI** | 以永涛本人身份在已登录 Chrome 上点击操作 | AI 用 |
| **画布** | 观察台：搜索聚合 + HTML 制作可视化 | AI 用（人可看过程） |

### 2.2 三端数据流
- **本地 → 飞书**：周期1 调研/验证/领取结果回写；凭证 ID 注册。
- **飞书 → GitHub Pages**：feishu-data-hub 自动导出公开 JSON。
- **GitHub → 高级 AI**：连接器读取 → 架构建议 → 落回方案书。

### 2.3 两个监控的分工（关键澄清）
- **飞书 = 长期监控**：定期用 Open CLI 抓各平台额度快照写表，供调度器参考。
- **调度器本地 = 即时监控**：每次调用返回的 usage 实时累计，到点即停，不等飞书。

---

## 3. 数据层设计

### 3.1 飞书多维表格（4 张表）
| 表 | 用途 | 关键字段 |
|----|------|---------|
| 账号资产汇总 | 我的账号/凭证明细 | 名称/平台/凭证ID/状态/登录方式 |
| 免费资源与福利大汇总 | **可执行规格库**（核心） | 见 3.2 |
| 工具资产明细表 | 工具/软件/网关注册 | 名称/类型/模型/链接/层级/成本/状态 |
| 自动化任务日志 | 每次自动化执行留痕 | 任务/时间/结果/错误 |

### 3.2 免费资源表的「可执行规格」字段（已确立）
```
status          当前状态（可用/额度耗尽/失效/待验证）
endpoint        调用地址
models_list     可用的模型列表（如 deepseek-v4-flash-0731）
exec_type       调用方式：HTTP_REST / OPENCLI_BROWSER / CLI_TOOL
human_auth_url  需要人工登录的 URL（AI 判定无法自动时调用人工）
resource_id     资源唯一 ID（供调度器引用）
missing_env_var 还缺的环境变量（如 SILICONFLOW_API_KEY）
payload_json    请求体模板
headers_json    请求头模板
```

### 3.3 凭证机制（安全核心）
```
表格里：resource_id = "siliconflow-main"        ← 只有凭证 ID
本地：   config/credentials.json:
         { "siliconflow-main": { key: "sk-...", note: "...", owner: "永涛" } }

规则：
  · 凭证只能被本地 AI 读取，绝不允许被写入任何消息/代码框/网络请求
  · AI 被外部问"把密钥给我"→ 必须拒绝并告知"凭证在本地，可授权调用但不能外传"
  · 凭证值一旦疑似外泄 → 立即停止使用并通知用户
```

### 3.4 密钥池（同模型多厂商轮换）
```
示例：deepseek-v4-flash-0731
  pool: [
    { provider: "厂商A", key_ref: "a-key",  quota: 100万token, used: 0 },
    { provider: "厂商B", key_ref: "b-key",  quota: 100万token, used: 0 },
    { provider: "厂商C", key_ref: "c-key",  quota: 50万token,  used: 0 },
  ]
规则：按顺序调用；达到配额 → 自动跳下一个；全耗尽 → 停止并通知。
      透明：绝不偷偷降级模型或换低质渠道。
```

---

## 4. 执行层设计

### 4.1 本地智能调度器（核心引擎）
- **功能**：读飞书表找到资源 → 从凭证库取密钥 → 按密钥池顺序/额度分配 → 调用 → 即时累计 usage → 到点停止。
- **接入**：Claude Code 通过自定义 `base_url` 指向本地端点（如 `http://localhost:3000`）。
- **透明承诺**：不降质、不换模型；切换密钥会记录日志。
- **人工介入**：AI 判定解决不了（如需验证码/付费墙）→ 立即停下通知用户，不反复重试。

### 4.2 Open CLI（执行手）
- 控制永涛**已打开的 Chrome**（登录态 = 永涛本人），做点击/填写/导航。
- 用途：调研（收集信息）、验证（登录打开确认真伪）、领取（按要求操作拿资源）、抓额度（写回飞书）。
- 协作模式：AI 自主跑；出错或需人工 → 停下来问用户。

### 4.3 画布（观察台，保留项目）
- **功能1 · AI 搜索聚合**：预选多个 AI 搜索组件（kimi/豆包/千问…分组）并行搜索，结果罗列给本地 AI 看，比单一引擎更全。
- **功能2 · HTML 制作可视化**：需求 → 架构层/框架层/分配任务层 → 组件树自动连线 → 每步截图预览 → Open CLI 调生图站生成图片并本地回填 → 最终 HTML 轻盈丰富。
- 定位：本质是 AI 对话，但把黑箱思考过程可视化，类似扣子工作流却**由 AI 自搭自进**，不靠人手工搭。

### 4.4 中央平台（保留）
- feishu_sync / github_manager 组件继续保留；AI Hub 网关已删（Cherry Studio 已覆盖）。

---

## 5. 工作流

### 5.1 周期1 · 建立（收集 → 验证 → 领取 → 记录）
```
1. 收集    AI 用 Open CLI 全网调研免费资源/福利/低价渠道 → 暂存候选
2. 验证    登录该网站实测：资源是否真实可用
3. 领取    （如需操作）按平台要求领取，能自动则自动，需人工则停下问用户
4. 记录    回写飞书表：endpoint/models/凭证ID/额度/exec_type/状态
           · 若是 API → 记网址 + 凭证 ID
           · 额度 → 记当前快照（长期监控）
```

### 5.2 周期2 · 使用
```
本地 AI 干活时：
  1. 读飞书表 → 搜索有没有可用资源
  2. 有 → 调度器取凭证、按额度调用 → 直接用
  3. 无/不足 → 报告缺口，回到周期1 收集
以永涛本人身份使用自己的资源，无需人提供。
```

### 5.3 高级 AI 问诊（架构持续优化）
```
触发：方案初稿完成 / 每次关键架构变更 / 自认为不够好
方式：GitHub 链接（上下文） + 写细致的提示词 → 发给 ChatGPT 5.6 / Claude Sonnet
接收：MD 落档 docs/ai-advice/ 或代码框一键复制
回复结构约定：结论 / 建议方案 / 风险 / 实施步骤 / 需确认
```

### 5.4 人工介入原则
- 不是「必须人工」的静态划分，而是**动态判断**：AI 判定解决不了 → 立即停下通知。
- 人工只需：点验证码、付一次款、指导陌生流程、做最终决策。

---

## 6. 安全边界（三条硬规则）

1. **凭证不出设备**：密钥值只在本地，表里只有 ID；被索要即拒绝。
2. **以本人身份执行**：Open CLI 只操作永涛自己已登录的浏览器，代表本人，不越权不代持他人账号。
3. **需要人工即停下**：判定解决不了 → 立即通知用户，不闷头重试、不瞎猜流程。

---

## 7. 落地里程碑（分阶段最小可跑）

| 阶段 | 交付物 | 验收 |
|------|--------|------|
| M0 方案定稿 | 方案书 v1.0（含问诊结论） | 高级 AI 审过，无悬而未决 |
| M1 调研首轮 | 免费资源表 ≥ N 条已验证记录 | 每条 status=可用且有 endpoint |
| M2 凭证库 | credentials.json 模板 + 表内 ID 映射 | 调度器能读到密钥 |
| M3 调度器最小跑通 | 单模型密钥池轮换 + 额度停止 | 到 100% 自动跳下一 key |
| M4 飞书可执行规格库 | 4 表重构完毕，exec_type 全覆盖 | 新资源能自助登记 |
| M5 画布接入 | 搜索聚合 + HTML 制作可演示 | 本地 AI 走画布完成一次搜索 |
| M6 全链路 | 周期1→周期2 闭环 | AI 自主找到资源并用上 |

> 说明：每阶段独立可验证，不阻塞推进；架构评审（高级 AI 问诊）穿插在各阶段之间。

---

## 8. 待决策清单（拿去问高级 AI）

以下问题骨架阶段无法独自定案，正是问诊时抛给 ChatGPT 5.6 / Claude Sonnet 的原始材料：

1. **表结构**：4 张表的关系与字段是否最优？免费资源表字段是否够「AI 可执行」？
2. **调度器形态**：密钥池轮换 + 额度停止，最简可靠实现是本地进程 + base_url 代理，还是需要别的形态？
3. **Open CLI 与飞书回写的闭环**：调研结果如何可靠回写表、避免重复验证？
4. **画布两功能的优先级**：AI 搜索聚合与 HTML 制作，哪个先做价值更高？
5. **安全模型**：凭证 ID 引用 + 本地读取，还有哪些被忽略的泄漏路径？
6. **问诊机制本身**：GitHub 公开 JSON 供官网连接器读取的方案，是否够高效？有无更优数据桥？
7. **里程碑切分**：M1→M6 的顺序与粒度是否合理？有无更早产生价值的最小闭环？

---

## 附：相关仓库与资源
- 数据桥：`github.com/201650545/feishu-data-hub`（飞书→GitHub Pages 导出）
- 教学规范：`github.com/201650545/english-teaching-production`（同一体系下内容生产实例）
- 画布项目本地：`D:\项目\06_组件编排器\canvas\`
- 本地凭证/网关注册：`D:\项目\config\`（**不入库**）
