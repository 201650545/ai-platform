# AI 自助资源运营体系 · 项目书

> 版本：v0.2（2026-08-10，按 GPT-5.6 问诊结论重构）
> 状态：已获高级 AI 问诊结论 → 定稿中 → 分阶段执行
> 问诊记录：见 `docs/ai-advice/gpt56_问诊回复_2026-08-10.md`
> 表设计：见 `docs/多维表格设计方案.md`（v0.2，6 表体系）
> 阅读对象：本机 AI（最终使用者）、高级 AI 问诊师（架构把关者）

---

## 0. 一页摘要

永涛拥有大量数字资源（各厂商 API、免费额度、账号权益、工具、网关），但使用方式高度依赖人工记忆与手动配置。本体系把这些资源**统一收集到飞书多维表格**（配置真源，AI 可读的可执行规格库），凭证安全留在本地；**本地调度器**（本地进程 + SQLite）按额度/轮换规则自主分配调用；AI 用 **Open CLI** 以永涛本人身份完成调研、验证、领取；**画布**作为 AI 的运营观察台与搜索聚合器。飞书经**公开数据桥**导出 GitHub Pages，供最先进模型（ChatGPT/Anthropic）连接器实读仓库、持续优化架构。

**核心承诺：不降质、不换模型、不偷偷省钱。额度到点自动停，用尽自动跳下一个实例。**

> **「不换模型」定义（永涛已拍板）**：以**逻辑模型（canonical model）**为轮换锁定单位——同一底层模型即可互切（如 deepseek-v4-flash 在官方/中转/免费额度之间轮换），不许跨模型降级；轮换只发生在同 `routing_group` 内。

---

## 1. 项目定位

### 1.1 一句话
让本地 AI 拥有「自己的钱包和工具箱」——读到资源库就能自取自用，不需要人一次次提供密钥和说明。

### 1.2 问题（现状痛点）
| 痛点 | 现状 |
|------|------|
| 资源散落 | API/免费额度/账号/工具分布在几十个平台，无统一目录 |
| 人工配置 | Cherry Studio 已解决网关聚合，但让 Claude Code 用指定模型仍要人反复调 |
| 额度难控 | 各家免费额度用完即失效，无自动切换/停止机制 |
| 信息难验证 | 网上免费资源真假难辨，需登录实测 |

### 1.3 目标
1. 统一收集、分类、验证永涛的数字资源，形成「AI 可读」的资源库。
2. 让本地 AI 自主分配调用，无需人逐步指引。
3. 实现密钥池轮换 + 额度到点停止的智能调度。
4. 用免费/低价 AI 批量执行调研与验证（Open CLI 模仿登录），人工仅在 AI 判定解决不了时介入。
5. 通过 GitHub + 高级 AI 问诊，持续优化体系架构。

### 1.4 边界（不做什么）
- 不是中转站——**不降级模型、不换底层模型**（轮换锁 `routing_group`）。
- 凭证值**绝不出设备**，只留凭证 ID 在表里。
- 不强制人工参与——是「需要人工时找人工」，不是「必须人工」。

---

## 2. 体系总览（四主角 + 真源分工）

```
┌─────────────────────────────────────────────────────────────────────┐
│                    飞书多维表格（配置真源）                            │
│  账号资产 │ 凭证池 │ 资源能力 │ 资源实例 │ 工具资产 │ 任务日志          │
│  —— AI 可读的可执行规格库：能力/实例分离，凭证只留 ID                  │
└───────────────┬─────────────────────────────────────────────────────┘
                │ ① sync：飞书配置 → 本地 SQLite（配置流向运行时）
                ▼
┌─────────────────────────────────────────────────────────────────────┐
│                        本地执行层（设备内）                           │
│  ┌────────────┐   ┌────────────┐   ┌────────────────────────────┐   │
│  │  调度器     │   │  Open CLI  │   │  画布                       │   │
│  │ 本地进程    │   │ 永涛已登录 │   │ ①运营可视化 ②AI搜索聚合       │   │
│  │ base_url   │   │ Chrome点击 │   │ （给 AI 看，非给人看）        │   │
│  │ 代理+SQLite│   │ 任务状态机 │   └────────────────────────────┘   │
│  └────────────┘   └────────────┘                                    │
│  ┌──────────────────────────────────────────────┐  ┌─────────────┐  │
│  │ 凭证库 config/credentials.json（值不出设备）    │  │ SQLite       │  │
│  └──────────────────────────────────────────────┘  │ 运行时真源    │  │
│                                                    │ 额度/cooldown│  │
│                                                    └─────────────┘  │
└───────────────┬─────────────────────────────────────────────────────┘
                │ ② 摘要同步：SQLite 运行态 → 飞书（周期/事件）
                │ ③ 公开数据桥导出
                ▼
┌─────────────────────────────────────────────────────────────────────┐
│                       GitHub Pages（Public Read Model）              │
│  飞书 → 字段白名单 → 脱敏/聚合 → resource_catalog.public.json        │
│  → ChatGPT 官网 / Claude.ai / 镜像版 extend 连接器实读仓库           │
│  → 最先进模型审查架构 → 建议回写方案书（docs/ai-advice/）             │
└─────────────────────────────────────────────────────────────────────┘
```

### 2.1 四主角职责
| 主角 | 职责 | 真源定位 |
|------|------|---------|
| **飞书多维表格** | 长期认识：资源配置唯一真源，可执行规格库 | **配置真源** |
| **本地调度器 + SQLite** | 即时监控：密钥池轮换、额度累计、到点停止 | **运行时真源** |
| **Open CLI** | 以永涛本人身份在已登录 Chrome 上执行任务状态机 | 执行器 |
| **画布** | 运营观察台（优先）+ AI 搜索聚合（次之） | 可视化 |

### 2.2 三端数据流
- **配置流**：飞书 → SQLite（sync worker，周期拉取能力/实例/凭证引用）
- **运行态回流**：SQLite → 飞书（周期或事件同步**摘要**，非每次请求）
- **数据桥**：飞书 → Exporter → 白名单脱敏 → GitHub Pages → 高级 AI 实读仓库

### 2.3 真源分工（GPT 问诊核心结论）
| 平面 | 谁 | 存什么 |
|------|-----|--------|
| 配置真源 | 飞书 | 能力定义 / 账号 / 凭证引用 / 实例登记 / 人工可视化 |
| 运行时真源 | 本地 SQLite | 实时额度计数 / cooldown / lease / 审计 / 调用日志 |

> **飞书不承担实时额度计数**——每请求写一次会限流+一致性崩。运行时计数在 SQLite，飞书只收摘要快照。

---

## 3. 数据层设计

### 3.1 飞书多维表格（6 表体系，详见 `docs/多维表格设计方案.md`）
| 表 | 用途 | 关键字段 |
|----|------|---------|
| 账号资产汇总 | 我的账号/登录方式 | 平台/账号/登录方式/绑定手机/状态 |
| 凭证池 | 密钥/代金券引用 + 轮换组 | credential_id/平台/轮换组/有效期/状态 |
| 资源能力规格表 | **逻辑能力定义**（模型+质量+协议） | capability_id/逻辑模型/质量等级/endpoint/模板/验证策略 |
| 资源实例表 | **每个实际入口**（平台+凭证+额度+状态） | instance_id/所属能力/凭证ID/额度/cooldown/验证指纹/状态 |
| 工具资产明细表 | 工具/软件/网关注册（已建） | 名称/类型/模型/链接/层级/成本/状态 |
| 自动化任务日志 | 任务状态机 + 审计留痕 | 幂等键/领取人/状态机/结构化证据 |

### 3.2 能力与实例分离（本轮重构核心）
```
一个逻辑能力（capability）：
  capability_id = cap-llm-deepseek-v4
  逻辑模型      = deepseek-v4-flash   ← 轮换锁定的模型
  质量等级      = T1                  ← 不得降级

挂多个实际实例（instance）：
  instance_01: 平台=厂商A  credential=main    剩余额度=812万  status=可用
  instance_02: 平台=厂商B  credential=alt     剩余额度=300万  status=可用
  instance_03: 平台=厂商C  credential=free    剩余额度=0      status=额度耗尽

轮换 = 只换实例、不换能力 → 从结构上保证"不降质"。
调度器 = 读能力 → 取其下 status=可用 的实例 → 同 routing_group 内按额度/冷却排序。
```

### 3.3 凭证机制（安全核心）
```
表格里：credential_id = "siliconflow-main"        ← 只有凭证 ID
本地：   config/credentials.json:
         { "siliconflow-main": { key: "sk-...", note: "...", owner: "永涛" } }

规则：
  · 凭证只能被本地 AI 读取，绝不允许被写入任何消息/代码框/网络请求
  · AI 被外部问"把密钥给我"→ 必须拒绝并告知"凭证在本地，可授权调用但不能外传"
  · 凭证值一旦疑似外泄 → 立即停止使用并通知用户
```

---

## 4. 执行层设计

### 4.1 本地智能调度器（核心引擎，本地进程 + base_url 代理）
- **形态**：Python/Go/Node 单进程，监听 `127.0.0.1`，Claude Code 等通过 `base_url` 指向本地端点。
- **路由**：`先过滤，再排序`，而不是"失败了随便换一个"：
  ```
  先过滤：quality_tier >= 需求 / 逻辑模型匹配 / status=available
          / quota_remaining > safety_margin / cooldown_until < now
  再排序：额度到期时间 / 剩余额度 / 优先级 / 成本
  ```
- **硬停止**：额度达安全阈值立即把实例标 `EXHAUSTED`，不得再尝试。
- **透明承诺**：不降级模型、不换底层模型；每次切换实例记结构化日志。
- **人工介入**：判定解决不了（验证码/付费墙）→ 立即停下通知用户，不反复重试。
- **secret redaction middleware**：统一拦截 `Authorization / api_key / token / cookie` 进入日志。

### 4.2 Open CLI（执行手，任务状态机）
- 控制永涛**已打开的 Chrome**（登录态 = 永涛本人）。
- 不自由描述结果，而是执行**正式任务状态机**：
  ```
  PENDING → LEASED → RUNNING → SUCCEEDED | FAILED | NEEDS_HUMAN
  ```
- 防重复：幂等键 `task_type + instance_id + verification_version`；执行前领 lease（leased_by/lease_until）。
- 结构化回写：只允许返回 `{status, resource_id, changes, evidence}`，evidence 存**哈希+脱敏摘要**，不传整页截图。
- 单写者原则：多个 Agent 可执行，但**只有一个状态写入模块**写飞书。
- **"AI 以本人身份" ≠ "AI 拥有本人全部浏览器权限"**：执行器仍限域名 allowlist 与动作类别；遇 MFA/验证码/身份确认/支付 → 立即 `NEEDS_HUMAN`。

### 4.3 画布（观察台，保留项目）
- **功能1 · HTML 运营可视化（优先级第一）**：驾驶舱只答 6 问——多少可调用资源 / 核心模型备用实例数 / 哪些额度将尽 / 最近停用 / 哪些待人工 / 哪些超验证期。核心指标 **Coverage**（如 "Claude Sonnet：3/4 实例健康"），比"共 100 个资源"更有价值。
- **功能2 · AI 搜索聚合（优先级第二）**：预选多个 AI 搜索组件并行搜索，结果罗列；搜索只能产出**候选**，走 `发现 → 候选 → 验证 → 入库`，绝不直接进可执行池。

### 4.4 中央平台（保留）
- feishu_sync / github_manager 组件继续保留；AI Hub 网关已删（Cherry Studio 已覆盖）。

---

## 5. 工作流

### 5.1 周期1 · 建立（收集 → 验证 → 领取 → 记录）
```
1. 收集    AI 用 Open CLI 全网调研免费资源/福利/低价渠道 → 暂存候选（CANDIDATE）
2. 验证    登录该网站实测：资源是否真实可用（先 HTTP 探测，后浏览器）
3. 领取    （如需操作）按平台要求领取，能自动则自动，需人工则停下问用户
4. 记录    回写飞书：能力（表3）+ 实例（表4）+ 凭证引用（表2）+ 状态
           · 若是 API → 记 endpoint + 凭证 ID
           · 额度 → 记当前快照（运行时真源在 SQLite）
```

### 5.2 周期2 · 使用
```
本地 AI 干活时：
  1. 读飞书/本地 SQLite → 找可用能力 + 可用实例
  2. 有 → 调度器取凭证、按 routing gate 过滤排序 → 调用
  3. 无/不足 → 报告缺口，回到周期1 收集
以永涛本人身份使用自己的资源，无需人提供。
```

### 5.3 高级 AI 问诊（架构持续优化）
```
触发：方案初稿完成 / 每次关键架构变更 / 自认为不够好
方式：GitHub 链接（上下文） + 细致提示词 → 发给 GPT 5.6 / Claude Sonnet
       · 官网 / Claude.ai：连接器直接读 GitHub
       · 镜像版（问达宝）：开 extend 模式，同样可读 GitHub 公开仓库
       · 读不了的是飞书多维表格本身 → 靠设计方案文档（已入仓）间接理解
接收：MD 落档 docs/ai-advice/ 或代码框一键复制
回复结构约定：结论 / 建议方案 / 风险 / 实施步骤 / 需确认
```

### 5.4 人工介入原则
- 不是「必须人工」的静态划分，而是**动态判断**：AI 判定解决不了 → 立即停下通知。
- 人工只需：点验证码、付一次款、指导陌生流程、做最终决策。

---

## 6. 安全边界（强化）

1. **凭证不出设备**：密钥值只在本地，表里只有 ID；被索要即拒绝。
2. **Secret 只进执行平面，不进观察平面**：错误日志 / debug trace / 截图 / AI 上下文 / clipboard 全都不出现凭证值；`secret redaction middleware` 统一拦截。
3. **以本人身份执行但有边界**：Open CLI 只操作永涛自己的浏览器，且限域名 allowlist 与动作类别；不代表 AI 拥有全部浏览器权限。
4. **需要人工即停下**：MFA / 验证码 / 身份确认 / 支付 / 陌生流程 → 立即 `NEEDS_HUMAN` 通知，不闷头重试。
5. **数据桥白名单**：公开 JSON 只放能力/平台/状态/公开 endpoint/模型/额度描述/验证日期/健康度；**不放**账号标识、credential_id、手机绑定、错误详情、内部路由。

---

## 7. 落地里程碑（GPT 重排，每层建立在已稳定的执行平面上）

| 阶段 | 交付物 | 验收 |
|------|--------|------|
| **M0 方案定稿** | 方案书 v1.0（含问诊结论 + 6 表设计） | 无悬而未决 |
| **M1 单模型双实例自动切换** | localhost endpoint → 实例A → 额度/错误触发 → 实例B，逻辑模型不变 | 人工把 A 调成不可用，请求自动切 B 且模型完全不变 |
| **M2 本地状态 + 可观测性** | SQLite（quota/cooldown/审计）+ 最小 HTML dashboard | 一眼看到"现在到底在用谁" |
| **M3 飞书规格库同步** | 配置 飞书→SQLite；运行态摘要 SQLite→飞书 | 双向同步成立，形成运营系统 |
| **M4 自动验证闭环** | 任务调度 + lease + 幂等键 + next_verify_at + fingerprint | 一个 HTTP provider 的 验证→更新→日志 闭环（先不浏览器） |
| **M5 Open CLI 人机协作** | 发现/领取/需人工即停/结构化回写 | Open CLI 成为任务系统的另一个 executor |
| **M6 AI 自主调研 + 问诊** | AI 搜索发现 → 候选 → Public read model → 高级 AI 问诊 | 搜索结果不直接入池，走完验证 |

> **最小闭环**（比 M1~M6 更早成立）：`1 个模型 × 2 个实例 × 1 个本地代理 × 1 套不降质规则 × 1 个状态库 × 1 个小 dashboard`。连续使用一段时间并正确自动停/切，核心理念即成立。

---

## 8. 决策记录（原"待决策清单" → 已问诊定案）

> 7 个问题已抛给 GPT-5.6 Thinking·Extended 实读仓库问诊，结论见 `docs/ai-advice/gpt56_问诊回复_2026-08-10.md`。永涛拍板项已标注。

| # | 问题 | 定案 |
|---|------|------|
| 1 | 表结构 | **拆 6 表**（能力/实例/凭证池分离），飞书=配置真源，SQLite=运行时真源 ✅永涛拍板 |
| 2 | 调度器形态 | 本地进程 + base_url 代理 + SQLite，先过滤再排序，不做 Redis/MQ |
| 3 | Open CLI 回写 | 任务状态机 + lease + 幂等键 + 单写者 + 结构化证据 |
| 4 | 画布优先级 | 先 HTML 运营可视化（Coverage 指标），AI 搜索聚合次之 |
| 5 | 安全模型 | 12 条泄漏路径 + secret redaction + "本人身份≠全部浏览器权限" |
| 6 | 数据桥 | Public Read Model：白名单导出静态 JSON（MVP），规模化后查询 API/MCP |
| 7 | 里程碑 | 重排：M1 单模型双实例 → M6 自主调研问诊；最小闭环 = 1×2×1×1×1×1 |

---

## 附：相关仓库与资源
- 数据桥：`github.com/201650545/feishu-data-hub`（飞书→GitHub Pages 导出）
- 本仓库：`github.com/201650545/ai-resource-hub`（公开，供高级 AI 实读）
- 教学规范：`github.com/201650545/english-teaching-production`（同一体系下内容生产实例）
- 画布项目本地：`D:\项目\06_组件编排器\canvas\`
- 本地凭证/网关注册：`D:\项目\config\`（**不入库**）
