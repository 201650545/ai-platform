# 网关项目全景整理 — 交付报告（2026-09-05）

> 由 AI Agent 依据 GPT 三轮问诊落地执行。目标：让散落的网关项目形成「声明/运行/代码/文档」四层一致的事实体系。

## 一、为什么做这次整理

多个网关与服务（central、orchestrator、search_gateway、ds_v4_cli）散落在
`D:\项目\services\*`、`D:\项目\ai-hub\*`、`D:\游戏\ds_v4_cli`，且：

- `config/gateways.json` 停留在 v1，混入 `status: online` 等**运行态**字段；
- 存在两个**孤儿进程**（8898 指向已删目录、8899 无目录）未被识别；
- central/orchestrator 有两处代码副本（`services/*` 与 `ai-hub/00_中央平台`、`06_组件编排器`），
  哪个是运行真源不明确（文档漂移）；
- Obsidian Work 库只有 search_gateway 单服务文档，缺跨服务全景。

## 二、本次完成事项

| # | 事项 | 详情 |
|---|---|---|
| 1 | **孤儿进程清理** | :8898（指向已删 `services/search_gateway/web`）、:8899（无目录裸服务）确认身份后 `Stop-Process`，端口已释放 |
| 2 | **canonical 判定** | 按「①运行入口 ②启动/config/脚本 ③Git/维护信号」多数票定唯一路径 |
| 3 | **gateways.json → schema v2** | 删除 `status/port/url/last_seen` 等混合运行态；新增 central/orchestrator/ds_v4_cli 登记 |
| 4 | **只读探测器** | 新增 `config/inspect_gateways.ps1`，生成 `gateway_runtime.json` + `gateway_drift.json`（禁止手改） |
| 5 | **启动脚本修正** | `启动AIHub.bat` 失效路径 → 真实路径 |
| 6 | **Obsidian 归档** | 新增 `AI基础设施/` 总览/端口/规范/ADR/4 服务页 |

## 三、canonical 判定结果

| 服务 | 类型 | lifecycle | desired | canonical path | 端口 |
|---|---|---|---|---|---|
| central | platform | active | started | `D:\项目\services\central` | 8000 |
| orchestrator | orchestrator | active | started | `D:\项目\services\orchestrator` | 8791 |
| search_gateway | gateway | active | stopped | `D:\项目\ai-hub\search_gateway\services` | 3000/3100 |
| ds_v4_cli | cli | legacy | stopped | `D:\游戏\ds_v4_cli` | — |

> `ai-hub/00_中央平台`、`06_组件编排器` 被判定为参考/历史副本，非运行 canonical。

## 四、当前运行态（inspect 探测）

| 服务 | alive | PID | 端口 | health | drift |
|---|---|---|---|---|---|
| central | ✅ | 1012 | 8000 | healthy | 无 |
| orchestrator | ✅ | 12968 | 8791 | healthy | 无 |
| search_gateway | ❌ | — | — | 已停 | 无 |
| ds_v4_cli | ❌ | — | — | 已停 | 无 |

**drift 数 = 0**，声明态与运行态一致。

## 五、验收核对

- `gateways.json` 中搜 `pid/last_seen` → **0 命中**；`online` 仅见于 `_comment` 说明文字。
- 8898/8899 无 listener → 已清理。
- central/orchestrator canonical 唯一。
- 端口最终：8000→central，8791→orchestrator，3100/3000 空闲（search_gateway 待需启停）。
- Obsidian 服务页不保存 PID/online。

## 六、遗留 / 建议（未强行改动）

- `runtime.yaml` + `runtime.cli` 是另一套 management 工具；其 search_gateway `cwd` 为历史约定，
  已在文件头加注释说明 canonical 迁移事实，**未改运行代码**。
- `state.json` 中 PID（旧 8-21 数据）与实际进程不一致——建议后续人工执行一次 `runtime.cli doctor`
  或手动 stop 属工具状态，不影响服务的真实运行。
- `ai-hub/00_中央平台`、`06_组件编排器` 副本本轮**未删**，若确认不再使用可后续归档。

## 七、后续维护入口

```powershell
# 一键探测（只读，安全）
powershell -ExecutionPolicy Bypass -File "D:\项目\ai-hub\config\inspect_gateways.ps1"

# 输出
#   D:\项目\ai-hub\search_gateway\data\gateway_runtime.json
#   D:\项目\ai-hub\search_gateway\data\gateway_drift.json

# 声明式编辑
#   D:\项目\ai-hub\config\gateways.json
```

Obsidian：`D:\Work\AI平台\docs\04-建设与演进\AI基础设施\`（总览开始读）。