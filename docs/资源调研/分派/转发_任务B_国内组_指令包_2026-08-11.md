# 任务 B · 国内平台组 + 已登录平台细查 · 免费资源调研执行指令

> 本文件是**完整执行指令**，可整体复制转发给外部执行 AI（能操作永涛日常 Chrome 的那一个）。
> 收到后按指令逐平台执行，完成后按文末「汇报要求」写简短汇报。
> 背景仓库：github.com/201650545/ai-resource-hub

---

## 0. 总则

### 0.1 目标
逐平台验证登录态 + 摸清免费额度现状，为资源库第一张「可执行规格表」提供素材。
结果统一追加到本地文件 `D:\项目\ai-resource-hub\docs\资源调研\验证记录_2026-08-10.md`，沿用现有格式。

### 0.2 执行环境（重要：绑定日常 Chrome，禁止乱开浏览器）

> ⚠️ 只允许操作**已打开的日常 Chrome**（session 名 `n8hh7hyn`，已登录永涛账号）。**禁止**新开浏览器窗口/换其他浏览器。判断登录态标准：页面出现「Sign in / 登录」= 未登录；出现用户名/账号菜单/Profile/Settings = 已登录。

用 **opencli** 控制已打开的日常 Chrome：
- `opencli browser n8hh7hyn tab list` — 查看当前所有标签页
- `opencli browser n8hh7hyn open <url>` — 打开页面（优先在当前已开标签导航，不新开窗口）
- `opencli browser n8hh7hyn state` — 看页面交互元素（找登录按钮/账号菜单）
- `opencli browser n8hh7hyn extract` — 提取页面 markdown（判断登录态用 PowerShell 过滤：`Select-String -Pattern "Sign in|Profile|Settings"`）
- `opencli browser n8hh7hyn eval "<js>"` — 执行页面 JS（处理按钮点击等）
- `opencli browser n8hh7hyn find --text "xxx"` / `--testid "xxx"` — 定位元素（**用 opencli click 点击，勿用 eval 直接 click**，eval 点按钮可能不触发 React 菜单）
- 遇到「登录/Sign in」页面：按 0.5 防卡死边界处理，不硬闯

> ⚠️ 注意：另一个外部执行 AI 可能同时在同一 Chrome 上执行任务 A，页面会被切换。**每次操作前先 `tab list` 或 `state` 确认当前页面是自己的平台；被切走了重新 `open` 目标 URL 导航回来即可**，不要新开浏览器。

### 0.3 三条指令（永涛拍板，必须遵守）
1. **能谷歌登录的平台 → 自动点谷歌登录**（Google OAuth，点后确认是否登录成功）
2. **需要手机号 / 微信扫码登录 → 不自动操作**，记录到「待永涛登录清单」（平台名 + 登录方式）
3. **有账号密码登录的平台 → 备注提示永涛**设置账号密码（避免短信验证收费）

### 0.4 每平台记录格式（六节扩展版）

```
## 平台 N · 平台名 · <STATUS>
### 1. 登录状态
- 状态：已登录 / 未登录 / 待人工
- 登录方式：Google OAuth / 手机短信 / 微信扫码 / 账号密码 / 已有登录态
### 2. 免费权益
- 免费额度：具体额度/余额/代金券
- 有效期：
- 免费模型：
### 3. API 能力
- API 入口（URL）：
- Endpoint：
- Key 状态：是否已存在（只记 有/无，不记值）
### 4. 限制
- RPM / RPD / TPM：
- 是否需绑卡：是 / 否 / 不确定
- 是否需实名/手机验证：
### 5. 验证证据
- 最终 URL：与初始 URL 不同时注明跳转原因
- 页面路径：
- 截图编号：（本轮不做，留空）
- 验证时间戳：YYYY-MM-DD HH:MM
### 6. 待办
- 人工动作：
- 原因：
```

> ⚠️ **找不到的字段一律写「未说明」，严禁自行猜测编造数值**——宁可留白让永涛复核，也不能编。

### 0.5 防卡死边界（核心）

| 场景 | 停止信号 | 处理 |
|------|---------|------|
| Google OAuth | 出现「需手机验证/二次验证/短信验证码」字样 | 立即 `NEEDS_HUMAN`，**不得模拟输入验证码** |
| Onboarding 问卷（公司名/用途） | — | 用合理占位值（如 "Personal / Research"）尝试提交 **1 次**；仍卡住 → 立即停止记待办 |
| 绑卡要求 | 出现「需要支付方式才能激活」字样，哪怕声称不扣费 | 一律停止，记入待办，**不得输入任何支付信息** |
| 国内手机/微信登录墙 | 确认登录方式后 | 不点击任何登录选项，直接记录，进入下一平台，不等待/不重试 |
| 单平台总耗时 | 国内组 3 分钟 / 海外 OAuth 组 5 分钟 | 超时立即标注「未探测，时间超限」，跳下一个 |

#### 统一状态值

每个平台记录以状态值开头（`## 平台 N · 平台名 · <STATUS>`）：
- `AVAILABLE_VERIFIED`：已登录，权益及调用规格已核实
- `AVAILABLE_DOC_ONLY`：官方确认存在资源，但本账号权益未登录核实
- `NO_FREE_QUOTA`：已确认当前账号没有免费额度
- `RETIRED`：产品/API 已退役
- `NEEDS_HUMAN`：验证码/扫码/实名/密码/人工表单阻塞
- `UNKNOWN`：官方资料与控制台均无法确认，**禁止自行猜测**

#### 禁止自动执行

- 不创建/删除 API Key；不充值；不绑卡；不开后付费；不订阅套餐；不修改账单设置
- 不领取需同意"付费回退机制"的权益
- 纯技术故障（网络错误/页面加载失败）可重试，但**总 attempt ≤ 3**，达上限转 NEEDS_HUMAN
- 404/入口迁移：最多"旧入口 → 官网导航 → 官方文档搜索"三步，找到官方退役/迁移声明即停，不继续猜 URL

### 0.6 完成后汇报（四段式，必填）
1. **模块 × 验收项**：探测了哪些平台、每平台是否完成验证
2. **变更清单**：验证记录新增/改了什么（写明写入位置：`## 任务B结果` 标题下）
3. **验证**：哪些平台已登录、哪些待永涛登录
4. **已知偏差**：没跑通的平台、猜测、待确认点

### 0.7 返回落点（结果写哪 + 是否 push）
1. **结果写**：本地文件 `D:\项目\ai-resource-hub\docs\资源调研\验证记录_2026-08-10.md`，按 0.4 六节格式，追加到文件**末尾**的 `## 任务B结果` 二级标题下（若无该标题先创建），平台编号 **B1–B6 + C1–C7**。**不改写文件已有的 1–10 号平台内容。**
2. **是否 push**：执行完毕后自己 git commit + push 到 GitHub（`git -C D:\项目\ai-resource-hub push origin main`）。这样永涛随时能从 GitHub 看进度。
3. **不写飞书**：本轮是"调研记录"，飞书是下一步才做的事，不碰飞书。
4. **凭证纪律**：记录里只写「key 是否存在/是否需绑卡」，**绝不写 key/token/密码值**；页面出现凭证值不复制、不落文件。
5. **推送前自查**：`git -C D:\项目\ai-resource-hub diff` 确认无凭证（搜索 `sk-`、`key`、`token=` 等模式），再 commit。

### 0.8 并发写协调
另一个外部执行 AI 正在跑任务 A，也写同一份文件。为避免互相覆盖：
- **本任务（任务 B）结果**写在文件末尾 `## 任务B结果` 标题下，编号 **B1–B6 + C1–C7**
- **不改写**文件已有的 1–10 号平台内容
- ⚠️ push 前先 `git pull --rebase`（对方可能已 push 过），再 `git add` 自己的文件段 commit push

---

## 任务 B · 国内平台组（6 新平台）+ 已登录平台细查（7）

> 目标一：登录方式记录清楚，公开定价页数据顺带抓一份做参考。
> 目标二：把验证记录里已登录但「待细查」的 7 个平台的免费额度细节补齐——不需要新登录，纯读数据，工作量轻。

### B 组 · 国内新平台（6）

| # | 平台 | URL |
|---|------|-----|
| B1 | 百度智能云千帆 | console.bce.baidu.com/qianfan |
| B2 | 腾讯混元 | console.cloud.tencent.com/hunyuan |
| B3 | MiniMax | platform.minimaxi.com |
| B4 | 零一万物 Yi | platform.lingyiwanwu.com |
| B5 | 讯飞星火 | xinghuo.xfyun.cn |
| B6 | 阶跃星辰 StepFun | platform.stepfun.com |

> ⚠️ **B2 腾讯混元特殊说明（GPT Extended 核实 2026-08-11）**：官方已宣布旧混元平台 **2026-09-30 全面停服**，新模型迁往 **TokenHub**。因此 B2 必须**双记录**：①旧混元剩余免费包/有效期 + API Key 是否存在 + 旧接口 Base URL/model IDs（目前仍 OpenAI-compatible）；②TokenHub 是否已迁移、独立 API Key、免费体验/资源包、模型 ID、新 endpoint、原权益能否迁移。记录中加 `migration_status=MIGRATING`、`successor=TokenHub`、`legacy_expiry=2026-09-30`。**不得因旧混元还能调用就标为长期稳定实例。**

**通用动作清单（6 个都按此走）：**
1. 打开控制台 URL → `state` 看是否有「登录/Sign in」按钮或已显示账号中心入口。
2. 已登录（极小概率）→ 查「资源包/额度/用量」页记录免费额度 + API 文档里的 endpoint/模型名/限流。
3. 未登录（大概率）→ **不点击任何登录方式**，只记录登录态❌ + 登录方式；顺带打开该平台**公开定价页**（无需登录也能看）记录官网公布的新户免费额度政策，**标注「来源：官网价格页，未登录验证」**，不能当作已核实数据。
4. 单平台停留上限 3 分钟。

### C 组 · 已登录 10 平台里的 7 项待细查（不需要新登录，纯读数据）

- **C1 SiliconFlow**（验证记录平台 1，已登录）：打开「账户/代金券」页 → 记录代金券金额、可用范围、有效期，判断负余额（¥-0.1083）是否影响使用；顺带查免费模型列表（如 DeepSeek-V3 免费档）。
- **C2 火山引擎 Ark**（平台 3，已登录）：「费用中心/免费额度」页 → 记录每日 200 万 token 免费档是否仍生效、适用模型、到期时间。
- **C3 阿里云百炼**（平台 4，已登录）：「费用/资源包」页 → 记录 7000 万 token/180 天政策是否仍生效、剩余额度、适用模型。
- **C4 Groq**（平台 5，已登录）：`console.groq.com/settings/limits` → 记录各模型 RPM/RPD/TPM 限流表格数值。
- **C5 智谱 BigModel**（平台 7，已登录）：先处理「绑定手机号」弹窗——**不自动确认**，记入待办交永涛；若能跳过弹窗，查 API Keys 页（是否已有 key，不记值）+ 资源包页记录 2000 万 token 新户额度剩余量。
- **C6 OpenRouter**（平台 9，已登录）：`/settings/keys` 记录是否已有 key（不记值）→ 筛选 `:free` 模型列表，记录当前可用免费模型数量与 3–5 个代表性模型名（不必穷举）→ Credits 页记录新用户信用余额。
- **C7 HuggingFace**（平台 10，已登录）：`/settings/tokens` 记录 Access Token 是否已存在（不记值）→ Inference API 说明页记录免费 Serverless 推理的额度政策。

---

## 附：待永涛登录/处理清单（探测后汇总）

| 平台 | 登录方式 | 永涛要做什么 |
|------|---------|-------------|
| （执行后填写） | | |

---

## 汇报要求（完成后必填）

按 0.6 四段式写**简短汇报**（模块×验收项 / 变更清单 / 验证 / 已知偏差），并单独列出「待永涛处理清单」。汇报发回给转发本指令给你的人。
