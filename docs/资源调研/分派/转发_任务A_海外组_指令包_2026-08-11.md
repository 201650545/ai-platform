# 任务 A · 海外平台组 · 免费资源调研执行指令

> 本文件是**完整执行指令**，可整体复制转发给外部执行 AI（能操作永涛日常 Chrome 的那一个）。
> 收到后按指令逐平台执行，完成后按文末「汇报要求」写简短汇报。
> 背景仓库：github.com/201650545/ai-resource-hub

---

## 0. 总则

### 0.1 目标
逐平台验证登录态 + 摸清免费额度现状，为资源库第一张「可执行规格表」提供素材。
结果统一追加到本地文件 `D:\项目\ai-resource-hub\docs\资源调研\验证记录_2026-08-10.md`，沿用现有格式。

### 0.2 执行环境（重要：绑定日常 Chrome，禁止乱开浏览器）

> ⚠️ 只允许操作**已打开的日常 Chrome**（session 名 `n8hh7hyn`，已登录永涛账号）。**禁止**新开浏览器窗口/换其他浏览器。判断登录态标准：页面出现「Sign in / 登录」= 未登录；出现用户名/账号菜单/Profile/Settings = 已登录。

用 **opencli** 控制已打开的日常 Chrome：
- `opencli browser n8hh7hyn tab list` — 查看当前所有标签页
- `opencli browser n8hh7hyn open <url>` — 打开页面（优先在当前已开标签导航，不新开窗口）
- `opencli browser n8hh7hyn state` — 看页面交互元素（找登录按钮/账号菜单）
- `opencli browser n8hh7hyn extract` — 提取页面 markdown（判断登录态用 PowerShell 过滤：`Select-String -Pattern "Sign in|Profile|Settings"`）
- `opencli browser n8hh7hyn eval "<js>"` — 执行页面 JS（处理按钮点击等）
- `opencli browser n8hh7hyn find --text "xxx"` / `--testid "xxx"` — 定位元素（**用 opencli click 点击，勿用 eval 直接 click**，eval 点按钮可能不触发 React 菜单）
- 遇到「登录/Sign in」页面：按 0.5 防卡死边界处理，不硬闯

> ⚠️ 注意：另一个外部执行 AI 可能同时在同一 Chrome 上执行任务 B，页面会被切换。**每次操作前先 `tab list` 或 `state` 确认当前页面是自己的平台；被切走了重新 `open` 目标 URL 导航回来即可**，不要新开浏览器。

### 0.3 三条指令（永涛拍板，必须遵守）
1. **能谷歌登录的平台 → 自动点谷歌登录**（Google OAuth，点后确认是否登录成功）
2. **需要手机号 / 微信扫码登录 → 不自动操作**，记录到「待永涛登录清单」（平台名 + 登录方式）
3. **有账号密码登录的平台 → 备注提示永涛**设置账号密码（避免短信验证收费）

### 0.4 每平台记录格式（六节扩展版）

```
## 平台 N · 平台名 · <STATUS>
### 1. 登录状态
- 状态：已登录 / 未登录 / 待人工
- 登录方式：Google OAuth / 手机短信 / 微信扫码 / 账号密码 / 已有登录态
### 2. 免费权益
- 免费额度：具体额度/余额/代金券
- 有效期：
- 免费模型：
### 3. API 能力
- API 入口（URL）：
- Endpoint：
- Key 状态：是否已存在（只记 有/无，不记值）
### 4. 限制
- RPM / RPD / TPM：
- 是否需绑卡：是 / 否 / 不确定
- 是否需实名/手机验证：
### 5. 验证证据
- 最终 URL：与初始 URL 不同时注明跳转原因
- 页面路径：
- 截图编号：（本轮不做，留空）
- 验证时间戳：YYYY-MM-DD HH:MM
### 6. 待办
- 人工动作：
- 原因：
```

> ⚠️ **找不到的字段一律写「未说明」，严禁自行猜测编造数值**——宁可留白让永涛复核，也不能编。

### 0.5 防卡死边界（核心）

| 场景 | 停止信号 | 处理 |
|------|---------|------|
| Google OAuth | 出现「需手机验证/二次验证/短信验证码」字样 | 立即 `NEEDS_HUMAN`，**不得模拟输入验证码** |
| Onboarding 问卷（公司名/用途） | — | 用合理占位值（如 "Personal / Research"）尝试提交 **1 次**；仍卡住 → 立即停止记待办 |
| 绑卡要求 | 出现「需要支付方式才能激活」字样，哪怕声称不扣费 | 一律停止，记入待办，**不得输入任何支付信息** |
| 国内手机/微信登录墙 | 确认登录方式后 | 不点击任何登录选项，直接记录，进入下一平台，不等待/不重试 |
| 单平台总耗时 | 海外 OAuth 组 5 分钟 | 超时立即标注「未探测，时间超限」，跳下一个 |

#### 统一状态值

每个平台记录以状态值开头（`## 平台 N · 平台名 · <STATUS>`）：
- `AVAILABLE_VERIFIED`：已登录，权益及调用规格已核实
- `AVAILABLE_DOC_ONLY`：官方确认存在资源，但本账号权益未登录核实
- `NO_FREE_QUOTA`：已确认当前账号没有免费额度
- `RETIRED`：产品/API 已退役
- `NEEDS_HUMAN`：验证码/扫码/实名/密码/人工表单阻塞
- `UNKNOWN`：官方资料与控制台均无法确认，**禁止自行猜测**

#### 禁止自动执行

- 不创建/删除 API Key；不充值；不绑卡；不开后付费；不订阅套餐；不修改账单设置
- 不领取需同意"付费回退机制"的权益
- 纯技术故障（网络错误/页面加载失败）可重试，但**总 attempt ≤ 3**，达上限转 NEEDS_HUMAN
- 404/入口迁移：最多"旧入口 → 官网导航 → 官方文档搜索"三步，找到官方退役/迁移声明即停，不继续猜 URL

### 0.6 完成后汇报（四段式，必填）
1. **模块 × 验收项**：探测了哪些平台、每平台是否完成验证
2. **变更清单**：验证记录新增/改了什么（写明写入位置：`## 任务A结果` 标题下）
3. **验证**：哪些平台已登录、哪些待永涛登录
4. **已知偏差**：没跑通的平台、猜测、待确认点

### 0.7 返回落点（结果写哪 + 是否 push）
1. **结果写**：本地文件 `D:\项目\ai-resource-hub\docs\资源调研\验证记录_2026-08-10.md`，按 0.4 六节格式，追加到文件**末尾**的 `## 任务A结果` 二级标题下（若无该标题先创建），平台编号 **A1–A4**。**不改写文件已有的 1–10 号平台内容。**
2. **是否 push**：执行完毕后自己 git commit + push 到 GitHub（`git -C D:\项目\ai-resource-hub push origin main`）。这样永涛随时能从 GitHub 看进度。
3. **不写飞书**：本轮是"调研记录"，飞书是下一步才做的事，不碰飞书。
4. **凭证纪律**：记录里只写「key 是否存在/是否需绑卡」，**绝不写 key/token/密码值**；页面出现凭证值不复制、不落文件。
5. **推送前自查**：`git -C D:\项目\ai-resource-hub diff` 确认无凭证（搜索 `sk-`、`key`、`token=` 等模式），再 commit。

### 0.8 并发写协调
另一个外部执行 AI 正在跑任务 B，也写同一份文件。为避免互相覆盖：
- **本任务（任务 A）结果**写在文件末尾 `## 任务A结果` 标题下，编号 **A1–A4**
- **不改写**文件已有的 1–10 号平台内容
- ⚠️ push 前先 `git pull --rebase`（对方可能已 push 过），再 `git add` 自己的文件段 commit push

---

## 任务 A · 海外平台组（4 平台，Google OAuth 为主）

> 目标：能自动登的都自动登录，拿到免费额度、endpoint、限流、是否绑卡。

| # | 平台 | URL | 备注 |
|---|------|-----|------|
| A1 | GitHub Models | github.com/marketplace/models | ⚠️ 官方 2026-07-30 已完全退役，改为「退役确认」 |
| A2 | Google Colab | colab.research.google.com | 免费 GPU 动态变化，不写死 T4 |
| A3 | Together.ai | api.together.ai | ⚠️ 无免费 trial，需 ≥$5 credits |
| A4 | Mistral La Plateforme | console.mistral.ai | 有 Free mode，查 Limits 页实际额度 |

### 逐平台动作清单

> 以下动作含 GPT Extended 问诊核实到的 2026-08 最新政策。

- **A1 GitHub Models（退役确认）**：打开 `github.com/marketplace/models` 记录最终 URL/状态 → 打开 GitHub 官方 Models 文档找 "retired / July 30 2026 / inference API 已关闭" 等字段 → **确认退役声明后直接填写 `service_status=RETIRED`、`api_available=false`，不要继续找"新入口"**（官方明确 Models 与 Copilot 是两个服务）。若账号已登录可顺手看 Copilot entitlement，但只新增一条"候选资源：GitHub Copilot"记录，**不得把 Copilot 额度写成 GitHub Models 额度**。验收：看到官方退役声明即算完成。
- **A2 Google Colab**：打开首页确认账号头像 = 已登录 → 新建空白笔记本 → 「Runtime → Change runtime type」记录**当前账户实际**出现的硬件选项（不预设 T4）→ 若免费账户可选 GPU：连接 runtime、仅执行一个最小探针（`nvidia-smi`）记录实际 GPU 型号后**立即断开**；若提示 usage limit/需购买 compute units 原样记录 → 看账户是否显示 Colab 订阅/Compute Units。**不写"免费 T4=固定权益"**（Google 官方明确动态变化）。`api_available=false/N/A`，资源类型记"交互式计算/GPU"不是模型 API。
- **A3 Together.ai**：Google 登录（遇 onboarding 按 0.5 处理）→ **先进 Billing/Credits 而非模型页**：记录 credit balance、有无 promotional/historical credits、是否显示充值要求、有无过期时间。官方当前**无免费 trial，需至少 $5 credits**：余额 0 且要求购买 → 直接 `NO_FREE_QUOTA`，禁止充值 → API Keys 只记 `key_exists` 数量，**不创建新 key** → 记录 Base URL `https://api.together.ai/v1`、OpenAI compatibility、当前 model IDs → Rate Limits 记录**动态值**（不写死 RPM/TPM）。历史 free credits 记现有资产；无则归"付费候选"。
- **A4 Mistral**：打开 `console.mistral.ai` → 登录（普通 Google OAuth 可自动完成）→ Admin/Subscription 记录当前 plan（是否 Free mode、pay-as-you-go 是否启用）→ Usage/Limits 记录 included monthly usage / remaining / reset 周期 / RPM/TPM（**以账户 Limits 页实际显示为准，不预填固定 token 数**）→ API Keys 只记 key_exists → Base `https://api.mistral.ai/v1`、auth Bearer、models 从当前页面可见 ID 记录。出现需绑卡才开 pay-as-you-go → **不开**，Free mode 能否继续单独记录；free allowance 耗尽记录下一 reset 时间，不简单写"无免费额度"。

---

## 附：待永涛登录/处理清单（探测后汇总）

| 平台 | 登录方式 | 永涛要做什么 |
|------|---------|-------------|
| （执行后填写） | | |

---

## 汇报要求（完成后必填）

按 0.6 四段式写**简短汇报**（模块×验收项 / 变更清单 / 验证 / 已知偏差），并单独列出「待永涛处理清单」。汇报发回给转发本指令给你的人。
