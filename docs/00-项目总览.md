# 00-项目总览

> curl 2026-09-03 · RFC v3 已拍板进入执行 · 本仓已 9→6 整合
> 本文件是**统一知识入口**：只做摘要与指向，不复制任何运行事实。

## 本仓速览

主仓 **ai-platform**（本地 `D:\Work\AI平台`，根仓即 Obsidian vault）。面向陌生 Agent：
读本文件 + 指向的各文档，即可回答「平台能力在哪 / 记忆协议在哪 / 飞书导出在哪」三类基础问题。

四模块构成：

| 模块 | 目录 | 内容 |
| --- | --- | --- |
| 平台主干 | 根 | 多网关 API 聚合（:3000 搜索 + :3100 API 转发）、编排、GitHub 管理、飞书集成 |
| AI 自助资源运营 | `resource-ops/` | 资源运营脚本与流程 |
| 飞书同步管线 | `integrations/feishu/` | 飞书多维表格 → GitHub Pages 数据管线 |
| Agent 记忆协议快照 | `agent/memory/` | 记忆协议文档快照 |

## 平台能力

见 [[03-规格与规范]] 的平台主干架构摘要与边界定义。能力概览：
- **搜索聚合**：:3000，多引擎 AI 搜索入口。
- **API 转发**：:3100，按编排模型组转发，统一鉴权与用量统计。
- **编排 / 委派 / 工具链**：平台主干承载，任务看板见 [[01-任务看板]]，资产与通道见 [[02-资产清单]]，运行细节见 [[运行手册/]]。

## 核心子项目 / 服务

### search_gateway

- 类型：API 转发网关 / 模型聚合服务
- 所属项目：ai-platform
- 形态：仓内子项目
- 技术栈：Python
- 协议：OpenAI Compatible API
- 监听端口：3000（搜索）/ 3100（API 转发）
- 代码与数据：`apps/search-gateway/`
- 当前状态：Active
- 详细文档：[[00-search_gateway-总览]]（及其 [[ADR-002-配置三拆与显式备用链]]）

> 本入口只说明"存在 + 唯一详细入口"，不维护配置字段 / fallback 逻辑 / 迁移历史（那些在子项目文档与 ADR）。

## 记忆协议与运行仓

`agent/memory/` 仅存**协议快照**。运行期数据**真源在其独立运行仓 ai-hub-memory**（分层：AGENTS / STATE / DECISIONS / CHANGELOG，执行前读前两者、做完写后者）。不要把本仓记忆目录当作运行数据源，勿混淆。

## 飞书数据管线和 Pages

`integrations/feishu/`：飞书多维表格 → GitHub Pages 静态 JSON，为 AI 工具提供可读数据中心。同步管线当前绿色，Pages 已上线：

- https://201650545.github.io/ai-platform/

## 命名原则

用户 2026-09-03 拍板：
- **人看层全中文**（笔记 / 文档 / 看板 / 项目名）。
- **代码 / CI / import 引用路径保留英文不重命名**（平台限 ASCII）。
- 本文档对关键英文路径注中文名便于对照（见上表「内容」列）。

## 相关入口（多 vault 索引）

本仓不是唯一入口。工作区索引 `D:\Work\项目索引`（projects.yaml + STATUS）是多仓的外部路由入口，**只指向不回流**。陌生 Agent 若需跨仓定位，先经该索引。

## 关联文档

- 根 **README.md** 保留为 GitHub / 陌生 Agent 入口，并指向本文件（Home）。
- **ARCHITECTURE.md 不是整仓架构真源**：平台主干架构摘要已进 [[03-规格与规范]]。
- [[01-任务看板]] · [[02-资产清单]] · [[03-规格与规范]] · [[运行手册/]] · [[migration/path-map]]（9→6 整合路径映射）