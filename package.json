{
  "name": "feishu-learning-english-export",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "check": "node --check scripts/sync.mjs && node --check scripts/validate.mjs",
    "sync": "node scripts/sync.mjs",
    "validate": "node scripts/validate.mjs"
  }
}
