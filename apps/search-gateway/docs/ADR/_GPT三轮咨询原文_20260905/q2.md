你的方案很清晰，我准备照着落地。落地前还有 4 个实际操作边界要你拍板，请就每条给明确结论（不要泛泛，直接说"应该/不应该 + 一句话理由"）。

【现状补充】
- ai-platform 仓库 = D:\项目\ai-hub（本地工程根），但 Obsidian work 库对应子库在 D:\Work\AI平台，两者是"工程代码"与"知识文档"两套位置，不是同一目录。
- search_gateway 已迁入 D:\项目\ai-hub\search_gateway。
- projects.yaml 目前顶层只有 ai-platform 一个 entry，其 schema 目前**没有** components / services 子字段。

【4 个问题】
1. 你建议给 projects.yaml 的 ai-platform 加 components（含 search_gateway）。但项目索引 README 的口诀是「先 WHERE 后 WHAT 再 FACT」，且 path-map 显示 2026-09-03 刚整合三仓进 ai-platform、还没验收入库。现在为一个子项目就改动 projects.yaml schema，会不会太早、超出"三仓整合收尾"范围？还是说加一个无侵入的 component 字段反而是好时机？
2. 任务卡我见 ai-platform 现有 `docs/04_任务卡/`（你建议放这），但你说"若现有 04_任务卡 不在 docs/ 下保持原位置"。实际现状是 `04_任务卡` 在**工程库 D:\项目\ai-hub\04_任务卡**，而 Obsidian work 库 `D:\Work\AI平台\docs\01-任务看板.md` 里引用的 `04_任务卡/task_XXX.md` 指向的是**工程库目录**。那么 search_gateway 的 SG-001/002 任务卡，到底应该落在工程库还是 work 库？两边怎么避免写两套账？
3. 你说"补充两张已完成任务卡 SG-001/002"。但这两件事其实连"验收标准"都还没定义过（因为过去是随手做的，不是从任务卡驱动的）。补"已完成"任务卡时，验收结果这栏没经历过真正验收，直接填"通过"会不会是编造成就？还是说补卡本身就是回头补记为验收证据？
4. Obsidian 里建的 search_gateway 目录是"人工知识真源"，但工程代码变更（如新增模型、改路由、改配置）发生频率很高。如果知识文档要求每次代码改动都要同步更新 Obsidian 对应文档，会不会变成沉重的 double-entry 负担？你建议的"同步触发时机"到底是什么——是每次提交、还是里程碑、还是仅在 ADR/规范层面变动时？

请直接给 4 条可执行的结论式回答。