你前两轮的结论我全部接受，尤其"新任务卡回归 Work、停止双写本次生效"和"回溯卡验收写待复核、不编造成就"这两条我立刻采用。最后第三轮，我还需要你给 3 个收尾把关，请直接给结论：

1. 端口职责问题：你在总览里要求"3000 / 3100 端口职责不要靠记忆写，标待核实"。但我现在确定知道的：3100 是 API 转发网关（OpenAI 兼容对外服务），3000 是另一套搜索网关（含历史记录）。不过为了让文档严谨，我打算在"架构与运行"里只登记"已确认事实 + 明确标 3100=API转发网关、3000=搜索网关"，并把尚未核实细节标待核实。这样处理可以吗？还是建议连"3100 是网关"这种已确认的都先不写死？

2. 落地的顺序：你第一轮给了 12 步，第二轮又改了"先不动 projects.yaml + 任务卡只落 Work"。请给一份**合并后的最终精简落地顺序**（去重、按依赖排序），让我能一次照做、不用来回翻前面。

3. 关于 channel_registry（渠道状态）在 Obsidian 的边界：你说 Obsidian 只记字段语义、不记真实 credential。但 search_gateway 的 channel_registry.json 里本来就存真实 quota/health 运行态。那么 "02-资产清单 里登记 channel_registry.json 这个配置资产" 时，文档里是否需要注明"该文件的真实运行内容在工程 data/，Obsidian 只登记职责"以防以后有人误以为 Obsidian 该同步其内容？请给一句应写进资产清单的模板说明。

请就 3 条分别给一句话结论（若某条你认为我已有正确判断，直接说"对，照做"即可）。