我有一个本地 API 转发网关（OpenAI 兼容的多渠道模型聚合转发服务），刚把它作为子项目迁进了我的 AI 项目工程目录。现在我要按团队的 Obsidian 知识管理规范，把这个「建设与开发」记录进本地的 Obsidian「Work」工作库（D:\Work），作为长期可维护的知识真源。请你作为 AI 网关 + Obsidian 知识库架构专家，给我一份具体、可直接落地的记录方案。

先给你事实背景（不用联网，基于我描述）：

【子项目现状】
- 服务名：search_gateway（API 转发网关），OpenAI 兼容，多渠道模型聚合转发，监听 3000 / 3100 端口。
- 技术栈：Python，目录结构 = `services/`（代码）+ `data/`（配置与运行数据）两段式。
- 刚做了两件大改动：
  1) 从旧目录整体迁入 AI 工程主目录作为子项目（services + data 已就位）；
  2) 按「配置三拆」重构：model_catalog.json（产品目录/alias 展示）、model_routes.json（路由 primary/backup/fallback）、channel_registry.json（渠道状态 credential/quota/health），并新增 catalog_routes.py 模块。目标是废弃旧的「多渠道智能轮动 + 隐式 fallback」，改成「默认单渠道单模型 + 显式备用链」。

【Work 库现状（D:\Work，Obsidian，本地唯一可写真源）】
- 是一个多子库合并的 vault：含 Handbook(通用规范)、项目索引、各项目文档仓。
- 其中有「AI平台」子库，对应 GitHub 仓 ai-platform（2026-09-03 刚由 ai-hub + ai-resource-hub + feishu-data-hub 三仓整合而成），已建 docs/ 四件套：00-项目总览 / 01-任务看板 / 02-资产清单 / 03-规格与规范。
- 知识管理三工具规范（50-知识管理三工具规范）定死：Obsidian=人工知识的主编辑区+本地真源；GitHub=版本历史+AI 公共读取镜像（单向发布，不反向写）；飞书=结构化数据管理界面。禁止双向同步。

【我的核心困惑 / 要你决策的点】
1. search_gateway 作为子项目，在 AI平台 子库里到底「放哪、以什么形态」记录最合理？是独立一篇建设开发文档，还是并入已有的 docs/ 四件套，还是两者结合？
2. 「建设开发记录」具体应包含哪些结构字段（现状 / 决策记录 / 演进历史 / 端口拓扑 / 数据流），既能当长期知识又能让后续执行 Agent 顺着读？
3. 三拆重构 + 迁入子项目这两件事，应该作为「一条决策记录」写进哪、还是作为「多张任务卡」登记到 01-任务看板 + 04_任务卡？两者怎么分工不重复记账？
4. 这套记录放好之后，如何与 D:\Work\项目索引\projects.yaml 的登记保持一致（因为三仓整合成 ai-platform，网关子项目算不算一个独立 entry）？

请给我：最终的推荐目录结构（含具体文件名）、每个文件该写的核心内容/字段、以及落地的先后顺序清单。直接用动作化清单回答，我要照着做。