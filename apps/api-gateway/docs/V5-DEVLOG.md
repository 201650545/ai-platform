# V5 DEVLOG

> `apps/api-gateway/v5/` 的开发进程记录。方案见 `V5.1-方案.md`，谱系见 `前端演进复盘-V3-V4-V5.md`。

## 2026-09-09

- **00:3x 归档迁移**：`Downloads/ai-gateway-complete-v3` → `prototypes/v3-complete`；`ai-gateway-v4-complete`（GPT 版 V4）→ `prototypes/v4-gpt-complete`；`ai-gateway-v4-experiment`（我的实验版）→ `prototypes/v4-experiment`。跨盘剪切，源目录已确认删除；指向旧路径的 3199 静态服务已停。
- **00:4x 文档入库**：复盘与 5.1 方案写入 `docs/`。V5 方案批评要点：GLB+PBR 资产管线无人能稳定产出→改全程序化；Vite/GSAP 与库内开发冲突→零构建；六阶段秒表过早→状态=f(u) 单参数；GPU benchmark 过度→单档+动态降 DPR；逐页相机微移与 3D 路由线=scope creep→降曝光+SVG 覆盖层。
- **待办**：vendor three.js → Gate1 → Gate2 → Gate3 → Gate4 框。
- **01:0x vendor**：three.js r169 落到 `v5/vendor/three.module.js`，零构建 ES module，离线可开。
- **01:1x Gate1 通过**（白昼静止像建筑效果图）：三层山脊 z=-52/-95/-170、amp 6/14/26，靠 crest 角变化叠剪影；太阳 y=38 抬到远脊之上；柱廊改浅进深 2.0 + 三柱 + rotY 0.46 面向相机，bay 透天才不读成黑块。坑：① 地形面伸到相机近/后方会让近坡糊满全屏，平面两边缘必须 smoothstep 高度衰减（`ramp`）消掉切口悬崖；② main.js 每帧 `lookAt` 会覆盖 scene.js 的相机朝向，两处必须同值 (0, 6.0, -18)；③ 光作弊方向改前右 `sunDir.set(sx*0.6+20, …, 25)`，坡面才有体积而不是背光纸片。出图 `previews/v5-day.png`、`v5-day-1920.png`。
- **01:1x Gate2 通过**（昼夜 = 单参数 u∈[0,1]，5.2s ease）：四 stop 调色板；太阳/月亮/星/雾面一律 `fog:false` 否则距离雾把天体擦没；UI 墨色翻转阈值 `u>0.5`（原 0.85 让蓝调时刻深墨压深底）。出图 `v5-sunset / v5-blue / v5-night` + 过渡中帧 `v5-morph-a/b`，证明 morph 真经过中间态。
- **01:2x Gate3 通过**（DOM Dashboard + i18n）：CDP probe 全绿——boot u=1/night、中英切换、戏剧时钟（夜 -3.5h，正则 ^\d{2}:\d{2}:\d{2}$）、主题按钮 u 1→0.89→0 且 night class 翻转、nav 点击进/出占位页、fps≈89.9、零页面异常。清债：V4 移植时 toast 调用已丢、词典也无 key，bench 卡片已常驻双语承载占位文案，故删掉死 toast（CSS+markup）。
- **01:2x Gate4 通过**（六页降级 + 性能自适应 + 1920）：按方案只降场景曝光不动相机——`body.paged #gl{filter:brightness(.5) saturate(.75)}` 0.7s ease + 工作台占位卡；probe 实测 filter 双向过渡中值（0.58→0.5 / 0.91→1）。性能单档：DPR≤1.25、shadow 1024、`document.hidden` 暂停；fps EMA 看门狗 <28fps 持续 3s 降 DPR=1、>50fps 持续 6s 回升。功能实测：CDP dsf=2 + CPU throttle 20×，pixelRatio 1.25 → 12.6fps 触发降 1 → 撤 throttle 回升 1.25。1920×1200 昼/夜均成立。出图 `v5-page-catalog.png`、`v5-night-1920.png`。
- **验收工具**：`%TEMP%/v5shot.mjs`（四时刻+1920+morph 帧）、`v5probe.mjs`（Gate3 断言）、`v5perf.mjs`（看门狗），走 CDP 9229；Chrome 对 http 文档有启发式缓存，URL 必须带 `?v=N`。
- **预览**：http://127.0.0.1:3200/index.html （v5 目录静态服务；3100 线上网关全程未碰）。
- **局限/下一步**：六页仍为占位（无真实分页内容）；paged 时 HUD 保持全强度是方案决定（只降场景）；窗口 blur 但可见时不暂停是有意为之——冻住用户看得见的窗口才是错的；手动画质三档留给 Settings；KPI 为静态演示数。
- **08:28 归档复原**：用户发现 Downloads 里 V4 实验版"消失"——实为 00:3x 归档迁移做了**剪切**。已把 `prototypes/v4-experiment` 复制回 `Downloads/ai-gateway-v4-experiment`（24 文件核对一致，Obsidian 份保留作备份）；另把仍只在 Downloads 的 `ai-gateway-prototype-v2` 复制入库为 `prototypes/v2-prototype`（35 文件）。至此 V2/V3/V4(两版) 全部在库内 `apps/api-gateway/prototypes/`。教训：用户日常从 Downloads 双击预览，动它之前先问"剪切还是复制"。
- **10:32 V5.1 入档**：用户报"V5.1 没放进去"——核查 v5 活线其实一直在库内 `apps/api-gateway/v5/`（19 文件齐全），只是不在 prototypes/ 里。按"每代并排留档"口径，快照一份到 `prototypes/v5.1-gates-passed`（Gate1-4 通过时点）。注意：快照是死档，后续 v5 活线改动**不会**自动同步；版本里程碑时手动重新快照。
- **11:00 视觉判决与 V5.2 立项**：用户并排看完 3199/3200/3201——V5.1 程序化观感**完全否决**（引擎保留、皮全换）；V4 点名两处硬伤（比例失衡、建筑写实混凝土 vs 山水墨雾画风硬拼）。用户提出"只太阳与月亮做三维"，随后拍板两叉：建筑**重画成同风格**、底盘 **V4 素材修整复用**。设计定型为"V4 皮 + V5 骨"：手绘分层底盘 + u 昼夜引擎 + 透明 WebGL 三明治层画日月星（放山层下，山脊遮挡天然正确），建筑屏高占比 78%→50%，parallax 改分层位移。方案已写入 `docs/V5.2-方案.md`，**待外部评审，评审未过零代码**。
- **~13:30 V5.2 实验版建成（评审被"先做实验版"指令取代）**：用户指示跳过外部评审直接出实验版。`v5.2-experiment/`（20M，不动 v5 活线）：V4 九件资产复用 + ImageGen 重画水墨平顶门楼（1024×930）+ three.module.js；src 五文件（timeline computeState=u 的 smoothstep 谱、scene3d 像素 1:1 透明叠加日月星、main u 引擎+分层视差+看门狗、ui/i18n 自 v5 移植，drawRoutes 锚点改回 V4 构图）。3202 静态服务 + `打开预览.bat`。**合成三坑**：① 带 transform 的包裹层会把后代的 mix-blend-mode 隔离成独立合成组——multiply 混到透明等于白底方块（山层自身 transform 不隔离自身混合，对照即知）；② multiply 白底并非纯 255（雾树区 243-254），深夜背景下 2% 暗块成"隐框"——白底反 premultiply 转真 alpha（`alpha=255-min(r,g,b)`）彻底根治，边界台阶 ≤0.4/255；③ 山脊昼夜双图慢交叉淡化必然出现瞬态隐框，改快速交换带（u 宽 0.10，与 V4 等价的快闪）。**编排一坑**：天先黑、地后黑的错拍让 u=0.5 出现横贯亮带——时间带重排为全场景 ~u=0.55 一起落夜。太阳换暖色径向盘+halo 垫底（renderOrder=-1，否则 additive 光晕把盘冲没）；月亮贴图加明暗端和月海、halo 收敛防"第二个月亮"；夜窗光降密度防"灯笼化"。**GateA-D 全过**：昼（建筑融景、太阳被右脊遮挡）、u=0.25 夕阳、u=0.395 morph 中帧、u=0.68 月出半山、u=1 夜（星/月/窗光）、占位页降曝光、1920×1200、2x 字屏清晰（1x 截图的文字重影是无头 Chrome 渲染伪影非 DOM bug）；全程 fps≈90 零页面异常，看门狗逻辑与 V5.1 已验证代码同源。验收集 8 张入 `v5.2-experiment/previews/`。**预览：http://127.0.0.1:3202/index.html**（与 3199=V4、3200=V5.1 并排对比）。局限：路由线仍为静态演示、KPI 假数、外发评审未做（升级到活线前补）。
- **~14:20 用户两处反馈修正**：① 左山"断层"——V4 为衬 86vh 黑柱廊把山层左缘羽化到全透明，换小号 alpha 门楼后羽化带裸露成垂直切崖；改为左缘保留 40-55% 墨色底、15%→42% 长渐升（fg 同理 34%→38%），左右山势连续。② 路由流动虚线点换**大雁**意象：SVG `<animateMotion>+<mpath>` 让"远雁"笔触（双弧翼形，SMIL scaleY 扇翅）沿原路径滑翔，7 只（主线 2、其余各 1，14-21s/趟、负值 begin 错峰）——比原 dash 点阵（50+ 点）少而慢；路径线本体改静态淡点线（`1 7`、opacity .3/.5），动感全交给雁。日/夜/morph/1920 帧全重验，previews 八张已同步刷新，fps≈90 零异常。
- **~14:45 用户报"不能切黑夜"——点击黑洞修复**：真实鼠标点不到任何 .ui 控件。根因：装饰用全屏 `svg.routes` 在 `.ui` 容器内且 DOM 顺序靠后，`.ui *{pointer-events:auto}` 使其成为全屏命中层，吞掉问候/按钮/导航全部点击（仅 DOM 更靠后的 .bench 幸免）。修复一行：`.routes` 加 `pointer-events:none`。**流程教训**：此前 GateA-D 验收全用 `V5.setU()/V5.page()` 直调，从未发真实鼠标事件，故未暴露；此后 UI 交互验收必须走 CDP `Input.dispatchMouseEvent`（移动+按下+抬起）而非仅 `Runtime.evaluate` 直调。复验三项全过：真点击 themeBtn → u 0→1 入夜（5.2s 动画正常）、真点击导航 → bench 弹出+高亮正确、再点 → 回昼。
- **~15:10 用户三处反馈修正（虚影/轨迹/建筑）**：① **落日虚影根治**——WebGL canvas 从全屏裁到天区带（`#gl height:var(--horizon)`），地平线以下永不渲染，落日/升月被地平线硬切，地面雾区零透光；配套 scene3d resize 改读 getBoundingClientRect + apply() 每帧自检重建（clientHeight 在无头/早初始化时为 0 的启动竞态曾致缓冲 900×CSS 558 竖向压扁、太阳成蛋形）。② **日月轨迹物理化**——太阳从核心标位沿弧线（x .56→.35，y .31→.62 二次减速）落向中央山体左肩，淡出带 .28-.38 让它"沉入山雾"且不进淡墨区（晚淡出会在浅雾上留可见残盘）；月亮从东侧山脊后升起（x .86→.54，y .72→.325），靠 canvas 地平线切割实现"从未升到升"；整体向西漂移，不再同一点又落又升；uiNight 翻夜阈值 .5→.42（u=.42 天已 98% 黑，旧阈值让暗字在暗天上不可读十分之一行程）。③ **建筑收敛**——arch opacity .92 + 底缘 mask 羽化沉入雾带，夜态 bright .52→.42/sat .72→.62，窗光四组再降 25%。**坑**：无头 Chrome 复用同标签页内存缓存，模块 JS 无 cache-buster 会一直跑旧代码——验收脚本必须 Network.setCacheDisabled(true)。验收集 8 张重拍（u395→u33 sundim、u68→u75 moonrise），真实鼠标回归三项全过，fps≈90 零异常。
- **~15:45 用户点名"黑夜时把建筑改成游船的意象"——夜态换装上线**：ImageGen 出水墨画舫（1536×1024，与门楼同风格：暖褐灰、卷篷舱、船尾挂灯笼、船底淡墨晕当水痕）→ 白转 alpha 入 `assets/scene/v52-boat.png`（成图 1344×547）。**两个坑**：① 生成图右下角带 "Qoder AI" 水印，白转 alpha 会把它一并转进透明层——先按角部矩形置 alpha=0 再裁 bbox；② 首次转换抄错配方（拿 SAFE 常数代替减白贡献），所有不透明像素塌成纯黑剪影——正确逆 premultiply 是 `C=(c-(255-a))*255/a`（v52-arch 当初用的就是它）；**转换后务必先在夜底色上合成预览检查颜色是否保留**。**换装编排**：首版门楼/船共用一条 band（.54-.70）交叉淡化，u=.62 出现双影；改真先后手——门楼消散带 archSwapK .46-.58 → 留白一拍（u=.62 空水面）→ 船浮现带 boatK .58-.72；`arch.opacity=.92×(1-archSwapK)`，archLights 随门楼同灭，boatLights=lightsK×boatK（舱窗两扇+船尾灯笼+舱顶轮廓光+船底水面微光，screen 混合），船本体夜态滤镜 brightness .6/sat .72，视差同 -14。验证：u0 白天不变（船 opacity 0）、u52 门楼沉入雾、u62 留白、u100 夜船+窗灯+灯笼，previews/boat-*.png 四张；真鼠标回归三项全过，fps≈90 零页面异常。
- **19:14 用户两处反馈修正（日月变形/水山交界）**：① **日月非正圆根治**——日月原是 `SphereGeometry`+经纬贴图：球面 equirect 映射把径向渐变在边缘经向压缩，轮廓明暗不匀读成椭圆；改为**面向相机的 PlaneGeometry(2,2)**（z=0 平面正对相机无需 billboard 代码），屏上形状=贴图里画的圆，像素级正圆。连带效应：盘边从球面模糊变锐利，u=.36 落日残盘在两脊间天空缺口处重新显出淡圆环——落日淡出带 .28-.38 收紧提前到 **.27-.35**（u=.30 仍是浑圆半沉的好落日，u=.36 起零残留）。② **水山交界雾化**——mid/fg 山层竖向 mask 加底缘羽化（`#000 58% → rgba(.42) 82% → transparent 100%`，fg 同理 54/80），山脚全墨直切水面的硬线化进雾里；floor 顶羽化 7%→14%（24px→53px 渐变带）；fogNight 终值 .28→.34、fogDay .13→.17 加浓交界雾带。验证：日月放大裁片正圆（山脊遮挡是层间前后关系，自然）、昼/夜水线裁片云雾过渡、u30/33/36 三连帧落日干净，验收集 13 张全刷新（删 gateC-u68 旧带名遗留），真鼠标回归三项全过，fps≈90 零异常。
- **19:25 用户两处反馈修正（星空排列/大雁太抽象）**：① **星空自然化**——原 150 颗均匀随机撒点+方形点被否；重写 placeStars 为三层结构：银河尘带（210 颗 1.7px 冷白，沿 (0.06W,.52H)→(0.94W,.16H) 对角带±高斯散布）+ 中星（92 颗 2.7px，一半绕 5 个随机星团中心高斯成簇、一半散布，带 ±20% 慢闪烁）+ 亮星（13 颗 3.9px 暖白稀散）；点材质全部换 32px 径向渐变圆点贴图（PointsMaterial 无 map 时方块点）。② **大雁去代码化**——SMIL 笔触雁形全部废弃；ImageGen 出"三只同款水墨雁横排、扬翅/滑翔/压翅三相"图，白转 alpha 后按空白列投影切分（列墨量阈值 30 找 gap）成三帧精灵（体长天然一致 422-445px 无需归一）；`.birds` HTML 层（z-30）+ main.js 每帧 `getPointAtLength` 沿原六条航线驱动 7 只（preserveAspectRatio=none 的 viewBox 1440×900 到 CSS 就是纯非均匀线性缩放 `x*vw/1440, y*vh/900`）；扇翅=FLAP[0,1,2,1] 三相循环（上→滑→下→滑），图 bottom:0 锚身体线防换帧上下窜；夜态 CSS `filter:invert(.92) saturate(.45)` 变月光白。**坑**：`.birds *` 必须 pointer-events:none（子元素默认 auto，和 routes 同一个点击黑洞坑）；纸纹噪点 alpha 20-60 会被 bbox 当墨——切分阈值提到 60 并做软下限。验证：昼/夜雁群沿航线飞（白天墨色、夜态月白）、星空裁片自然，验收集 12 张全刷新，真鼠标回归三项全过，fps≈90 零异常。
- **20:14 用户五处反馈修正（山的虚实/白天建筑/雁线/月遮/日月轮转）——本轮大改**：
  ① **夜山真实心**。用户判"山的虚实不明、透视不对"。三层根因连挖：夜山 PNG 沿用 V4 的左缘羽化 mask（月亮 x 处层透明度仅 73%）→ 夜层 mask 删横向项只留竖向（真 alpha PNG 本就不需要缝）；multiply 墨色不够黑（亮盘残留 ~10%）→ 转换加**墨色压深** `k=(A-120)/60, f=1-0.85k` 只压实心区色通道、软边晕留光；月亮出生点落在非实心带（mid 实心带 x=.30 处仅 y .415-.49 撑不住 108px 盘）→ 出生点改**最左 fg 山体**（唯一上下通体近黑实心的山）。alpha 曲线改陡峭实心化 `A2=(A-60)*255/45`（体 ≥105→255）。**multiply 遮挡物理**：DOM 山层盖在 WebGL canvas 上方，几何遮挡只在"层不透明度×体 alpha≈1"处成立，半透明晕区（昼山/前山脚/鞍部/雾脚）必透亮盘——这是本引擎的硬约束，记入 memory。
  ② **黄昏顺序物理化**。日昼山半透明晕与亮盘交叉淡化期无法互相遮挡（multiply 中间态无解）→ 时间轴重排为**物理顺序**：天先黑（.04-.34）→ 山换实心夜剪影（.22-.34 快换带）→ 太阳**才**启程（.30-.52，x .56→.72 落向中央峰右肩实心带，y .30→.40 晚启程防盘底越脊）→ 月亮 .55 后升。落日终点三换位：x .90（撞 KPI 列 x .88+）→ .84（鞍部 x .78-.80 薄墨穿帮）→ **.72 中央峰右肩**（ crest .345-雾脚 .49 通体实心，盘右缘最大 .78 不进 KPI 列）。
  ③ **月亮左右轮转**（用户给的方案 2）：太阳晨升午移暮落（左→右），月从最左 fg 山体出生（.10,.495）对角爬升，u .60-.68 新月越过前山脊、.68-.78 越中脊、终 (.44,.27) 悬开阔夜空——**终点从 (.54,.285) 西移**：原位正压 GPT-4o 核心标签（56%,33vh）。星随月：stars=.65×sstep(.62,.86)，月到中天星最亮，符合"月亮走到正中间星星越来越闪耀"。
  ④ **大雁自由飞 + 删航线**。用户明示不要那条线：svg.routes 六路径全删（CSS+DOM），drawRoutes() 删；雁改随机三次贝塞尔自由 journeys——src (.375,.83)±抖动（用户请求节点）、dst x .885 y∈PROV_Y 五渠道（±1.5%）、控制点弦插值+上抬 10-22% 弓形、12-20s/程、35% 概率复用上只目的地成松散雁群、到站后 0.8-3.8s 折返、首尾 8% 淡入出。中间路径完全随机，只保留"从请求出发、回归渠道"的语义。
  ⑤ **白天建筑重绘**。旧平顶门楼被否（"分辨率很差的贴图"）：ImageGen 重出水墨楼阁（1024×1458，江南层檐塔楼+岩石底座，与山同画风）；生成图右下水印先置白再转换；纸纹噪点 alpha 12-22（p50 16）→ 阈值 T=24+软下限 8 杀膜保晕；bbox 裁净。CSS 移 left 3vw/bottom 7vh/高 48vh。夜游船不动（用户点名的质感标杆）。
  验证：1440 全帧 u=0/.50（日落半盘几何切）/​.56（月隐）/​.64（月牙出前山）/​.73（月越中脊）/1，1920 昼夜定妆 final-day/night-1920.png，工作台页 final-bench-day.png；GPT-4o 标签避让后 u=1 复验 v4-u1b.png。真鼠标三项回归（themeBtn 双向/nav→bench/回夜）全过，全程 fps≈90、errs []。**教训追加**：UI 遮挡碰撞验收必须看"天体终点 × 所有 DOM 标签锚点"的叠加帧（.core-label 56%/33vh、KPI x .88+、nav 列 x .03-.11 是三大禁区）。previews/ 旧 gate*/boat* 为陈旧逻辑帧仅留档。
- **20:49 大雁动作精化（用户要求"更多动作帧、更自然流畅"）**：六相连拍精灵重制 + 扇翅节律引擎重写。
  ① **六相精灵**：ImageGen 出"六只同款水墨雁一字排开"（上扬高/上中/回拢蓄力/下压/下拍最低/滑翔），v1 姿势好但尾尖喙尖在列投影上相互侵入切不开（全图仅 2 处真空隙）→ 加"间距≥两倍身长"重生成 v2 成功。处理链三个坑：**软阈值斜坡方向写反**（把纸纹 alpha 26-34 映射到 0-255 而不是 0-8，全图变墨块——改实测纸纹 max=32 后硬切 a≤32）；水印擦除矩形（右下 H-70/W-260）；切分改**墨量最小列下刀**（边界 ±60px 窗口找 min，5 刀中 3 刀全白 2 刀 6-8px 墨）+**贴边小连通域清除**（面积<260 且横向跨度<26px 且贴左右边 22px 内=邻雁残翼，8 连通 BFS 摘除）。
  ② **体轴标准化（关键新工序）**：六帧 bbox 宽 225-309 且**下压相翅膀伸到身体下方**——等宽显示会身体呼吸 ±30%、bottom:0 锚 bbox 底会让身体随翼上下窜。方案：按体长（行墨量横向 spread≥55%峰值的行带 = 体轴，加权中心）等比缩放到 250px，再统一画布 444×415、体轴中心统一锚在 (W/2, H-BU)——CSS 一行不用改。缩放后各帧体长实测 248-251。
  ③ **节律引擎**：真实雁类=重鸟爆发式扇翅——每拍 8 步 SEQ[0,1,2,3,4,3,2,1]（SDUR 非等时长，下扑段发力）×BEAT 0.62s×wing 系数（0.4-0.6s/拍≈1.6-2.5Hz），连扇 2-5 拍→滑翔 1.0-2.6s→再扇；扑翼进度 p 同步身体起伏 bob=-sin(2πp)×1.6px 与俯仰 rot=-sin(2πp)×2.2°（下扑段抬头），指数平滑过渡防切相跳变。**坑：dt 毫秒当秒用**（首拍慢 60 倍+平滑恒 1），除 1000。显示宽 ×1.65 补偿统一画布空白边距（体轴只占画布宽 56%，原尺寸雁缩成墨点）。
  ④ **验证**：连拍脚本 v52burst.mjs 一次加载 10 帧×180ms 拼网格——不同雁相位错开、同雁翼位跨帧连续、体型零呼吸抖动；昼/夜/裁片全过，fps≈90 errs []。出图 previews/bird6-day2/night.png。体轴测量-缩放-锚定脚本 split_bird6_v2.py/normalize_bird6.py 留在工作目录备复用。

- **21:05 塔除名，船独挑昼夜（用户判"楼阁更突兀"）+ 夜雁收工（"晚上飞大雁反常理"）**：
  ① **楼阁删除**：V4→v52 楼阁 v2 重绘后用户仍判突兀，直接除名——`.arch/.arch-lights` CSS 块、DOM 两个元素、main.js el 映射/视差/apply 滤镜、timeline arch* 七个字段全清，不留兼容层。楼阁谱系就此终线：V4 arch → v52 arch v1 → v2 重绘 → **dead**。
  ② **船=唯一水上母题**：白天不开灯（`.boat` 去掉静态 filter 与 opacity:0，opacity 恒 1；亮度由 timeline 逐帧驱动 boatBright=lerp(1,.6,boatK)、boatSat=lerp(1,.72,boatK)，boatK=sstep(.28,.58,u)——天光暗船跟着暗），夜里舱窗+船尾灯笼亮（boatLights=lightsK=sstep(.60,.85)）。一个物件讲完昼夜，不再有换装。坑：v52shot 探针读 `.arch` filter 会崩，改读 `.boat`。
  ③ **夜雁收工**：雁群 opacity × dayK=1-sstep(.32,.48,u)——黄昏雁群渐散、入夜无雁、白天回来；淡出期间引擎照常飞（相位连续，回来不跳）。雁从此只属于白天。
  ④ **验证**：u0/u42/u100 三帧 probe 全绿（fps 90，errs []，boatFilter 1.0→0.82→0.6 平滑插值）；目检三态——白日墨船无灯静泊+雁群自由飞、暮色船渐暗雁渐隐、夜船舱窗双暖光+船尾灯笼亮+雁无+月(.44,.27)不压标签；final-day/night-1920 重出（旧版带塔作废）。
- **21:49 日月真抛物线（用户令"轨迹应当是抛物线、从左右边界消失"）+ 月出漏光根治**：
  ① **坐标系认知纠偏（先记坑）**：scene3d 的 fy 是**从画布顶往下**量的视口分数——toWorldY(fy)=H-fy×VH（世界 y 向上），页面 y=fy×VH、fy=.62 正好压 canvas 底缘（地平线）。按"fy=离底高度"心算全错——u.62 的月亮实际在页面 y≈480px（雾脚漏光区），不在高空。
  ② **盘径定律**：PlaneGeometry(2,2)+setScalar(s) → 屏上直径=**2×s**（不是 s）：太阳 2×.105×VH、月亮 2×.065×VH×scale，像素实测吻合（u1 月盘 140px≈预期）。
  ③ **山脊线测量**：ridge_scan.py（PIL 列扫描首个 alpha>200 行）对 mid/fg-night 按 cover-fit 映射 vh——左肩 47.6、中央峰 x.56 顶 34.3、鞍部 x.72-.80 为 40-42.5、右缘山体 40.3-43.5。日月车道全部按这张 crest 表避让。
  ④ **太阳=抛物线右落**：y=.205+.92(x-.44)²，全程压进 u .06-.34（与天黑完同一拍落山，夜空高盘物理错）；sunX .35→1.06 几何穿越右边界 + x .78-.88 沉入右缘山体；淡出带 .79-.855→**.78-.88**——旧带可视落日仅 ~47ms 读成"啪没了"，新带 ~200ms 且与几何沉没重叠。KPI 列（x .86+）物理上容不下满亮盘穿行：消失点定在右缘山体而非数学边界，汇报已向用户交底。
  ⑤ **月亮=抛物线左升**：y=.26+1.95(x-.42)²，从左边界 x -.03 贴山脚入画，顶点 (.42,.26)（不压 GPT-4o 标签/nav）。**漏光三层根因**：mid-night mask 实心带只到 58%（46.9vh）< 左肩 47.6vh → 加深到 70%（51.2vh）**仍漏**——u.62 盘跨 y 446-546 大半在实心带以下的雾脚渐变区，multiply 遮挡物理不成立（雾相就住在这）；终修=**出山同步 op 爬升** sstep(.60,.68)（雾脚期做成有意的"雾中暗月升"，u≈.66 清 crest 后满亮）+ **halo 滞后本体**（新 st.moon.halo=sstep(.64,.78)，scene3d 改用，加色晕不再先于盘体）。帧间差分定量：雾脚月亮贡献 u.60=0 / u.62=+28 / u.64=+52 / u.66=+182（满盘出脊），旧漏光峰值 250。
  ⑥ **杂项**：u030 左缘 (30,335) 琥珀小点放大鉴定=nav 活动项圆点符号（UI 非 scene bug）；**过期截图会撒谎**——timeline 改动后旧 arc-* 帧全部重拍再用；月爬升扫过 nav 列（x .05-.15 约 0.5s）沿用上轮已拍板先例，不翻案。
  ⑦ **验证**：u .22/.24（落日半沉右山、KPI 可读、无残影）/ .30（空暮色）/ .58-.66（月出四连）probe 全绿 fps 90 errs []；final-day/night-1920 重出（白日日位 .35/.21、夜月顶点+星幕+雾脚干净）。

## 22:38 V5.2 — 日月西行循环 + 6s easeInOut + 光影随动（第三版车道，用户定稿方向）
- **需求**：①日月都"左落右升"抛物线 ②切换总时长 6s、缓动 渐慢-渐快-渐慢 ③出画入画必须贴屏幕边界几何完成 ④光影随天体移动（时间）连续变化。**关键推论：夜→日不再是倒放**——月亮也要左落、太阳也要右升，昼夜两个方向的切换都守法。
- **①架构升级：u 引擎 → p 主时钟 + 每天体段序列器**。computeState(p, sunB, moonB)：环境通道全键 p（0=昼 1=夜，双向对称）；天体脱离 p 独立驱动 (phase,q)：
  - 每个天体一条**固定西行循环**：rest →(exit 段)左边界几何落出(op 恒 1)→ 隐身 →(rise 段)右边界升入→ rest。切换只**推进**循环、永不倒带 → 两个方向都左落右升。
  - planBody(b,toRest) 生成段队列；**中断续接**：切换进行中再点，先以压缩窗口(SQUEEZE [0,.14]/[.20,.60])飞完当前弧，再补跑新目标缺的段——实测中断在 p .66 反向：月亮先升完回巢、再折向西落，太阳再右升，全程无闪跳无传送。
  - 隐身侧跳（左缘↔右缘，都在画外）免段免费。
  - **坑**：planBody 漏写 rest→exit 分支，夜转日月亮原地定住（probe q 恒 0 抓到）；rise q=0（隐藏右）target hiddenL 若直接排 exit 段会**从巢位传送**（exit(0)=巢位）——必须先免段跳到 hiddenL。
- **②弧线几何**（全部真抛物线，crest 表避让）：
  - 太阳 exit：沿用已验证 x .35→-.10、y=.205+.92(x-.44)²；**新增 rise**：x 1.06→.35 同一抛物线西爬，op=1-sstep(.90,.99,x) 位置键控（窗无关，防 mask 渐变区透光），出生点在右山体后 y .559 盘顶 .454 全遮。
  - 月亮 rise：沿用 x 1.03→.40、y=.26+1.95(x-.42)²，op 改**位置键控** 1-sstep(.74,.79,x)（替代旧时间键控 .66-.72——序列器会压缩窗口，时间键控会错拍）；**新增 exit**：y=.26+.78(x-.40)² 浅俯角滑向西——k=1.95 的陡弧会在 x .05 就沉到左肩 crest(.476) 后面、到不了边界；浅弧 k=.78 全程盘底轻吻山脊线、贴边界出画，终点 x -.07 全裁。
  - halo 出画随体：sun sstep(-.09,0,x)、moon sstep(-.06,.02,x)——大光晕不再滞留画内。
- **③6s easeInOut**：DUR 3000 easeIn → **6000 easeInOut**（渐慢-渐快-渐慢）；段内 q=easeInOut(sstep(w0,w1,j))，j=旅程进度（双向 0→1），正向窗口与旧 u 窗口数值相同（.06-.34 落、.53-.84 升）保证已验证几何原样重放。实时轴：落 1.0-2.5s、升 3.1-4.3s、6.0s 净。
- **④光影随动**：
  - 暖光池 .glow 加 --gx 变量，每帧 gx=57+(sunX-.35)×90 clamp[8,92]——日落光池跟太阳西移熄灭、日出从右侧重新点亮；昼静置 gx=57 与已验证日景零差异。
  - 新增 .moonwash 冷晕层（screen 混合 rgba(196,212,234,.17)），--mx 跟月亮 x、透明度=halo×.7——月光随月走。
  - scene3d 色温：st.sun.warm=sstep(.30,.48,y)（贴地平线=1），盘色→(1,.86,.70)、halo→(1,.74,.48) 深琥珀；日出日落暖、昼静置中性。
- **⑤验证**：tg7 双向全真鼠标（Input.dispatchMouseEvent）时序截帧 15 张 + 中断 7 张全绿 fps 88-90 errs []；月亮掠 nav 列（夜墨字上白盘 ~0.5s）与太阳出脊穿 KPI 区（x .86-1.0 ~1s，字仍可读）沿用已拍板先例；final-day/night-1920 重出。u 静置态（0/1）与上轮零回归。

## 22:47 V5.2 — 大雁慢拍 + 体轴锁定（用户两令）
- **需求**："翅膀的挥动频率再慢一些，且翅膀煽动时身体不应该晃动"。
- **改法**：
  - **BEAT 0.62 → 1.0**（wing .78 时实际拍频 ~0.69s/拍 ≈1.45 拍/秒，原 ~2.6 拍/秒）——只动这一个常数，SEQ/SDUR/爆发节律（扇 2-5 拍→滑翔 1-2.6s→再扇）原样，节奏感保持只是放慢。
  - **体轴锁定**：删掉 bob（±1.6px 正弦浮动）与 pitch（±2.2° 摆动）整套机制，wingFrame 退化为纯姿态选择器；transform 只剩 translate3d 到路径点——身体位移 100% 由飞行路径驱动，扇翅期间零晃动，"只有翅膀在动"。
  - **坑 1（误删即崩）**：改 transform 那刀把 dayK 四行（雁只白天飞 1-sstep(.32,.48,p)）连注释一起删了但引用还在——每帧 ReferenceError；下一刀立刻补回，新开页面 probe errs [] 确认活体修复。
  - **坑 2（量尺失真）**：birdcheck 正则 `/,\s*(-?[\d.]+)px,0\)/` 永远匹配不上实际 transform 格式 "px, 0px)"（逗号带空格+尾随 0px）→ 报 "y delta 0.0px" 假冻结；用原始 transform 直出（/tmp/bdbg.mjs）实证 y 平滑滑行 773.5→772.4→772.3→773.0、x 单调、无 rotate——页面没错，是量尺错了。**教训：验 transform 先打印原文再写正则。**
- **验证**：birdcheck 15 采样 rotate 0、3s 翼姿切换 12 次（200ms 采样混叠=下界）、无页面错误；bird-slow-000.png 日景 7 雁松散雁群正常渲染 fps 90。

## 23:00 V5.2 — 高清真月贴图上线（用户贴图，程序化月盘退役为占位）
- **来源回收**：用户往聊天里贴 1254×1254 真月照片——**粘贴图不落磁盘**， TEMP/AppData/.qoder-cn 全搜不到；从会话转录 jsonl（`~/.qoder-cn/projects/<proj>/<session>.jsonl`）tail 里抠出 OSS 图片 URL，`curl -x http://127.0.0.1:7890` 拉回原图。此法通用，记入用户记忆。
- **黑底转真 alpha（moon_prep.py）**：亮度阈值 14 粗扫 bbox（步 4px）→ 过圆心行列精扫得 center(622.5,616)/R≈589（椭圆度 0.76% 取均值）；圆盘掩模 + 2px 羽化，pad 3px 裁 1184² 方，LANCZOS 缩 512 → `assets/scene/v52-moon-hd.png`（~230KB）。四角 alpha=0、月海暗斑保持不透明（**不可用亮度当 alpha**——暗月海会变鬼影，必须圆盘掩模）。
- **scene3d 接线**：程序化 moonTex() 保留为瞬时占位，`TextureLoader.load('assets/scene/v52-moon-hd.png')` onLoad 换 map（路径相对页面 URL）+ `colorSpace=SRGBColorSpace`（three r169 必设，否则照片发灰）。MeshBasicMaterial 白色直乘，不调色；halo/moonwash/位置键控全不动——只换皮不换骨。
- **验证**：u=1 夜景静置（月巢 .40/.268）环形山/月海/临边昏暗全出，halo 融合无方块边；u=.62 月亮正确藏右缘外（位置键控 x>.79 不可见，符合设计）；u=.70 出山帧山脊遮挡正确（canvas 在 DOM 山层下）、GPT-4o 标签可读；fps 90 errs [] 全绿。昼帧无回归（昼 moon op=0）。
- **太阳未动**：仍是程序化暖盘——用户哪天贴同风格真太阳照片，同管线（暖色底转 alpha 要另配阈值）即可换装。

## 23:08 V5.2 — 太阳风格化换装 + 日月边缘光弥散（用户两令）
- **需求**："太阳照片太写实、色调不统一，要修改；天空中日月边界不会这么清晰，应带光的弥散"。
- **太阳风格化（bodies_prep.py make_body remap=True）**：照片盘（R≈597）→ GaussianBlur 7px（米粒组织压成柔质感）→ 亮度归一化 Lnorm → **base_col(t) 重映射到程序化太阳同款香槟金三停**（#fff8e6/.62/#ffdd96/1/#ffc069——色调统一的关键）× (0.86+0.28×Lnorm)（±14% 微纹理，太阳黑子只剩软斑）→ **边缘 10% alpha 羽化**（发光体弥散感，配 sunHalo 5.5× additive 正好融进光晕）→ 512²。scene3d 双 TextureLoader 换 map（程序化盘留占位），SRGB 必设；warm 色温逐帧 color 乘法对照片贴图照常生效。
- **月亮边缘弥散**：重切 v52-moon-hd.png，改 4.5% 半径 limb 羽化（rim 融进 moonHalo），照片色与细节原样保留。
- **坑（自造回归）**：首版 make_body 把香槟金 remap 无差别套给了月亮——detail 关了但 base_col 仍覆盖全盘色，夜景月亮变金球、环形山全灭（bodies2-night 截图当场抓获）。修法：加 remap=False 分支（rgb=原像素），月亮永远不吃太阳调色板。**教训：两资产共用的处理函数，"保留原色"也是一条显式路径，隐式默认会咬人。**
- **验证**：bodies2-day（香槟金发光体、弥散边、与暖光池同调）、bodies2-dusk（太阳几何切左缘、暖色温生效）、bodies3-night（照片月+软边+halo 融合）全绿 fps 90 errs []。u=1 月面 Lrange 打印 [0,0] 是 remap=False 跳过扫描的正常输出非异常。

## 00:21 V5.2 — 进场挪到第 5~7 秒（用户令，总长 6s→7s）
- **改法**：DUR 6000→**7000**；WIN_RISE [.53,.84]→**[.837,1.0]**（.837=easeInOut(5/7)，主钟 7s easeInOut 下 j=.837 恰在实时 5.0s）——升弧实时占用 **5.0s→7.0s**，落位即动画收尾（原 5-6s 净场段取消）；canonicalBodies 月亮窗口同步 (.837,1)/u≥1→rest。
- **新实时时刻表（7s）**：出画 1.2~2.9s（原 1.0~2.5 随总长微拉）；双星隐身 2.9~5.0s（拉长到 2.1s，天色先行）；**进场 5.0~7.0s**；窗值只有 WIN_RISE 一处，SQUEEZE 中断续接窗不动。
- **坑（测试拍点漂移）**：toggle6 中断相位 t1+3500 在 6s 时 p≈.66（真反向），7s 下恰逢 p=.5 → toggleTheme 的 p<.5 判据把它退化成**同向重触发**（master 时钟重播、段队列重排——顺带实证了同向重触发也无跳变）。下次真反向验收拍点要用 t1+4200 以上。
- **验证**：真鼠标动画探针——t+5.0s 升方仍 {exit/rise, q≈0}（画外未动）、t+6.3s 升弧 q≈.996 收尾、出画方 1.9s q .089 / 2.6s q .973；中断链连续无传送；fps 110~132 errs []。

## 09-10 00:36 夜景雾重做：5 种 AI 水墨元素替换矩形雾带
- 用户判定：夜雾仍是长方形块、且各物体上缘齐平。指令两步：①设计提示词出 5 种黑夜元素 ②编进夜景。
- 出图（ImageGen 1536×1024，水墨+纯黑底+月下冷白+禁山水月星）：夜云/斜岚带/山脚雾团/水面轻纱/雾缕流霭，形状谱系拉满。
- mist_prep.py 管线：水印「Qoder AI 生成」白字局部修复（右下 82%+ 区，MedianFilter9×2 为背景、亮于背景>10 即替换+膨胀2，mounds 雾压字不能涂黑块）；黑场扣除（顶角中值做 bg，sash/tendrils 底灰 10/15 被扣净）；alpha=亮度（雾正确、实体错误的老规矩）；冷月白 BASE(206,220,240)×(0.35+0.65·Ln)；底部 5% alpha 收边；bbox 裁切。
- 编排：6 层（mounds 用两次，预翻转出 -flip.png——CSS scaleX(-1) 会和 JS transform 打架）。位置高低错落破齐平线：云 bottom:56vh、岚带 34vh、雾团 15/14vh、轻纱 21vh、雾缕 18vh；宽度自适应。z 序在 mid 之后 fg 之前。
- 驱动：透明度=st.fogNight×层系数(.55-.9)；漂移=JS 正弦(t/T, 46-90s, amp10-18px)+视差折进同一个 transform（一个属性一个写入者，CSS 动画会和 shift() 冲突的老坑）。旧 v4-fog-night 整条退役（img+CSS+el 表），昼雾 v4-fog-day 未动。
- 验证：u=1 夜/0.9 过渡/0 昼三帧截图——夜景矩形带消失、白天雾层全隐；间隔 2.5s 双帧 diff 雾区 max25 有位移=漂移在动；probe errs:[]，fps 90。
- 坑：Git Bash /tmp 与 Windows Python 不互通，v52shot 输出走会话目录相对路径。

## 09-10 11:26 V5.2 — 12 小时弧线时间系统重构 + 进场再慢一半（用户令 3b/4）
- **需求**：页面按 12 小时均分 12 个位置，但必须是抛物线圆弧的**等弧长**均分（非直线等分）；太阳 6:00 右缘露角→12:00 居中→18:00 左缘全没，月亮 18:00→6:00 同弧接力；点切换按钮切到对应天体。进场速度"再慢一半"。
- **核心（timeline.js 重写）**：单一抛物线 y=.14+.712(x-.5)²，xR=1.03（右缘、山脊自然遮挡露角）→xL=-.065（左缘全没）；512 采样累计弧长 LUT+二分 arcPos(s)，s=0 右端 s=1 左端（西行）。timeS(nowMs,kind) 读**本机真实时钟**：sun 偏移 6h、moon 18h；rest 位=实时钟点→静置时天体随钟缓移（约一小时一个盘位）。进场减速：WIN_RISE [.837,1]→**[.3673,1]**（.3673=easeInOut(3/7)→实时 3.0~7.0s，行程 4s=原速一半）。
- **"卡"根因确诊**：真鼠标切换 fps 全程 90 平线→不是掉帧，是**双重缓动**（主钟 easeInOut 之上 evalBody/canonicalBodies 窗内又套一层 easeInOut，速度曲线读起来像顿挫）。修法：删内层，全链路只剩主钟一层缓动；新弧从右缘全可见进场（op=sstep(0,.05,q) 防弹跳），废除旧 rise 的 1-sstep(.74,.79,x) 位置门（前 40% 路径不可见的旧病）。
- **接线（main.js）**：sceneNow()=Date.now()+HOUR_OFFSET；`?hour=H` 开发参数可冻结任意钟点（bootNight/帧循环/setU 全走 sceneNow）；V5.pos 暴露日月实时 x/y/op 供验收。computeState 增 nowMs 参数；sun.warm 带 (.30,.48)→(.26,.36) 适配新弧高度。
- **验收（arccheck.mjs）**：静态 9 时点（6/9/12/17.9/18/21/0/3/4.9）对 timeline.js 真值全部 dx,dy≤.002，17.9 白天太阳 op<.05 全没；进场扫点 t+3.4~6.9s 太阳 x .992→.477 半弧走完、月亮 2.9s 出净、fps 90。**真鼠标验收（togaccept.mjs，Input.dispatchMouseEvent 点 #themeBtn）**：夜→日月亮 2.9s 出净、太阳 3.3s(x1.012,op.85)→6.9s(x.476) 走完进场；日→夜反向对称；双向 fps 90 无页面错误。截图 arc-6/9/12/18/0 + arc-toggle-day/back-night 八张全人工过目。
- **坑**：①arccheck 用 V5.toggle() 只是运动学验收，交互验收必须 CDP 真鼠标（铁律重申）；②?hour 只改场景时钟，问候语仍读真实时钟——开发参数下问候与场景可能不同步，产品无此问题；③测试 5.9 点位 FAIL 是自造错（5:54 落 boot 昼间边界 5≤h<18，月亮藏右缘是正确行为），换 4.9 过。

## 09-10 13:40 V5.3 — 山雾方案更替：用户贴图雾否决 → GPU 噪声雾场落地 + 昼雾三时
- **否决**：用户 5 片水墨雾贴图（双色调 11 资产）上线即被否——"像人吸了一口烟吐了一口气"。根因：素材是"烟雾形状"（丝缕/卷曲轮廓+各自摆动=物体），水墨雾应是**留白/空间场**。贴图路线治标不治本，v53-mist-* 11 张留盘退役。
- **三选一拍板**：GPU 程序化雾场（three.js shader）/ Canvas2D CPU 版 / 贴图改良；用户选 1（"那就1试一试"）。
- **落地（新 src/fog.js + #fog canvas）**：FBM 值噪声，双域反向滚动（大团慢 + 细碎快）=整片涌动；海拔衰减 prof=(1-smoothstep(.16,.80,v))^2 谷底浓山尖清；阈值 patchiness 峰线穿插；底缘溶解带入岸线；55% 分辨率上拉伸=免费羽化；screen 混合；DOM 在 mid 之后 fg 之前吃山脚。uDensity = fogNight*0.95（月白）+ (1-fogNight)*dayK*0.85（暖白）；dayK 真实时钟：晨 4.8-6.5h 升 .95、基础 .16（正午最少）、暮 15.8-17.2h 回 .6、18.2-19.6h 收。
- **连带退役**：v4 fog-day 贴图雾（硬边横带、跟 toggle 不跟时钟，与新雾场职责重叠）——img/CSS/el 表/opacity/shift 全拆；main.js 旧 MIST 数组与逐层驱动整块删除。
- **验证**：hour=0/6.5/12/17.5 四时截图——夜雾贴岸线涌动无烟丝、晨浓/午淡/暮中梯度成立、山尖清爽；fps 87-90、errs:[]。截图 fog2-night-h0 / fog2-day-h65 / h12 / h175（会话目录）。
- **坑**：① main.js 已有 sstep（:118），重复声明整模块崩——动手前先 grep 现有名；② CDP 同 URL 可能重放内存缓存里旧坏模块的报错（probe 却健康），换 cache-bust 参数即消。
- 规划全文（含日月系列提示词原文 / 朝霞晚霞日程 / 泊位流程）→ docs/V5.3-PLAN.md。

## 09-10 14:35 V5.3 — 夜雾四点修 + 视差出血修（用户逐条批后一并落地）
- **视差穿模（用户报告：鼠标靠左，山被推离边界留缝）**：根因=推移图层 `left:0;width:100%` 零余量，右移即露边缝。修=mid-day/mid-night/floor-day/floor-night 统一 `left:-24px;width:calc(100%+48px)`、floor 加 `bottom:-16px`（纵向 6.6px 极值）；object-fit:cover 吸收放大，viewpor 裁掉出血。**parcheck.mjs 探针**（真鼠标 Input.dispatchMouseEvent 贴左缘+等 3.5s 收敛）：光标贴左时 mid +5px/floor +11px 右移，图左缘仍在 -19/-13px 屏幕外——几何上缝不可能露出；昼夜两态 fps 90 errs:[]。
- **夜雾四点**（用户四问截图反馈，"可以"批）：① 水线黑带：mid-night 底羽化 .55@88% 平台改渐进（.45@80%→.18@90%→0），实心遮挡区保持 70% 不动；② 夜雾减淡：uDensity 夜档 .95→.5 + 阈值 0.42→mix(.42,.55,uNight)（夜更成团不成毯）；③ 前后两景：**远雾 bank 进 scene3d**（z=-80 山后、fy .225-.645 山区带、山脊间透出=山从雾里长出来，密度夜 .34/昼 dayK*.5，随相机视差自然位移、日月盘 z=0 在其前保持脆）+ 近雾 #fog 保留水线职责但减淡；④ 月光映雾：fog shader 朝月方向偏移采样大团噪声，密度沿月光方向衰减处雾缘泛冷月白 rim（uMoon/uMoonOp 新 uniform，moon uv 由 canvas rect 反算），初版太弱升到 ×4.0/步长 .075/col+.65。
- **dayKof() 提升为共享**：main.js 帧 loop 内联 dayK 改模块级 dayKof()，sky3d.apply(st, dayKof()) 三处调用点同步——远雾近雾同一晨浓/午淡/暮中曲线。
- **验证**：hour=0/6.5/12/17.5 四时扫图+夜雾放大 crop——黑带消失、夜雾收敛成山脚含蓄薄雾、月下雾团迎月面微光轮廓、晨雾绕山腰/午淡/暮中梯度保持；fps 90、errs:[] 全程。截图 fog3-night-h0(b)/fog3-day-h65/h12/h175 + pck-night/day + 放大 crop（会话目录）。
- **坑**：headless Chrome（9229）随会话死，重验前先 curl /json/version 检查、拉起用 --headless=new + 独立 user-data-dir。

## 09-10 20:40 V5.3 — R62 五项综合反馈落定（素材残留/雁左边界/无缓存服务器/5.1 规范/对话记录制度）
- **①旧形状素材残留**：页面本就零引用（index.html/main.js 无 mist/arch 痕迹，CDP 全新截图干净）——用户看到的"形状还在"判定为浏览器缓存旧版。双管齐下：(a) `assets/scene/` 里 21 件退役贴图（v4-fog-*、v52-arch*、v52-mist-*、v53-mist-*）隔离到 `assets/retired-fog-arch/`，未删可恢复；(b) **预览服务器换 serve.py**（SimpleHTTPRequestHandler 子类加 `Cache-Control: no-cache, no-store, must-revalidate`），"打开预览.bat"同步改启动行，curl -I 验证 HTML/JS 都带无缓存头——根治"改了没生效"复发。
- **②大雁左边界飞入**：jsonl 溯源到 09-09 19:26 原话"大雁反正从左至右可以自由地飞翔"（旧代码从画面中 (.375,.83) 出生=没守住）。newJourney 改 `src={x:R(-.09,-.04), y:R(.28,.58)}`（边界外进场、任意天际高度），dur 12-20→20-34s 保同速；初始相位去同步保留（首屏即有雁在飞）。**goosecheck.mjs 新探针**：1s 采样×50s，断言每次 op 0→正的瞬间 x<0 且后续递增——7 雁 14 次新旅程全 PASS、errs:[]、昼景截图人工过目。
- **③版本目录归拢**：方案 A（v5/ 挪 prototypes/v5/，根目录只留现役）已呈报，待拍板后动手（归档只复制不剪切）。
- **④5.1 规范更新**：V5.1-方案.md 头部补「〇、结局状态」块（Gate1-4 过→视觉否决→V5.2/5.3 接棒）；docs/README.md 补 V 系文档索引（实际文件名核对过）。
- **⑤对话记录制度（用户长期新规）**：`apps/api-gateway/对话记录/` 建 `更新记录.md`（四字段台账：要求/状态/日期/备注，R 续号，已从会话记录回填 62 行，时间 UTC+8）+ `方案记录.md`（P 续号方案谱系+待拍板清单）；制度入项目长期记忆 feedback-update-log.md，docs/README.md 挂索引。
- **坑**：① CDP evaluate 里对 transform 写正则——模板字符串送浏览器须双反斜杠（`\(`、`\s`），且浏览器序列化 transform 逗号后带空格（"px, 0px)" 老坑变体）；② Git Bash 下 taskkill 要 `MSYS_NO_PATHCONV=1 taskkill /PID …`（`//PID` 被 UNC 检查拦）；③ 本机探针脚本先小单发 evaluate 验证假设再跑长循环，防正则类低错烧 50s。

## 09-10 21:15 V5.3 — R63 太阳 5 态系列接入（用户回传图即交付）

- **原图取得**：用户只发图无文字——按 R48 交付的太阳 5 态提示词对号（晨曦金橙→暖金→香槟近白→饱和金→琥珀橙红）。粘贴图不落盘，从会话 jsonl 尾部回溯 `{"type":"image","source":{"type":"url"}}` 拿 OSS 地址，`curl -x 127.0.0.1:7890` 拉 2000×667 原图。
- **拆分预处理 sun5_prep.py**：黑底系列图五连，逐日轮定位（3 轮质心收敛 + 72 向边缘游走，阈值 plateau*0.55/0.42）；日轮半径归一到旧贴图占比（v52-sun-hd plateau 163px/512 半砖 = 0.637，缩放系数 1.04-1.17）；alpha = max(日轮实心→羽化, 光晕=亮度×1.12 封顶 238) × 砖边淡出坡（0.86→0.995 半砖），防方角。**坑：PIL EXTENT 裁剪后的坐标必须减去裁剪原点**（用全图坐标采样裁后图=采到外面出黑砖，sun1 恰好原点≈0 逃过）。
- **驱动链**：timeline.js `bodyPos` 三相位（rest/exit/rise）都挂 `h: base + sA*12`（sT 为真实时钟弧参，exit/rise 用 sA 插值，h 全程连续）；computeState 透传 `st.sun.h`。scene3d.js 太阳改 5 层 PlaneGeometry 组，按弧线小时交叉渐隐：`tH=clamp((h-6)/3,0,4)`，iH/fH 拆整数小数，相邻两层权 重叠；贴图自带颜色→**删盘面暖色叠加**（halo 保留 warm 逻辑）；缩放 .105→.085（用户 R48）；v52-sun-hd 引用移除（月亮保留）。SRGBColorSpace 必挂，r169 不挂会发灰。
- **验证**：sunshot.mjs CDP `?hour=` 钉时钟截五整点 6.2/9/12/15/17.6——晨右缘金橙、9h 暖金、12h 香槟近白、15h 饱和金、17.6h 左缘暖晕，全零页面报错。h17.6 落在 4→5 态渐变段（tH=3.87）颜色正确。
- **遗留**：月亮 8 相图未到；v52-sun-hd.png 已无引用（退役候选，未删）。

## 09-10 22:50 V5.3 — R64 月亮 8 相系列接入（系列图协议闭环）

- **原图与拆分 moon8_prep.py**：会话 jsonl 抠 OSS 原图（2000×667 黑底八连，新月→残月，蛾眉/残月暗面低于阈值）。盘心逐盘定位：满 lit 盘用 bbox 中心；蛾眉(2)/残月(8) 用亮肢（右/左边缘）+共享半径反推盘心；半径全列统一 113.25px（各盘 y 向直径 226-228 高度一致，满月光晕膨胀不可信）。归一：旧 v52-moon-hd 盘占比 0.977（半径 250/256）→ 目标半径 250、4.5% 边缘羽化、盘外 alpha=0。**关键取舍：系列图光晕不带入**（满月光晕半径 1.55×盘，250 目标半径下 tile 放不下），光晕继续由 moonHalo 精灵负责——与旧月行为一致，也避免方形裁边。
- **驱动链**：timeline.js 增 `moonPhaseT(nowMs)`（朔望月 29.530588853d，参考新月 2000-01-06 18:14 UTC，返回 0..8；0=新月 2=上弦 4=满月 6=下弦），bodyPos 三相位挂 `ph`（仅 moon）；computeState 增第 5 参 phOv（?phase= 开发钉相）；main.js PHASE_OV 三调用点透传。scene3d.js 单月盘改 **8 层 PlaneGeometry 组**，`tP=((ph%8)+8)%8` 相邻交叉渐隐，**nP=(iP+1)%8 跨新月环绕**（残月→新月）；贴图原色不动（月亮永不染色的铁律），SRGB 必挂。
- **验证**：8 相钉相截图全对（蛾眉右亮/上弦右半/盈凸左暗/满月/亏凸右暗/下弦左半/残月左亮）；分数相位 7.65 渐变正确；**真实日期档全帧过目——今夜（09-10）实际月龄 28.25d→残月细条，渲染一致**；V5.moonDbg 探针常驻（tP/iP/fP/八层透明度）；fps 78-80、零页面报错。
- **坑（白查 30 分钟的教训）**：moonshot.mjs 把 `--phase` 默认值 '4' **无条件**拼进 URL——不带 --phase 的"真实档"运行实际钉死在满月，现象是"钉相全对、真实档满月"，疑点被引向缓存/贴图加载/公式（全查了个遍），最后 CDP 全帧+moonDbg 探针对照才发现 URL 里躺着 `&phase=4`。**教训：探针脚本的默认参数不要静默注入被测行为；"钉相全对只有真实档不对"时先 diff 两条运行的 URL。**
- 至此日月分时系列（P11）闭环：太阳 5 态按弧线小时、月亮 8 相按真实日期。v52-sun-hd/v52-moon-hd 均无引用（退役候选，未删）。

## R65 水线灰带直边终修（09-10 23:40）
用户红圈夜景水线上方整幅灰带+直边，"说了好多好多次"。逐层隐藏探针锁死真凶：floor 层，不是雾。
- **根因**：`.floor-day,.floor-night` 只写 `top:62%; bottom:-16px` 未写 `height`。绝对定位的替换元素（img）height:auto 时按固有宽高比取高（1968 宽 → 1054px），`bottom` 被静默忽略——盒子是设计意图（426px）的 2.5 倍。后果：资产顶部 57 行暗带露出（暗条）、资产内部 row57 内容硬边落在 fy.679（直边）、14% 羽化按 1054px 拉宽至 147px 罩不住该边。
- **修法**：补 `height:calc(100% - var(--horizon) + 16px)`，一行修复。
- **为何反复复发**：R47/R59 修的是雾与 mid 层，从未核对 floor 的实际盒几何。
- **教训**：①CSS 写了 top+bottom 不等于盒子就是那段高度——替换元素（img/video）必须显式给 height；②"修不好"的顽疾要先怀疑几何/状态实测（getBoundingClientRect + 单层渲染复现），而不是继续调不相关的层；③逐层隐藏探针（阶差 14.2→1.6）是定位图层冲突最快的手段。
- 验证：夜/昼硬刷新阶差 14.2→1.8/2.0（噪声级），接缝放大图无直线无暗条，fps 78-79 零报错。

## R66 观感三连修：月阴影盘 / 岸线直边 / 太阳硬币边（09-11 00:35）
用户三连反馈：月牙背后有阴影圆月、山河过渡是一条笔直线、太阳"中间黄周围深褐像两个太阳"。批准后按 1→2→3 执行。

- **① 月暗面转透明（moon8_prep.py）**：alpha = 盘掩模 × 边坡 × **模糊亮度门控** `sstep(60,110, GaussianBlur(6)(L))`。第一版逐像素门控 sstep(16,55) 失败：源图暗肢有一圈描边笔触，逐像素看它亮度够高→剩一条细弧挂在残月暗侧（钉相 8 的新月帧上最清楚：整轮只剩弧+圆光晕）。模糊门控让细笔触溶进暗邻域（模糊峰值 ~43 < 60）归零，亮肢模糊亮度 150+ 全保留，明暗界线天然柔和。诊断链：贴图暗侧 alpha 实测 0 → 弧必在场景侧 → 钉相 8（新月、单层权重 1）仍见弧+圆晕 → 弧=新月贴图描边残弧、圆晕=moonHalo 不看月相。配套：timeline.js 月 `halo × (0.15+0.85·lit)`（lit=(1-cos(π·ph/4))/2），新月光晕/月洗几乎归零，"阴影圆月"第二个来源切除。验收：新月贴图盘区 alpha 均值 0.0、px>8 占 0%；残月/蛾眉 18% 盘区有 alpha（恰为亮肢）；满月 100% 无损；真实日期夜帧与钉相 2/4 干净无鬼影。
- **② 岸线去直边（shore_mask.py + index.html）**：值噪声 1D（4.5/13/40 格三频叠加）生成 v53-shore-mask.png（1968×426，边线 ±16px≈±1.4vh，10% 高 smoothstep 羽化）；floor 层 `mask-image` + `mask-size:100% 100%` + `no-repeat` 接入（替换 R65 的线性渐变羽化）。昼/夜接缝放大均为波浪雾边，水线行阶差 ≤4.0。
- **③ 太阳去硬币边（sun5_prep.py 径向配方重写）**：先测原始径向亮度剖面，发现照片结构=平台(rr<0.75)+亮环(0.75-0.85)+悬崖(0.85-1.05)+长尾；旧 alpha 把悬崖段以 ~200 不透明度画在天上，半透明深褐压暗天空=用户看到的"第二个太阳"。新配方：裁切重标定 `scale=DISC_R/(0.93·R)` 把悬崖推出可见日轮；按设计径向亮度曲线（TK 结点，永不暗于 0.7×核心）**乘法**校正各壳亮度（纹理保留、暗环消除）；rr>1.05 辉光径向对称化（顺带消灭裁切越界邻日光晕造成的不对称弧）；alpha=255×(1-sstep(0.80R,1.30R,dt))×边坡，跨悬崖渐隐。验收：6.5/12/16.5 三时点日轮暖而无边环，径向剖面单调无台阶。
- **教训**：①"两个太阳"类观感问题先测径向剖面再动手——暗环是照片悬崖×不透明 alpha 的乘积，调羽化宽度治标不治本；②门控类 alpha 要想到源图笔触（描边/噪点），逐像素阈值会被细笔触骗，模糊后门控才稳；③光晕/洗光等"氛围层"也要跟物理量（月相照亮率）走，否则新月顶圆晕。
- 验证汇总：钉相 2/4、真实日期夜帧、昼 6.5/12/16.5 共 6 帧过目；水线行阶差 4.0/4.0/2.1；fps 90；零页面报错。R66 已入账。

## 2026-09-11 ~12:00 — R67 白天山倒影（1/3 高度镜像层）

用户令："白天水面应该是山的倒影，倒影的高度应该是原来山高度的1/3"。方案呈报后回"可以"落地。

- **结构**：index.html 新增 `.reflect-mid/.reflect-fg` 两个 img（复用 v4-mid-day / v4-mid-day-flip，与 mid-day/fg-day 同源同 box 同 object-fit:cover——镜像内容与山层逐点对应），multiply + blur(1.5px) contrast(.98) brightness(1.08) saturate(.9)，DOM 位置在 floor 之后 boat 之前（船浮于倒影之上）。**镜像数学**：`scaleY(-.3333)`，transform-origin 放在水线上——mid 底边即水线用 `50% 100%`；fg 底边高出水线 5vh，origin 用 `50% calc(100% + 5vh)` 补偿（fg 内容只出现在水下 1.67-11.67vh，贴线 1.67vh 内无 fg 倒影，几何自动正确）。深度=物高÷3 对每一点精确成立。
- **mask 坐标系坑**：CSS mask 随元素一起被 transform——mask 写在元素局部坐标，翻转后"局部底→屏上水线、局部顶→水下最深处"。0deg 渐变首色标在元素底边。首版 `#000 0 → .5 50% → transparent 88%`（贴线最强）在水线压出 **19.8 单行落差的直线**——正中用户 R66"笔直一线"雷区；且雾层（#fog 在 mid 之上、reflect 之下）只提亮岸上山脚不提亮倒影，贴线倒影比岸上雾脚暗 25 级，线感加倍。修法=水墨逻辑反转：**岸线雾留白处倒影最淡，峰值在水下 3vh**——`rgba(0,0,0,.3) 0 → .55 30% → transparent 100%`，单行落差降至 **5.55**（R66 岸线验收同级），倒影从雾岸下"长"出来。
- **驱动**：main.js `el.reflMid.style.opacity = st.midDay*.45`、`reflFg = st.fgDay*.45`（fg 层本底 0.9 → 反射 0.405，反射强度∝源层实际透明度）；视差 `shift(node, f, suffix='')` 增 suffix 参数，reflMid 传 `' scaleY(-.3333)'` 与 translate 同一次写入（一个属性一个写入者；漏 suffix 会被 mousemove 写掉翻转）。夜/切换熄灭与昼层同源自动成立，无需新状态。
- **验收**：正午可见倒影带 672→~772 ≈ 9.1vh ≈ 可视山高（~27-30vh）1/3（几何带 12vh=36vh 山层 ÷3 精确）；带底渐隐无直边；夜间 computed opacity **0/0**（reflcheck.mjs 直读）＋与改前夜帧水带 diff mean 0.07/p99 2（雾漂移噪声级=零倒影）；黄昏 16.5 帧正常；全程 fps 89-90 零报错。
- **测量坑**：行均值统计必须排除船体列（boat x≈96-654，否则船顶边 24.7 阶差污染硬边判定）与右侧 KPI 玻璃面板（拉低 deep 参考造成假"可见行"）。
- 新探针：reflcheck.mjs（CDP 读 computed opacity，断言昼/夜两端点）。截图 v-refl12c/165c/nightb(+zoom)。R67 已入账。

## 2026-09-11 ~13:05 — R68 三项观感修（太阳正午档 / 山提实 / 倒影限暗）+ 可回退机制

用户反馈三条：①倒影波浪暗线中间实周围虚、实部超过山体最深墨色，突兀；②正午太阳太黄且可见"耀斑"纹理，与苍白主题不合；③山虚实：浅山透出后山轮廓不对劲，顶层山太虚。

**诊断**（详见更新记录 R68 行）：
- ① 倒影最暗 150-160 贴着中景山墨色；双反射层 1.67-11.67vh 重叠区 multiply 叠乘；倒影在 #fog 之下吃不到雾提亮。
- ② v53-sun-3 贴图天生饱和金黄+满面斑（耀斑感=贴图纹理），只有正午档在用（12-15h 渐入 tile4）。
- ③ 关键发现：白天山贴图 alpha 全 255，multiply 混合下遮挡全靠墨色——浅色调画的前山≈透明，后山轮廓透出。**穿透是画进贴图的色调，不是 alpha**，全局调色去不掉（根治=3b 重出贴图，另议）。

**执行**（顺序 2→3a→1；用户批"可以，注意要建立可回退机制"）：

可回退机制（先行）：`assets/backup-r68/` 存改前快照（v53-sun-3.png、v4-mid-day.png、v4-mid-day-flip.png、index.html.bak、main.js.bak）+ `还原R68备份.bat` 双击即还原全部。

- **②太阳正午档中性化**（sun_noon_fix.py 就地重写 v53-sun-3.png）：向亮度去饱和 60% + 向白压缩 `255-(255-x)*0.82` + 盘面纹理向径向对称场收 70%（72 bins，[1,2,3,2,1]/6 平滑，仅 rr<1.05 生效，盘外云气纹理保留）。盘面 254,251,190→249,248,229；斑驳 std 36.2→10.5；亮环饱和 80→26。晨昏四档不动。
- **3a 山体提实**（两张 v4-mid-day*.png 就地 remap）：乘性 `g*(1-0.14*w)`，`w=sstep(115,165,L)*(1-sstep(235,243,L))`——150-235 墨色压深 ~14%，天空 ≥243 一分不动（边缘行 242.9/243.9 落保护区，无接缝）。
- **①倒影限暗**：index.html `.reflect-*` filter brightness 1.08→**1.25**、mask 峰值 .55→**.5**（岸线触点 .3 不变）；main.js reflFg 系数 .45→**.35**（reflMid .45 不变）。倒影与山共用资产，3a 之后倒影同步变深，故 1 放最后调。

**验收**（x 700-1900，排除船与 KPI 玻璃面板）：
- 倒影最暗 p1=154 vs 山体最暗 p1=74 → 倒影比山最深墨色亮 80 级（目标 ≥10，大幅达标）；行均值 p50 口径 8.9。
- 可见倒影带 9.5vh ≈ 山高 1/3（R67 几何保持）；水线最大单行台阶 4.28、带内 5.32（≤6，软）。
- 夜间 reflcheck 计算透明度 0/0，夜帧无倒影；fps 90，errs []。
- 目视：正午太阳淡奶油圆盘无耀斑；山体墨色明显更实；倒影带柔和无突兀暗线。

**教训**：行均值统计必须排除船（x<654，船顶 24.7 台阶是假硬边）和右侧 KPI 玻璃面板（假性拉低深色基准）。

**遗留**：3b 根治"前山透后山"需按提示词重出白天山贴图（提示词先交付），待用户拍板；区域修复零成本但有手工痕迹风险。

## 2026-09-11 ~15:50 / ~19:30 — R69 问候时钟解耦 + 去核心标签 + 太阳"第二个太阳"光晕重调

用户三条：①问候语显示"早上好"但实际是下午，要求跟真实时间走并加具体时间显示；②太阳边缘"第二个太阳"怪象（截图圈认）；③去掉"GPT-4o/核心"标签。回"可以"后 ①③ 先行落地，② 按诊断→方案→拍板流程后补落地。

- **①根因**：timeline.js `computeState` 里 `tzOffsetMs=-3.5h×sstep(.2,.8,p)`（"夜间场景 UI 跟随"旧设计）喂进 ui.js 问候/时钟——下午开着夜间模式=读"真钟−3.5h"→早上好。**修**：三文件解耦——timeline 删该字段；main.js 删 import 与 3 处调用点；ui.js `nowShifted()`→`nowReal()=new Date()`，问候/日期/点按时钟全部真实时钟；dateline 追加 `· HH:MM` 每秒刷新（zh/en 各按 locale）。问候时段表不变（<5 夜深/<12 早/<13 午/<18 下午/<23 晚/其余夜深）。验证 hour=14.5/22 两帧问候与时钟均读真实时间。
- **③**：删 `.core-label` 全链（index.html HTML 块+CSS 5 行、ui.js coreSub 引用、i18n zh/en `core` 键），无残留无报错。
- **②诊断**（CDP hour=14.5 分层探针）：DOM `.glow` 隐藏前后径向剖线全等=零贡献；真凶 scene3d.js `sunHalo` 精灵——`glowTex` 单级渐变（α .5→.16@40%→线性 0）+ AdditiveBlending：内圈 α>.07 区在全通道上削顶成纯白（238 亮度天空 + 贡献>17 即 255），读作金盘外"蛋白圈"与大白场；α→0 的长尾（屏 r≈320-450px）压在稍暗天空上成圆弧边="第二个太阳的边缘"；面板直径达日盘 ~7 倍加剧。
- **②修**（拍板后）：`glowTex` 泛化为 stops 数组签名；日晕专档四档渐变 `[0,.30][.31,.055][.62,.032][1,0]`——关键数学：面板 `lerp(8.5,4)`→`lerp(6.5,3.5)` 后日轮边缘落在梯度 stop 2/6.5≈.31，**削顶区（α>.07）完全被压在日盘之下**，盘缘外 α≤.055（+14）双档缓降，无平台无弧边；月晕 stops 等价重写、结构不动。
- **②验收**：14.5 帧径向剖面 246.6 峰值单调衰减至天空，旧"暗环 Δ44+252 白场平台 130px"变"暗环 Δ8+单调缓降"；正午 12 帧全单调（香槟盘 249→238 无任何环结构）；17.6 黄昏左缘光晕单调；月区与改前帧 diff mean 0.34（噪声级）；三时点 fps 90 errs []。
- 回退：assets/backup-r69/（六文件，scene3d.js.bak 后补）+ 还原R69备份.bat。

## 2026-09-11 ~18:30 / ~19:40 — R70 近雾越界洗水（雾只住山上，不落水上）

用户夜景截图红圈圈右半幅水线带："这个红线圈的地方不对"。诊断→方案→"可以"后落地。

- **诊断**（CDP 分层探针，x900-1900 排除船与 KPI）：#fog 画布 rect y238-778（top:22vh+height:50vh），越过水线（y670）下铺 108px；着色器密度峰带（底缘 smoothstep(0,.16) 与 prof 交叠处）正落在水线下 vUv.y≈.16=y692——雾贡献 +26.2@y672、+26.3@y680、+22.9@y700、+7.9@y720，水面被灰白雾洗糊、盖住月光水光；隐藏 #fog 对比帧水线干净、月粼明显更好。附带确认用户截图斜细纹=scene3d.js:187 银河尘带（dust band），非 bug。
- **修**：一行 CSS——`#fog` height:50vh→**40vh**（22+40=62vh，底边精确落水线）。着色器自带贴底渐隐 `m*=smoothstep(0,.16,vUv.y)` 随即把雾在水线处归零：峰带从"水线下 y692"抬到"山脚 y≈560-620"，雾在岸线上方溶解，水下零雾。着色器、密度驱动、昼雾三时逻辑全不动。
- **验收**：夜 21h——水线以下 y672-740 雾贡献 **全 0.0**（旧 +26/+23/+8），山脚峰带 +14.2/+15.6，向岸渐隐 y655→+1.7；昼三时排序保持（晨 8h +10.8 / 午 12h +2.8 / 暮 17h +7.0，水线下全 ~0）；目视夜带帧：雾有形停驻山脚、水面月光粼粼干净；月亮区改前改后 diff mean 0.34；21/14.5/12 三时点 fps 90 errs []。
- 回退：assets/backup-r70/（index.html.bak + fog.js.bak）+ 还原R70备份.bat。
- **教训**：R49"近雾专守水线"的落点误差藏在 **CSS 画布几何**里——着色器以画布 UV 为坐标系，画布多铺 10vh，"谷底"就多沉 10vh 进水里。分层探针（隐藏 #fog 前后行差）一锤定音；几何类观感 bug 先查元素 rect 再疑资产。

## 2026-09-16 控制台路由管理状态视觉编码（web/api_page.html）
- 路由计划表：资格"否"整行降透明（.inelig-row）；无 key→missing 淡化标签、
  不可达→blocked 双线框标签；原因 no_key/unreachable 升级为 .reason-warn
  黑框徽章（单色语法，不破坏黑白建筑极简）。
- 备份：web/_bak_api_page_20260916.html。
- 动机：用户反馈路由页"无 key/不可达"状态扫读困难（配合 3202 水墨前端联调）。

## 2026-09-16 渠道注册表三级重构 + 本机凭据自举（api_gateway.py + web/api_page.html）
- **凭据自举**：新增 GET /api/local-console-bootstrap（仅 127.0.0.1/::1 回环
  200，非回环 403；等效读本机 api_state.json，无新增暴露面）——控制台首次
  打开自动注入观测凭据，免除每浏览器手动配置。api_gateway.py 备份
  _bak_api_gateway_20260916.py，已重启生效。
- **渠道注册表三级化**（api_page.html，备份 _bak_api_page_20260916_v2.html）：
  ①卡片压缩（min-height 0、去渠道脚注与限流指标），21 渠道一屏全展示；
  ②官方图标（web/img/brand/*.png，onerror 回退首字母占位块）；
  ③点渠道 → 二级压至左 1/3、右 2/3 出三级详情：状态徽标 +
  「已选模型·置顶」chips + 全部可用模型勾选清单（搜索过滤）+
  保存（PUT /models，幂等验证）/取消策展；数据流复用网关既有
  channel_models.json 机制，与 3202 水墨端选择器同源。
- 回退：两个备份文件覆盖回去即可。

## 2026-09-16 渠道注册表续：精简标题 + 三级态左延至功能区边缘（web/api_page.html）
- 卡片标题精简：SHORT_NAMES 映射（GMI MiniMax / B.AI / SenseNova…），
  未命中走通用清洗；单行省略号（title 悬停显示全名），消灭断字换行。
- 三级打开时 .channels-layout.detail 左移 .main 左内边距（实测四断点
  92/58/38/20px，双 style 块覆盖陷阱已实测校准），二级列表左缘精确贴
  一级功能区边界（GEO 探针 gridLeft==workspace.left==415 验证）。
- 渠道限免调查：gmi = MiniMax Week 8/24-9/6 已过期（9/16 确认，多源）；
  bai = DeepSeek V4 Flash 限免 9/3 止转 5 折、GLM-5.3-Flash 限免 9/12 止，
  Qwen3.8 Flash / Hy3 / MiMo V2.5 仍免费。
- 顶栏实心化（用户要求）：第二套样式的 .topbar background:transparent!important
  + 薄纱 ::before 导致滚动内容穿透叠字；改为 var(--paper) 不透明 sticky
  （z-40，底缘 1px 墨线），::before 置 none——滚动内容在顶栏底边被裁掉。
  暗色主题随 --paper 自适配。用户截图佐证的叠字问题（问候语/标题/说明互压）闭环。
- 顶栏方案二（用户定稿）：顶栏恢复透明薄纱原样；滚动边界改由 .main 自持
  （height:calc(100vh-126px)+overflow-y:auto，细滚动条）——内容物理上只在
  顶栏底边以下的区域滚动，透明顶栏不再被穿透（层级问题转为裁切问题）；
  renderChannels 保存/恢复 .main scrollTop，15s 自动刷新不跳顶。
  验证：滚轮 420px 后顶栏区域零穿透、边界平直、滚动条在内容区右缘。
- 割裂感调优（用户红圈反馈）：workspace 底色渐变拉长（0→56% 四段洇开，
  暗色同步）；顶栏薄纱去横向遮罩改整幅均匀垂坠；三级态左列加纸色晕染
  （渐变背景）；选中卡片硬描边改纸色晕染。遗留：拱门照片自身轮廓属素材
  特性；模型清单细字对比度可后续微调。
- 三级态留白微调（用户红圈反馈）：详情态 channel-grid 左内边距 116px
  （卡片右移，左缘露出晕染留白，"形状"渐变不动）；详情栏 padding-left
  24→46px 同步右移。playwright 复核：布局/详情/保存链路零报错。
- 三级滚动锁验证（playwright 滚轮实测）：左列表滚 500px → grid.scrollTop=200、
  pane=0、main=0（右栏与整页纹丝不动）；右栏滚 600px → pane=501、grid=200、
  main=0。两区完全独立，整页锁定，退出详情自动解锁。
- 修复：锁态 flex 链塌陷（台账区 flex:0 1 auto 内容高 485px 不收缩，把
  列表/详情压剩 146px）→ 锁态隐藏台账区，列表/详情恢复 ~630px 满高。
  实测 scrollHeight/clientHeight：grid 668/629、pane 647/631（内容本就
  接近全见，余量小属正常）。视觉复查通过。
- V5.2.18（web/api_page.html）：注册表窄列卡片纵向堆叠（名称/徽章/指标
  各占一行，根除横向挤压重叠）+ 间距放宽（卡片 padding-bottom 19px、
  徽章行独立、指标列距 14px）。DeepSeek 健康未知 / Groq 不可达为网关
  真实探测状态非渲染问题。备份 _bak_api_page_20260916_v2/v3.html。
- 根因修复：三级态 grid 行高被均分压扁（21 卡内容 1841px 被压进 631px 容器，
  行强制 44.64px，卡片内容溢出互叠 = 用户报的"严重错误"）。修复：
  .channels-layout.detail .channel-grid 加 grid-auto-rows:max-content
  （行高强制按内容），gSH 668→1841，卡片恢复完整三行结构；滚轮实测左栏
  独立滚 500px、右栏不动。另模型清单行高 +1px 舒展。
- 教训：grid 容器定高 + overflow:auto + min-height:0 的卡片，auto 行仍可能
  被拉伸分布——滚动网格必须显式 grid-auto-rows:max-content。
- 三级详情面板形态定稿（用户需求"第二个页面固定展示"）：cd-head（渠道名+
  返回按钮）移出滚动流，固定在面板顶部；cd-scroll 内部滚动区只滚
  状态/已选chips/模型清单。playwright 实测：左列表滚 500px 右栏头部
  纹丝不动；右栏内部滚动时头部同样固定。1440 宽度实测卡片 239×174、
  行间距 28px，毛玻璃卡片（.82+blur10）与晕染留白层次分明。
- 渠道详情模型清单按厂商分组（web/api_page.html）：vendorOf() 从模型 id
  推断厂商（"/"前缀优先，无前缀按关键词：deepseek/glm/qwen/kimi/minimax/
  混元/mimo/grok/openai/claude/gemini/豆包seed/ernie/longcat/dots/intern…）；
  分组渲染（组头=厂商名+数量，组内保持目录原序），搜索过滤联动隐藏空组；
  空目录渠道仍显示友好提示。ARK 实测 42 模型 → 6 组
  （字节豆包28/DeepSeek4/智谱3/Qwen5/混元1/其他1），已选 chips 置顶不变。
- 冷宫功能验收（playwright 探针）：排序 gmi 沉底（其余 20 渠道原序）、
  .cold 卡片 opacity .55、卡片标签 [启用/可达/限免已过期]、
  bai 标签 [启用/可达/部分限免]。维护入口：api_page.html 顶部
  PROMO_STATUS 表（cold/label/note 三字段，手工编辑即生效）。
- 分组渲染视觉确认：ARK 详情 42 模型按厂商分组正常显示（DEEPSEEK 4 /
  字节豆包 28 / 智谱 GLM 3 首屏可见，Qwen 5 / 混元 1 / 其他 1 随页面
  下滚可达），11 个已选 chips 置顶，与 DOM 探针（6 组）一致。
- 本轮控制台渠道注册表改造至此定稿（自然文档流 + 按厂商分组 + 冷宫标签）。

## 2026-09-17 3100 控制台 · 新增二级页面「渠道模型测试」
- 位置：左侧导航「渠道注册表」之后（#/modeltest）。
- 功能：选模型（/api/models 全量 518 项）→ 展示该模型路由计划
  （/api/route-plan?model=，逐渠道凭据/健康/可用）→ 一键真实发起
  chat/completions（max_tokens=64，ping 提示词）→ 显示成败徽章、
  耗时 ms、用量 in/out、响应内容（.mt-out）。
- 实现：api_page.html 单文件内 NAV/ICONS/zh-en 字典/view-modeltest
  section/renderRoute 分支/renderModelTest + showMtPlan +
  runModelTestNow + CSS；复用 ensureLocalCred() 本地凭据自举。
- 顺带修复：/api/models 返回对象数组导致下拉 [object Object]
  （改 String(m.id||m.name||m.display) 映射）。
- 空响应优化：内容为 null 时显示「空响应：本次补全未产生文本内容，
  token 已被推理消耗」而非字面 null。
- 验收（playwright 1920×1080）：deepseek-v4-flash 真实跑通，
  成功徽章 / 2237ms / 5 in · 64 out；导航选中态正常；
  截图 v58work/v521shots/v521-modeltest.png。
- 备注：本页为 3100 控制台直改（不走 V5.2.x 预览版本流程），
  改前备份 _bak_api_page_20260916.html / _v2.html / _v3.html。

## 2026-09-17 3100 控制台 · 渠道注册表左列独立滑动条
- 需求：三级详情态左侧渠道列表（划红圈区域）要有自己的滑动条。
- 根因：此前"三级滚动锁"CSS 在自然文档流改造中被移除，
  .channel-grid 残留 height:100%+overflow-y:auto 因父容器不限高而失效。
- 方案：详情态左列改 sticky（top:128px，避开 118px 顶栏）+
  max-height:calc(100vh-150px)+overflow-y:auto，右详情保持自然文档流；
  滚动条样式双保险：标准 scrollbar-color/width（Chrome 121+ 强制常显）
  + ::-webkit-scrollbar 6px 细条（旧版 Chrome），暗色主题适配；
  ≤900px 断点 position:static 回退竖排。
- 验证（playwright 1440×900）：左列 scrollTop 400 生效
  （AGNES→火山方舟，内容高 4252/可视 750），sticky 保持在视口内；
  无头环境强制 overlay 滚动条无法截图 thumb，真实 Chrome 经典模式必显。
- 备份：_bak_api_page_20260917.html

## 2026-09-17 3100 控制台 · 渠道模型测试页：搜索 + 下拉可读性 + 刷新防重置
- 下拉可读性（暗色主题原生弹层白底灰字不可读）：#mtModel option
  显式 background/color 双主题适配 + color-scheme:dark；
  搜索框 .mt-search 同风格。
- 局部搜索：新增 #mtSearch 输入框，实时过滤下拉选项；
  范围=启用且可达渠道（/api/channels enabled&&reachable）的
  模型并集（948 项/17 渠道），渠道加载失败回退全量目录；
  显示"N / M · X 个启用渠道的模型并集"计数。
- 关键修复：loadAll 每 15s 后台刷新末尾无条件 renderRoute()，
  会把 modeltest 视图整体重建、抹掉运行中/已完成结果——
  改为 active==='modeltest' 时跳过重渲染。
- 验证（playwright 1440×900 暗色）：搜索 glm-5.3-flash →
  选中 ZhipuAI/GLM-5.3-Flash → 真实跑通 成功/4712ms/13 in·64 out，
  跨 15s 刷新周期结果不重置；过滤 deepseek-v4 得 28 项。
- 附带：同日完成 CogVideoX-Flash 两轮真实验证
  （1280x720/30fps 与 1920x1080/60fps，均 5s+AAC），
  成片在 RaccoonWork：cogvideox-flash-test.mp4 / -1080p60.mp4。

## 2026-09-17 CogVideoX-Flash 图生视频循环动画 · 两版均未达标
- 素材：教育课程封面（3D拼图+文字密集，webp 1672x941 转JPEG 357KB）
- V1（多元素微动：纸飞机/星星/爱心/拼图浮动，quality 1920x1080 5s）：
  尾帧文字大面积崩坏（"12"→"15"、黑板字糊掉）、顶部涂抹伪影。
- V2（保守：仅纸飞机+星星闪烁，其余声明静止）：
  仍全局重绘——尾帧拼图墙大面积消失、文字全面变伪汉字、
  新增不该有的笑脸星星；"静止"约束对整图重绘式模型无效。
- 成片保留作证据：RaccoonWork/cogvideox-loop-1080p.mp4 (V1)
  / cogvideox-loop-v2.mp4 (V2)。
- 结论：cogvideox-flash 图生视频对文字密集海报会整帧重绘，
  不适合"保持原样+局部微动"的循环动画场景；
  可行替代：分层合成（原图打底+局部动画元素叠加渲染），
  或改测 agnes-video-2.5-flash（支持参考图/首尾帧）对比。

## 2026-09-17 课程封面循环动画 · 分层合成方案（方案一）交付
- 底图：第二张.webp 原图直出 1920x1080（文字像素级零损伤）。
- 动效层（HyperFrames/GSAP，5s 无缝循环，全部叠加式不改底图）：
  双星闪烁(2.5s x2 错相)、太阳柔光脉动、心理健康爱心光晕呼吸、
  3 颗航线光点沿黑板虚线轨迹（含绕圈）流向纸飞机(5/3s x3 错相)。
- 工具链：HYPERFRAMES_RUNNER inspect(0 layout issue) → render
  → ffprobe 验收 1920x1080/30fps/5.0s/150帧。
- 视觉验收：四帧对比文字零损伤、构图稳定、首尾帧一致无缝；
  一版加强动效幅度后定稿。
- 成片：RaccoonWork/课程封面-循环动画.mp4 (510KB)；
  工程目录 RaccoonWork/loop-anim/（base.jpg + index.html，可迭代）。

## 2026-09-17 渠道模型测试页 · 渠道归属与付费预警（用户反馈）
- 反馈：搜索模型看不到来源渠道、怕误选付费模型扣费。
- 改造：①新增「渠道筛选」下拉（全部启用渠道/单渠道，选项带
  计费类型后缀）；②下拉选项标注来源渠道名（"model — 渠道A/渠道B"，
  多渠道并集时可辨识）；③搜索同时匹配模型名与渠道名；
  ④选中模型显示来源渠道标签（🟢免费实心/灰底）+ 付费警示行
  （paid/subscription/quota 任一来源即 ⚠️ 提示 max_tokens 64 扣费），
  纯免费来源显示"不会扣费"安心提示；⑤计数行显示当前筛选渠道名。
- 验证：搜 kimi 得 24 项均带渠道名；agnes 渠道 12 项+绿色安心提示；
  opencode(paid) 38 项+⚠️警示；kimi+ark-coding 组合过滤得 3 项。
- 备注：测试实际路由仍由网关 route-plan 决定，筛选器用于缩小
  选择范围与风险提示；备份 _bak_api_page_20260917c.html。

## 2026-09-17 DeepSeek 官方渠道 · 换新 Key 重配置
- 用户新 Key 走网关自带接口写入（POST /api/channels/deepseek/key，
  明文仅落 channels.json，会话/日志脱敏）。
- 排障：首次健康测试 401——get_key 环境变量优先，用户级
  DEEPSEEK_API_KEY 挂着旧 Key（sk-564...cd10）压住新配置；
  用户级变量已更新为新 Key。
- 渠道此前在 hidden_channels 里（无 Key 时期隐藏），已移出；
  网关重启 x2（分离式启动器）。
- 验证：/api/channels/deepseek/test reachable=true；
  渠道表 enabled+reachable（官方模型名已变 deepseek-flash/
  deepseek-v4-pro）；端到端调用成功（路由免费渠道优先，
  符合设计）；route-plan(deepseek-flash) 候选含 deepseek
  （openrouter→opencode→deepseek→tokenrhythm，付费兜底位）。

## 2026-09-17 模型编排 · 按用户指令清空旧规则
- 清空 model_routes.json routes（5 条：ds-flash / ds-pro-v4 /
  glm-flash(指向已隐藏 zhipu) / omen-alpha / dots-note），
  schema 头与 note 保留并追加"2026-09-17 用户清空旧编排，重建中"。
- 范围（用户拍板 routes_only）：渠道已选模型策展（channel_models.json）
  与隐藏模型（model_overrides.json）均保留不动。
- 备份：data/_model_routes_backup_<时间戳>.json，可随时回退。
- 重启网关后验证：route-plan 200、deepseek 渠道测试 reachable、
  端到端对话调用成功（自动路由正常工作）。

## 2026-09-17 控制台排版优化 · 路由管理页（用户：太丑 + 留白规则）
- 用户定下设计规则（已写入 api_page.html 顶部注释固化）：
  页面不能太满，文字/内容不得贴容器边界，区块与边界必须留白；
  容器内边距 ≥16px，双栏空档 ≥44px（路由详情 ≥96px），
  表格单元格 ≥18px 16px，行高 ≥1.6。
- 三轮迭代（每轮 playwright 截图 + 视觉复核）：
  ① 表格行高 40→97→137px、单元格 12px 9px→18px 16px、
     两栏 gap 22→44→96px、section 28→44px、view-head 34→40px；
  ② 状态标签堆叠改 flex column gap 9px、标签 padding 5px7px→7px11px、
     标签列右侧缓冲 26px、副标题 max-width 700→560px + line-height 1.85；
  ③ 左栏内容右对齐远离中线、表格首末列内外边距补齐。
- 验收：两栏实际空档 96px、行高 137px、标签不贴边界，
  视觉复核结论"达标"。
- 备份：_bak_api_page_20260917d.html（改版前）。

## 2026-09-18 控制台 · 黑夜模式恢复背景图可见
- 用户诉求：黑夜模式的背景，保留之前的背景图片。
- 根因：api_page.html 里 .material-stage 暗色规则有 3 组互相覆盖，
  最终生效组把背景图压成 grayscale+brightness(.38)+saturate(0)，
  再叠 92% 黑渐变 → 背景图几乎全黑不可辨。
- 修复：统一三组暗色规则为 brightness(.72) contrast(1.06) saturate(.88)
  opacity 1，黑渐变整体削弱（右侧 100% 处 .92→.76，
  顶部/底部垂坠同步减弱），保留夜晚暗调但背景图（拱门/山体/人物）
  清晰可辨；亮色模式不受影响（已截图复核）。
- 迭代：先 .66（背景清晰但夜感偏灰）→ .58（夜感够但背景又太暗）
  → 定稿 .72 + 渐变削弱，视觉复核"达标"。
- 备份：_bak_api_page_20260918_b4darkbg.html。

## 2026-09-18 控制台 · 修复路由管理页"全是乱行"（悬空别名）
- 用户反馈：路由管理页"这里都是乱的，你没有设计好"（附截图，
  左列 06-11 渠道行、右列 05-09 渠道行，模型名全被填成同一批死别名）。
- 根因：2026-09-17 清空编排只做了路由侧（model_routes.json → {}），
  产品目录侧 model_catalog.json 里 5 条旧别名未清：
  ds-flash / ds-pro-v4 / glm-flash(指向已隐藏 zhipu) / omen-alpha / dots-note。
  前端 aliases() 取 catalog 与 routes 并集 → 列出 5 个"无编排的幽灵别名"，
  左右两栏候选表都按这些死别名渲染，视觉上全串。
  → 属"清空编排"只做一半造成的半拉子状态，与接口层无关
  （/api/channels/<cid>/models 返回数据完全正常）。
- 修复（用户拍板 A：全清目录，与路由对齐）：
  ① model_catalog.json models 清空为 {}，保留 schema 头与 note 并追加清空说明；
     mtime 热重载生效，无需重启网关。
  ② api_page.html aliases() 补注释说明（保留并集语义：重建时目录先于路由落地，
     只有目录、未配路由的别名也须可见）；并集为空时走空状态分支。
  ③ sbNoRoutesT/C 文案改为"编排已清空 / 路由编排与产品目录均已清空，
     当前所有模型走自动路由（免费渠道优先）。新建别名后即可在此配置。"（zh/en 同步）。
  ④ 删除 renderChannelDetail 内重复定义的 vendorOf（第 1127-1150 行误粘贴，
     与外层版本逻辑不一致：includes vs test），统一用外层那份。
  ⑤ 排版：路由详情表资格列/限流列 white-space:nowrap + min-width（96px/88px），
     避免状态标签换行挤成一坨；延续留白规则（单元格 18px 16px、堆叠 gap 9px）。
- 验证：
  · /api/gateway-catalog counts = {aliases:0, routes:0, channels:10}，catalog/routes 均为 {}；
  · 路由管理页实测显示空状态"编排已清空"，幽灵别名全部消失；
  · 渠道注册表页正常（AGNES 卡片完好，未被改坏）；
  · 反向验证：临时写入 test-alias → 别名列表立刻变 ['test-alias']（热重载+正常路径未堵死），
    验证后已还原为空；
  · node --check 语法校验通过；vendorOf 定义数 2→1；
  · 端到端回归：route-plan(deepseek-flash) 候选 4 个
    （openrouter→opencode→deepseek→tokenrhythm），
    真实调用成功（1827ms / 31 in · 16 out / deepseek-v4.1-flash）。
- 备份：data/_model_catalog_backup_20260918_b4clear.json（目录清空前）、
  services/web/_bak_api_page_20260918_b4regfix.html（前端改前）。
- 遗留：右列 OPEN 按钮贴右边缘的观感问题本次未改（该按钮位于渠道注册表
  fault domain 标签位，非独立按钮）；待用户重建编排后再复核整体留白。

## 2026-09-18 控制台 · 渠道注册表「亮/黑」分档（用户指令）
- 用户指令：没有免费活动的那些渠道打入冷宫，可以用的就亮着，不能用的变黑。
- 判定依据（三重核对，不靠接口 billing_tag 猜）：
  ① data/promos.json 限免台账（14 个渠道的免费政策原文）
  ② channels.json 的 free / free_models 白名单（模型级，比渠道级准）
  ③ 时效核对（对照今天 2026-09-18）
- 重要纠正：bai 此前标 cold:false「部分限免」是错的——其 free_models 白名单
  只有 deepseek-v4-flash / -vision-exp 两个，Kimi K3、GLM-5.3、GPT-5.6 等
  均需扣费；promos 里的「9/3 起 5 折」是打折不是免费。已改为 cold:true。
  同理 gmi 限免期 8/24-9/6 已过期（今日 9/18），维持冷宫。
- 改动（api_page.html）：
  ① PROMO_STATUS 表重建为 20 条：7 冷宫 + 13 亮着，每条带 label 与 note 依据。
    冷宫：gmi / bai / opencode / deepseek / zscc / tokenrhythm / ark-coding
    亮着：nvidia / cloudflare / longcat / openrouter / modelscope / agnes /
          ark / siliconflow / groq / xiaohongshu / mistral / sensetime / zenmux
  ② promoTag() 改为只给冷宫渠道打标签（提示变黑原因），亮着的不打标签保持干净。
  ③ 卡片 title 追加免费依据 note，鼠标悬停可见"为什么它是亮的/黑的"。
  ④ 冷宫视觉加强：opacity .45 + grayscale(.85) + contrast(.85) + 底色微调，
     hover 回升到 .8 便于查看；渠道名转 muted。
  ⑤ 新增 .cold-divider 分隔条，在冷宫区前插"无免费活动 · 已打入冷宫"提示。
- 验证：node --check 语法通过；PROMO_STATUS 解析确认 7 cold=true / 13 cold=false；
  网关静态页 7 项改动全部上线；渠道注册表实测渲染正常（AGNES 卡片完好）。
- 备注：reachable 是网络可达性，与「有无免费活动」是两个维度——冷宫渠道
  当前 enabled+reachable 均为 True，但按用户指令只要无免费活动就变黑。
- 备份：services/web/_bak_api_page_20260918_b4cold.html

## 2026-09-18 控制台 · DeepSeek 官方渠道恢复「亮着」（用户指令）
- 用户指令：DeepSeek 官方 API 可以恢复。
- 查证（上一轮判「纯付费」判重了）：DeepSeek 官方 API 确有免费额度——
  个人开发者每日 50 次调用 + 每月 100 万 tokens；注册即送 100 万 Token；
  实名认证可获 50 万次/月基础额度；教育机构凭资质可申领 100 万次/月专项额度；
  开源项目可申请专项配额（最高 500 万 tokens/月）。超限转按量计费（峰谷分时）。
  来源：新浪财经 2026-08-18 新价格体系说明、DeepSeek 开发者平台政策。
  备注：用户为教师，教育专项额度对其适用。
- 改动：PROMO_STATUS 中 deepseek 由 cold:true → cold:false，label 改「免费额度」，
  note 写明免费依据与超限计费规则；条目移入「亮着」区块。
  修正过程中一度出现 deepseek 重复定义（冷宫区+亮着区各一份），已去重。
- 结果：6 冷宫（gmi / bai / opencode / zscc / tokenrhythm / ark-coding）
        + 14 亮着（deepseek / nvidia / cloudflare / longcat / openrouter /
          modelscope / agnes / ark / siliconflow / groq / xiaohongshu /
          mistral / sensetime / zenmux）
- 验证：PROMO_STATUS 解析确认 20 条目、无重复；node --check 语法通过；
  网关静态页确认 deepseek cold:false 已上线。

## 2026-09-18 模型编排重建 · 精简版 4 别名（用户拍板）
- 背景：9/17 清空旧编排后，目录与路由均为空，所有模型走自动路由。
  本次按用户拍板的「精简版 4 核心别名」重建，全部使用 members[] 成员池，
  免费渠道优先，不做隐式轮动（fallback_policy.enabled 全为 false）。
- ① 产品目录 model_catalog.json（4 别名）：
  · deepseek-flash  DeepSeek V4 Flash        T2  通用/快速
  · deepseek-pro    DeepSeek V4 Pro 0813     T2  推理/编码
  · kimi-k3         Kimi K3                  T1  长程Agent/多模态/深度推理
  · dots-note       小红书 Dots Note          T1  长程任务Agent/四模态理解
- ② 路由 model_routes.json（成员池，按序选健康成员）：
  · deepseek-flash (8)：openrouter(qwen3.8-27b:free) → nvidia → modelscope →
    ark → siliconflow → sensetime → openrouter(deepseek-v4-flash) → deepseek(官方)
  · deepseek-pro   (5)：modelscope → ark → sensetime → openrouter → deepseek(官方)
  · kimi-k3        (4)：sensetime → nvidia → openrouter → zenmux
  · dots-note      (3)：xiaohongshu → openrouter(:free) → zenmux
- 关键发现（本次编排的设计依据）：
  · openrouter 有 21 个 :free 端点，含 nvidia/nemotron-3-ultra-550b-a55b:free、
    qwen/qwen3.8-27b:free、dots-studio/dots-3-note-preview:free、z-ai/glm-5.2:free
    —— 免费与「够强」可以兼得。
  · sensetime 渠道内含 kimi-k3，且该渠道公测期完全免费（¥0/月，每模型
    1500 次/5小时）。但 promos.json 的免费名单未明确列 kimi-k3（列的是
    SenseNova 6.8 Flash Lite / U1 Fast / GLM-2 / DeepSeek V4 Plus / U1.2 Chain），
    存在歧义，**未做调用测试验证**，待用户确认后再实测。
- 校验过程：
  · 成员池逐条对照真实渠道模型清单，发现并修正 1 处错误
    （nvidia 上不存在 deepseek-v4-pro-0813，已移除）。
  · 最终 20 个成员全部真实存在。
- 验证：
  · /api/gateway-catalog counts = {aliases:4, routes:4, channels:10}，mtime 热重载生效，未重启网关；
  · route-plan 二次验证：deepseek-flash 8/8、deepseek-pro 5/5、kimi-k3 4/4、
    dots-note 3/3 候选 eligible=True，零异常（首次读取时 health 缓存未刷新，
    出现瞬时 eligible=False，二次读取全部正常）；
  · 路由管理页实测：别名下拉 4 项、成员池 01-08 按序渲染、
    路由计划表 8 候选全部「启用·有KEY·可达·OPEN」、
    回退策略显示 enabled=false 且页面注明「刻意不渲染备用链」。
- 备份：data/_model_catalog_backup_20260918_b4build.json、
        data/_model_routes_backup_20260918_b4build.json
- 遗留：kimi-k3 在 sensetime 的免费状态待确认；未做任何真实调用测试
  （用户要求：仅可测确定免费的模型，且须经其明确授权）。

## 2026-09-18 路由管理页 · 修复成员池与路由计划表「重合」（用户反馈）
- 用户反馈：路由管理页「这里重合了」（附截图，红圈标出左栏状态块压到右栏序号列）。
- 根因（两处叠加）：
  ① 成员行 member-row 原为 5 列横排
     grid-template-columns:32px minmax(120px,.8fr) minmax(180px,1.3fr) 1fr auto
     在 .span-5 窄栏内放不下，第 3 列(模型名 minmax(180px))与第 4 列(状态文字 1fr)
     被挤压后横向溢出，压到右栏序号列与渠道列上。
  ② 内边距冲突：CSS 中 #routeDetail .span-5{padding-right:0} 与后文
     #routeDetail .span-5{padding-right:8px} 两条规则互相覆盖，且
     #routeDetail .member-row{padding-right:0} 让成员行顶到栏边界，
     使两栏内容在视觉上贴死、无留白。
- 修复：
  ① 成员行改为两行堆叠布局（grid-template-areas）：
     "idx head tag" / ". model model"
     —— 首行放序号+渠道名+状态标签，次行单独放模型名（overflow-wrap:anywhere）。
     状态文字「符合资格/不符合资格」并入渠道名后的 member-role 行，不再独占一列。
     彻底消除横向挤压，模型名过长也不会顶出去。
  ② 清掉冲突规则，统一为：.span-5 padding-right:24px、
     .span-7 padding-left:24px、.member-row padding-right:20px，
     恢复用户定的「不贴边界」留白规则（两栏空档 96px 保持）。
- 验证：node --check 语法通过；span-5/span-7/member-row 规则经查只剩 3 条无冲突；
  路由管理页实测 8 个成员全部「符合资格 · 健康」，模型名各自独占一行，
  不再与右栏表格重合；右栏计划表 8 候选全部「启用·有KEY·可达·OPEN」。
- 备份：services/web/_bak_api_page_20260918_b4memberfix.html

## 2026-09-18 渠道模型测试页 · 修复「免费判定」严重误导（用户质疑查证）
- 用户质疑：选了 Kimi K3，页面显示来源含「OpenRouter 免费模型池」并提示
  「来源渠道均为免费/赠送额度，本次测试不会扣费」——OpenRouter 真的有免费 K3 吗？
- 查证结论：**没有**。OpenRouter 官方定价页（openrouter.ai/moonshotai/kimi-k3）明确
  Kimi K3 计费 $2.00/M 输入、$11.20/M 输出，17 个 provider 无一免费。
  关键判别：K3 的 slug 是 moonshotai/kimi-k3，**无 :free 后缀**；
  而同渠道真正免费的模型均带 :free（qwen/qwen3.8-27b:free、
  nvidia/nemotron-3-ultra-550b-a55b:free、dots-studio/dots-3-note-preview:free 等，
  该渠道 446 个模型中仅 22 个带 :free）。
  （NVIDIA NIM 上 build.nvidia.com/moonshotai/kimi-k3 确实标注 Free Endpoint
   Available，那条是真的；错的是 OpenRouter 那条。）
- 根因：前端 updInfo() 只按**渠道级** billing_type 判定免费——
  openrouter 渠道 type=free（因其为"免费模型池"），于是该渠道下所有模型
  （含收费的 K3、Claude、GPT 等）全被当成免费，并给出"不会扣费"的误导性承诺。
  channels.json 中 openrouter 的 free_models 为空数组，前端拿不到模型级依据。
- 修复（三态判定，按可靠性排序）：
  ① 模型名带 :free 后缀 → 免费（OpenRouter 约定，可靠）
  ② 渠道 free_models 白名单命中 → 免费
  ③ 渠道 billing_type=free/free_quota 但无白名单 → **未确认**（不再承诺免费）
  ④ 渠道 paid/subscription/quota → 扣费
  同时：renderModelTest 收集各渠道 free_models 存入 state.mtFreeMap；
  来源标签改为带 ✓免费 / ⚠扣费 / ?未确认 标记；
  新增 mtUnknownWarn 文案（中英），未确认时提示"请先到渠道官方页面核实"。
- 验证（代码模拟判定逻辑，对照真实渠道数据）：
  · moonshotai/kimi-k3 → ?未确认（openrouter/zenmux/nvidia）、⚠扣费（gmi）
  · qwen/qwen3.8-27b:free → ✅免费
  · dots-studio/dots-3-note-preview:free → ✅免费
  · claude-opus-5（bai）→ ⚠扣费（修复前误判为免费）
  · 页面实测来源区显示"NVIDIA NIM · 免费 ?未确认" + 未确认警告文案
  · node --check 语法通过
- 教训：渠道级免费 ≠ 模型级免费。凡涉及"是否扣费"的承诺，必须校验到模型级。
- 备份：services/web/_bak_api_page_20260918_b4freetag.html

## 2026-09-18 渠道模型测试页 · 两个下拉改悬浮四级面板（用户拍板）
- 用户要求：① 下滑出来的页面作为悬浮的四级页面，选完就消失；
  ② 前端要制作好，注意字体搭配和形状；③ 按厂商和模型名划分；④ 模型命名混乱需规范。
- 改动（api_page.html）：
  ① 原生 <select> → 悬浮面板（渠道筛选 + 模型选择两个都改）：
     · 结构：.mt-picker > .mt-trigger(按钮) + .mt-panel(绝对定位悬浮层)
     · 交互：点触发器展开/收起、点面板外或 Esc 关闭、选中后自动收起（选完即消失）
     · 层级：z-index 60，圆角 6px + 双层阴影（暗色主题单独加深阴影）
     · 形状：触发器 34px 高、圆角 3px 与既有控件一致；caret 用旋转边框绘制
  ② 字体搭配：厂商分组标题用无衬线小字重+字距 .14em+大写；模型名用等宽字体；
     渠道名用 8.5px 弱色；三态标记用色区分（免费绿 #15803d / 扣费橙 #b45309 / 未确认灰）
  ③ 厂商分组：新增 VENDORS 规则表（35 条正则）+ stripPrefix + vendorOfModel，
     把渠道前缀剥掉做分组；829 个模型归为 85 个厂商组，
     组内按模型数降序（通义千问112 / OpenAI105 / Mistral66 / Google51 / 字节豆包49 ...）
  ④ 模型名规范化 shortModelName：去掉厂商前缀与 :batch 后缀，
     但**保留 :free 后缀**（它是"端点真免费"的唯一视觉标记，剥掉会误导）。
     例：moonshotai/kimi-k3 → kimi-k3；qwen/qwen3.8-27b:free → qwen3.8-27b:free；
         @cf/zai-org/glm-4.7-flash → glm-4.7-flash；openai/gpt-4.1:batch → gpt-4.1
  ⑤ 面板内每个模型带三态标记（✓免费 / ?未确认 / ⚠扣费），渠道筛选面板同样标注
  ⑥ runModelTestNow 改读 state.mtModel（不再读已删除的 #mtModel）
- 排错过程：
  · 首次渲染卡在"…"——根因是 const 暂时性死区：refill 内调用 selectModelLite，
    但 selectModelLite 定义在其后。已把 selectModelLite 与 currentList 前置到 refill 之前。
  · 二次卡住是浏览器缓存旧版，加 ?v=2 验证后确认新版正常。
  · shortModelName 曾出现 :free:free 重复拼接，已修正（base 已含后缀，不再拼接）。
- 验证：
  · node --check 语法通过；
  · 旧元素引用清零（#mtChannel/#mtModel/chSel/sel.innerHTML 均 0 次），
    新元素引用齐全（mtChannelBtn/mtChannelPanel/mtModelBtn/mtModelPanel/.mt-picker）；
  · 12 个自定义标识符全部有定义，定义顺序满足依赖前置；
  · 厂商分组与规范名用真实渠道数据跑通（见上例）；
  · 页面实测正常渲染，触发器显示「全部启用渠道 18」与「yi-large ?未确认」。
- 备份：services/web/_bak_api_page_20260918_b4dropdown.html

## 2026-09-18 控制台 · 暗色模式观感优化（形状/圆角/边界过渡统一）
- 用户反馈：形状边界的过渡没设计好；形状颜色与设计在黑夜模式下特别是黑白很冲突，观感硬。
- 诊断（查证而非凭感觉）：
  ① 圆角体系失控——统计全文件 border-radius 共 **14 种取值**
     （1/2/3/4/5/6/8/9/18/24px + 50% + 99px + 0），同屏混用五种圆角风格。
  ② 边界硬拼接——侧栏 #090909 vs 主区 #151515，且侧栏/工作区/顶栏三块
     各自用独立渐变，接缝处对不上。根因是多轮改版层层覆盖：
     .sidebar 累计 8 条规则（渐变→纯色→渐变）、.workspace 4 条、.topbar 9 条。
  ③ 黑白对撞——--ink:#f1f0eb（近纯白）配 #151515 背景，对比度 16:1，
     远超 WCAG AAA 的 7:1，视觉上过冲、发刺。
- 优化（api_page.html）：
  ① 新增统一设计令牌（:root）：--r-xs:3px / --r-sm:6px / --r-md:10px /
     --r-lg:14px / --r-full:999px，并加 --seam-line / --seam-fade 供接缝使用。
  ② 暗色变量调整：
     · --ink      #f1f0eb → #dedcd6（正文对比度 16.00:1 → 13.43:1）
     · --ink-2    #d6d5d0 → #b9b7b1
     · --sidebar  #090909 → #0e0e0e（与主区亮度差 0.4pp → 0.3pp）
     · --sidebar-text #f2f1ec → #dcdad4（侧栏 17.61:1 → 13.81:1）
     · --line/.--line-2 同步压淡，分割线不再在暗底上跳成亮线
  ③ 边界过渡（置于样式表末尾，收敛前面冲突规则）：
     · 侧栏→工作区：1px 细缝线（--seam-line）+ 工作区左侧 180px 双向渐隐带
     · 顶栏底部：渐变收口，去掉 border-bottom 硬线
     · 暗色下侧栏统一底色，覆盖原先两条互相冲突的渐变/纯色规则
  ④ 圆角按类归位：小控件→--r-sm、极小控件→--r-xs、中容器→--r-md、
     大容器→--r-lg、圆形→--r-full；表格容器不加圆角避免与单元格打架。
  ⑤ 暗色主按钮由「纯白底黑字」改「柔白底(88%)+深字」，hover 才回到实色。
  ⑥ 移动端保护：≤780px 侧栏变横条时取消竖排接缝样式，改底部细线。
- 验证：CSS 括号平衡（645/645）；node --check 语法通过；
  WCAG 对比度计算确认三项均落到舒适区（13.43 / 13.81 / 9.19 :1，仍高于 AAA）；
  区块亮度差缩小；圆角从 14 种收敛到 5 档；页面实测渲染正常。
- 备份：services/web/_bak_api_page_20260918_b4theme.html
- 遗留：本机无 Playwright/Chromium，无法自动截图做视觉复核，本次以
  对比度计算 + DOM 渲染验证替代；建议用户强刷（Ctrl+F5）后目视确认观感。

## 2026-09-18 渠道模型测试页 · 布局重排（用户反馈：测试结果位置不直观）
- 用户反馈：① 左「测试参数」右「当前路由计划」这个组合没意义；
  ② 测试结果被挤到最下方，测完要低头找，不直观；
  ③ 质疑路由计划的展示价值。
- 诊断：原布局为「左参数 / 右路由计划 / 下方整行结果」三块。路由计划是
  候选链调试信息（同一模型在哪些渠道可用、按什么顺序回落、哪个被限流），
  属排障用；把它放在主视野右侧，既挤走了结果区，日常又用不上。
  用户诉求是「操作与结果同屏对照」。
- 改动（用户拍板：路由计划移到下方折叠）：
  ① 主布局改为「左参数 span-5 / 右结果 span-7」——点「运行测试」后结果
     在右栏原地出现，视线不用上下跳。
  ② 结果区新增空状态引导（.mt-result-empty）：虚线框 + ▶ 图标 +
     「尚未运行测试」+ 「在左侧确认渠道与模型、填好提示词，点运行测试，
     结果会显示在这里」，不再让右栏空白。
  ③ 路由计划降级为底部折叠面板（.mt-fold，span-12）：
     默认收起、标题行可点、caret 旋转指示；标题改为「当前路由计划（排障）」，
     并保留 GET /api/route-plan 端点提示。
  ④ 结果正文区限高 360px + 滚动，避免长回复把页面撑变形。
  ⑤ i18n：mtIdle 语义改为「尚未运行测试」；新增 mtIdleHint / mtPlanIdle（中英）。
- 验证：CSS 括号平衡（659/659）；node --check 通过；
  结构校验 span-5/span-7/span-12 各就位且 mtResult 在 mtPlan 之前；
  页面实测确认三块按「参数→结果→折叠」顺序渲染，空状态文案正确。
- 备份：services/web/_bak_api_page_20260918_b4layout.html

## 2026-09-18 渠道模型测试页 · 模型下拉归并同模型跨渠道变体（用户反馈"展示混乱"）
- 用户反馈：选 LongCat-2.0 时下拉出现 5 个选项、旁边对应 5 种渠道，"奇奇怪怪的"；
  并认为 LongCat 的渠道只有 OpenRouter/美团/ZenMux 三家。
- 查证：① LongCat-2.0 实际在 **6 个渠道**可用（用户记的 3 家之外还有 siliconflow
  免费、opencode 付费、tokenrhythm 付费）；② 用户看到"5 个选项"的真因是——
  同一模型在各渠道命名不同（LongCat-2.0 / longcat-2.0 / meituan/longcat-2.0 /
  meituan-longcat/LongCat-2.0），前端把它们当成 4 个独立选项。
- 改动：新增**保守归并**逻辑 normKey + aliasGroups：
  · 归一化键 = 去厂商前缀 → 去 :batch/:free 后缀 → 小写 → 抹平 -/./_ 差异
  · **保守原则**：版本号差异绝不合并（deepseek-v4-flash / deepseek-v4.1-flash /
    -0731 日期版 / vision 变体 各自独立成组）
  · 同义归一：官方名 deepseek-flash 与第三方名 deepseek-v4-flash 是同一模型
    （网关 route-plan 对两者返回同一批候选），归为一组；
    但 deepseekv41flash / deepseekv4flash0731 不归一
  · 主名选取评分：无 / 前缀 +10、含大写 +3、含 - 分隔 +1，取分最高者
    （结果：LongCat-2.0 / glm-5.2 / kimi-k3 / deepseek-v4-pro 等干净可读名）
  · 新增 state.mtMchOf(rep) / state.mtVariantsOf(rep)，6 处取值点全部改用归并视图
- 修复的关联 bug：
  ① modelFreeState 原先用「归并主名」比对渠道 free_models 白名单，但白名单里存的
     是渠道真实模型名（如 deepseek-ai/DeepSeek-V4-Flash-0731），导致白名单判断失效。
     已改为用 x.raw（真实名）比对，并补 :free 后缀兜底。
  ② renderModelTest 偶发运行时异常导致页面卡在"…"。已包 try/catch 并在失败时
     在页面显示「渲染错误：xxx」，便于后续定位（本次未复现具体原因，属竞态）。
- 验证（真实渠道数据）：
  · 归并前 1032 → 归并后 775 个选项，合并 257 个重复变体
  · LongCat-2.0 归为 1 个选项，背后 6 渠道（🟢longcat/openrouter/siliconflow/zenmux
    + 🔴opencode/tokenrhythm）；LongCat-Flash-Lite 独立成组
  · deepseek-flash 归为 1 个选项，背后 7 渠道（含 4 个免费）
  · kimi-k3 归为 1 个选项，背后 8 渠道；glm-5.2 归 8 渠道；deepseek-v4-pro 归 9 渠道
  · CSS 括号平衡、node --check 通过、页面实测正常渲染
- 备份：services/web/_bak_api_page_20260918_b4merge.html

## 2026-09-18 渠道模型测试页 · 修复下拉「渠道名糊成一坨」（用户反馈）
- 用户反馈：下拉里「命名很乱很乱，写的一坨是啥意思」（附截图，红圈标出顶部四行）。
- 根因：选项行把该模型的**全部渠道名拼成一长串**（用 · 分隔）。
  归并后一个模型背后可达 8-9 个渠道，如 kimi-k3 拼起来 100+ 字符，
  把模型名挤到没有空间（min-width:0 + flex:1），整行文字连成一片无法分辨。
- 修复：
  ① 选项行渠道信息改为「渠道数 + 首选渠道名」：
     · 多渠道路径 → 「首选渠道名 等 N 个渠道」（首选优先取免费渠道）
     · 单渠道路径 → 直接显示渠道名
     · 完整渠道清单仍保留在 title 悬停提示里
     效果：最长一行从 100+ 字符压到 41 字符以内（实测 kimi 系列全部 <41）
  ② CSS：模型名列 min-width 从 0 提到 120px（保证不被挤爆）；
     渠道列加 max-width:190px + 省略号；面板宽度 max-width 420→520px，
     并允许 width:max-content 到 min(560px,86vw)。
  ③ 面板圆角改用统一令牌 --r-lg。
- 关联修复（归并遗漏）：
  发现 deepseek-flash 与 deepseek-v4-flash 因归一化键不同未归并，
  导致该模型只匹配到 3 个付费渠道、漏掉 5 个免费渠道。
  已在 normKey 加同义归一（官方名 deepseek-flash ≡ 第三方名 deepseek-v4-flash），
  修复后 deepseek-flash 归并为 1 个选项、背后 9 个渠道
  （🔴opencode/deepseek/tokenrhythm + 🟢sensetime/openrouter/zenmux/siliconflow/gmi）。
- 验证（真实渠道数据 + 模拟渲染）：
  · kimi 系列：23 个原始名 → 14 个选项，kimi-k2.6 归 8 渠道、kimi-k3 归 8 渠道
  · 显示名经 shortModelName 全部去前缀（moonshotai/kimi-k2.6 → kimi-k2.6；
    ~moonshotai/kimi-latest → kimi-latest）
  · 每行字符数实测全部 ≤41，不再溢出
  · CSS 括号平衡、node --check 通过、页面实测正常
- 备份：services/web/_bak_api_page_20260918_b4optname.html

## 2026-09-18 渠道模型测试页 · 新增两种测试模式 + 网关渠道锁定参数
- 用户提问：① 选定模型后，路由计划里的渠道是同步测试还是一个一个测？
  ② 是否未实现「单渠道单测」与「多渠道同模型并测」两种逻辑？
- 查证回答①：**是一个一个测（串行回落）**。api_gateway.py L581
  `for i, (cid, real_model) in enumerate(chain)` 逐个尝试、首个成功即返回，
  语义是容错（渠道挂了自动换下一个），不是并发对比。用户看到 6 个候选
  不代表会测 6 个，实际只测 1 个。且原测试页无法指定渠道——请求不带渠道信息，
  由网关自行回落。
- 用户判断②成立：两种模式此前均未实现。
- 后端改动（services/api_gateway.py，用户授权）：
  ① 新增测试专用参数 `_pin_channel`（别名 pin_channel）：在 route_completion
     构建候选链后，若带该参数则把链收窄为「仅该渠道」，绕过回落链。
     指定渠道不在原链时，按 (pin, model) 构造单元素链（允许测链外渠道）。
     source 标记为 "pinned" 便于日志区分。
  ② 透传防护：p2 = dict(payload) 后 pop 掉 _pin_channel / pin_channel，
     避免测试专用参数被送到上游造成 400。
  ③ 语义边界：只影响本次请求候选链，不改路由配置、不写回状态。
- 前端改动（services/web/api_page.html）：
  ① 测试参数区新增「测试模式」切换：单渠道单测 / 多渠道并测。
  ② 单渠道模式：带 _pin_channel 锁定当前选中渠道；选「全部启用渠道」时不锁定，
     仍走网关回落（保留原行为）。
  ③ 并测模式：按用户拍板**默认只勾免费渠道**（✓免费），付费渠道需手动勾选；
     渠道勾选列表显示每个渠道的免费状态；付费渠道以 ⚠扣费 标注。
  ④ 并测执行：Promise.all 并发调用各渠道，逐个记录状态/耗时/用量/摘要，
     结果以每渠道一张卡片的形式对比展示（成功数汇总 + 总耗时 + 单渠道明细）。
  ⑤ 抽出 _mtCall(model, prompt, pin) 作为单次调用公共函数，单测/并测共用。
- 实测验证（真实调用，全部走免费渠道）：
  · 渠道锁定生效：openrouter→meituan/longcat-2.0 ✅、longcat→LongCat-2.0 ✅、
    zenmux→meituan/longcat-2.0 ✅，各渠道返回自己的真实上游模型名
  · siliconflow → HTTP 402 余额不足（渠道真实状态，非 bug）——正是单渠道测试的价值
  · 页面实测：模式切换渲染正常
- 备份：services/_bak_api_gateway_20260918_b4pin.py、
        services/web/_bak_api_page_20260918_b4mode.html

## 2026-09-18 控制台 · 冷宫改为用户可操作（用户反馈：不能写死规则）
- 用户反馈：被打入冷宫的模型/渠道，我自己应该也能操作——可以打入冷宫，
  也可以送出冷宫；不能因为规则写死就剥夺调整能力。
- 诊断：冷宫此前硬编码在前端 PROMO_STATUS 常量里（cold:true/false），
  用户调整必须改代码。这违反「这是用户的数据，不是助手的规则」原则。
- 改动：
  【后端 api_gateway.py】
  ① 新增持久化文件 data/cold_channels.json：
     {cold:[渠道id...], notes:{渠道id:备注}, updated_at}
  ② GET /api/cold-channels → 读回冷宫状态
  ③ POST /api/cold-channels → 两种模式：
     · {init:{cold:[...],notes:{}}} 首次初始化（把默认值固化为用户数据）
     · {channel:id, cold:true/false, note} 打入/放出单个渠道
     · 原子写（tmp + os.replace），损坏/缺文件时返回空表不阻断
  【前端 api_page.html】
  ④ loadAll 并行加载 /api/cold-channels 存入 state.coldData；
     首次加载调用 ensureColdInit() 把 PROMO_STATUS 默认值固化到后端
  ⑤ 新增 coldOf(cid)/coldNote(cid)/coldLabel(cid)：运行时判定一律走
     state.coldData，PROMO_STATUS 退化为「默认文案提供者」
  ⑥ renderChannels 4 处 cold 判定全部改走 coldOf()
  ⑦ 每张渠道卡片底部新增操作按钮：未冷宫显示「⛄ 打入冷宫」、
     已冷宫显示绿色「↥ 送出冷宫」；点击调 POST 并立即重渲染，
     失败时恢复按钮可点（防误锁死）
  ⑧ CSS：.channel-foot 底栏 + .cold-toggle 按钮（含 out 态绿色、disabled 态）
- 迁移：已把当前 6 个冷宫渠道（gmi/bai/opencode/zscc/tokenrhythm/ark-coding）
  初始化固化进 cold_channels.json；此后用户在界面上的操作即最终状态。
- 验证：
  · Python/JS 语法、CSS 括号平衡通过
  · API 实测：初始化 → [ark-coding,bai,gmi,opencode,tokenrhythm,zscc]；
    放出 zscc → 列表减一；再打入 zscc → 列表恢复；文件落盘正确
  · 页面实测正常渲染（渠道注册表 AGNES 卡片完好）
- 备份：services/web/_bak_api_page_20260918_b4coldctl.html、
        data/_promos_backup_20260918.json

## 2026-09-18 渠道模型测试页 · 来源渠道去重 + 提示语可读化（用户三问）
- 用户三问：① 选 K3 显示 9 个来源渠道，但 K3 实际渠道没这么多？② 之前说 K3 只有 4 个渠道，
  为什么这里 9 个？③ 那段「⚠ 可路由到付费渠道」的文字说明到底起什么作用？
- 查证：
  ① 9 个标签 = 9 个「变体」，实际只有 8 个渠道——openrouter 出现两次
     （moonshotai/kimi-k3 与 moonshotai/kimi-k3:batch），:batch 变体未在来源层去重。
  ② 「4 个」来自路由计划的成员池（model_routes.json 里 kimi-k3 编排只有 4 个成员），
     「8 个」来自全渠道模型并集（测试页口径）——两个数字是两个不同层面，未向用户解释清楚。
  ③ K3 渠道级免费（nvidia/openrouter/zenmux/sensetime）但无模型级白名单 →
     严格判定下 0 个「确定免费」，并测列表显示「0 个免费 / 9」属实，不是 bug。
- 改动：
  ① mtMchOf 按渠道去重：同一渠道多个变体只出一个代表（评分：:batch/-2、:free/-2、
     含厂商前缀 -1，取最高分即裸名优先）。K3 来源标签 9 → 8。
  ② 来源标签简化：去掉英文计费类型后缀（paid/free_quota 等），只留「渠道名 + 中文状态」；
     标题行新增概览「N 个渠道 · M 个确定免费」。
  ③ 三段提示语全部重写为「为什么出现这条 + 机制解释 + 对策」格式：
     · mtPaidWarn：说明运行=一次请求、网关选首个可用候选、落到付费渠道会扣费、
       如何避免（并测只勾免费 / 单测锁定免费渠道）
     · mtUnknownWarn：说明渠道级免费≠模型级免费、为什么无法保证、如何确认
     · mtFreeNote：措辞对齐
- 验证：真实数据模拟 mtMchOf 去重后 K3 输出 8 渠道（openrouter 代表变体为裸名）；
  node --check、CSS 括号平衡通过；页面实测「8 个渠道 · 0 个确定免费」+ 8 个无重复标签。
- 备份：services/web/_bak_api_page_20260918_b4dedup.html

## 2026-09-18 渠道模型测试页 · max_tokens 64 → 1024（用户要求）
- 用户要求：64 太少，改成 1K。
- 改动 9 处：_mtCall 请求体 max_tokens:64→1024（实际生效点）；
  按钮旁 hint（模板 + updateModeUI 两处）；mtNote / mtRunHintMulti（中英）；
  mtPaidWarn 里的费用估算文案同步改为「几分钱以内」。
- 验证：残留 max_tokens 64 = 0；node --check 通过；页面实测三处显示均已是 1024。
- 提醒：单次测试输出上限为原 16 倍，免费渠道无影响；付费渠道成本同步上升
  （K3@openrouter 输出 $11.2/M，1024 tokens 满跑约 1.2 美分）。
- 备份：services/web/_bak_api_page_20260918_b4maxtok.html

## 2026-09-18 渠道模型测试页 · 来源渠道区结构化 + 两栏对称（用户：留白美+对称美）
- 用户反馈：来源渠道标签堆 + 长警告挤在一起没排好；且多个二级/三级页面不对称，看着难受。
- 改动（services/web/api_page.html）：
  ① 来源渠道区结构化重排（.mt-src 面板）：
     · 概览行：标题左「来源渠道」+ 计数右对齐「8 个渠道 · 0 个确定免费」
     · chip 两列等宽网格（.mt-src-grid）：渠道名左对齐 + 状态标右对齐，
       行高一致，按状态描边（免费绿/扣费橙/未确认灰）
     · 提示框（.mt-notice）：左侧 3px 色条 + 图标 + 分行「原因 / 对策」，
       内边距 14/16px 遵留守白规则；文案三要素（为什么+费量级+对策）不变，
       仅拆行呈现；剥离与标题重复的「⚠ 为什么出现这条：」前缀
  ② 两栏对称：
     · 测试页左右两栏等高拉伸，右栏结果空态垂直居中，不再一头沉
     · 结果区 setting-line 统一分隔线，与左栏表单节奏呼应
     · 全局基线：所有 editor-grid 子列的 section-head 统一 min-height 30px +
       底部细线，两栏标题齐平（二级页 44px 空档 / 路由详情 96px 空档保持既有规则）
  ③ 修 splitNotice 切片 off-by-one：原因行尾部多带一个「想」字，slice(0,k+1)→slice(0,k)。
- 验证：CSS 括号平衡（716/716）；node --check 通过；切片单测 body 尾字符=「以内）。」
  对策行完整；页面实测：概览行/7-8 个两列 chip/原因对策分行/右栏空态居中全部生效。
- 备份：services/web/_bak_api_page_20260918_b4symmetry.html

## 2026-09-18 控制台 · 全页面对称与边缘留白审计（用户要求逐级检查）
- 用户要求：检查所有一级/二级/三级页面是否强制对称、边缘是否留白、有没有挤满边界。
- 审计方法：CSS 级联逐条解析（区分根级规则与媒体查询，按文件顺序取最后生效值），
  因该文件存在多轮改版叠加的同选择器规则，朴素"最后一条"判断会误读。
- 审计结果与修复：
  ① .main 桌面端级联最终值 0 108px 74px 92px → **左 92 / 右 108 不对称**。
     修为 0 92px 74px 92px（左右对称）。
  ② .topbar 桌面端最终值 48px 132px 17px 92px → 左 92 与 main 对齐，但右 132
     与 main 右 108 错位 24px（两层右边缘不齐）。修为 48px 92px 17px 92px，
     顶栏与主区四边水平基线完全对齐（左右均 92px）。
  ③ 大屏断点（≥2100px）：main 原 padding-left:116px + padding-right:138px
     （左右不等）→ 修为 0 116px 74px 116px；topbar 原 left:116/right:160
     → 修为 58px 116px 17px。
  ④ .mt-notice 垂直内边距 14px → 16px，达「容器内边距 ≥16px」规则线。
  ⑤ 移动端断点（≤780px/≤1180px）保留原有递减 padding（0 20px 等），
     属响应式收缩而非违规。
  ⑥ 上一轮已落地的对称基建继续覆盖全部视图：所有 editor-grid 子列的
     section-head 统一 min-height 30px + 底部细线（标题齐平）；
     测试页左右两栏等高拉伸；表格单元格 18px 16px、行高 1.6 维持规则线。
- 各级页面覆盖情况：
  · 一级（8 个主导航视图）：共享 .main 骨架 → 边缘留白统一 92px ✓
  · 二级（各视图内 section/两栏）：editor-grid 44px 空档 + section-head 基线 ✓
  · 三级（路由详情/渠道详情/模型测试详情）：96px 空档规则、sticky 左列、
    cd-*/mt-* 面板内边距 ≥16px ✓
- 验证：CSS 括号平衡（716/716）；node --check 通过；
  页面实测渠道注册表正常渲染（首次加载出现一次空态竞态，复查正常，非回归）。
- 备份：services/web/_bak_api_page_20260918_b4audit.html

## 2026-09-18 控制台 · 背景图顶部白色边界渐黑（用户：改同色系由下至上渐黑）
- 用户反馈：页面顶部边界是白色渐变带，不好看，要求改成同色系由下至上渐黑。
- 根因（查证）：暗色模式下背景图层 ::after 的透明度被历史规则 L109 钉死在 .18，
  其顶部垂直渐变 rgba(4,4,4,.10) 经 .18 透明度折算实际只压暗约 2%——
  architecture-hero.png 的亮天空原样透出，与透明顶栏叠加形成顶部白带。
  （另发现 L592 的白色 radial 光斑是死代码，已被 L755 的 background 覆盖，不生效。）
- 修法：不动既有 ::after 链（避免破坏已验收的整体暗调），
  在 opacity=1 的 ::before 图片层背景列表最上方插入 180deg 顶部渐黑层：
  rgba(10,10,10,.88) 0% → .46@12% → .10@30% → transparent 46%，
  色值取主题同源深灰（#0e0e0d/#141414 系），实现"由下至上渐黑"；
  既有 90deg 横向暗化层与图片 URL 原样保留（覆盖 background-image 需全量重列）。
- 验证：CSS 括号平衡（717/717）；新规则位于旧 180deg 渐变之后（级联胜出）；
  node --check 通过；页面实测正常渲染。
- 备份：services/web/_bak_api_page_20260918_b4topfade.html

## 2026-09-18 控制台 · 顶栏浅灰亮带修复（用户：白色太亮）
- 用户反馈：顶栏区域有一条明显比周围亮的浅灰横带（附截图红圈）。
- 根因（查证）：.topbar::before 硬编码米白渐变
  rgba(243,242,238,.58)→(.30)→(.06)，**没有任何暗色主题覆盖**。
  顶栏本体 background 虽是 transparent !important，但压不住这个 z-index:-1
  的伪元素——它在暗色下就是一条浅灰亮带，正好叠在背景图顶部渐黑层之上。
- 修法：样式表末尾追加暗色覆盖（级联位于背景图渐黑规则之后）：
  :root[data-theme="dark"] .topbar::before 改为
  color-mix(var(--paper) 72%→38%→transparent) 的 180deg 渐隐，
  与背景图顶部渐黑层同语言，顶栏区域融入页面底色。
  亮色模式不受影响（覆盖仅限 dark）。
- 验证：CSS 括号平衡（718/718）；新规则级联位于背景渐黑之后；
  node --check 通过；页面实测正常（路由管理页 4 别名 + 8 成员完整渲染；
  首次加载出现过一次空态竞态，复查正常，与本次改动无关的历史偶发问题）。
- 备份：services/web/_bak_api_page_20260918_b4topbar.html

## 2026-09-18 DeepSeek Harness · 接入 DeepSeek 官方 API provider
- 用户要求：在 DeepSeek Harness 中加入 DeepSeek 官方 API 的模型配置。
- 定位：harness 源码装于 D:\DeepSeek\deepseek-harness（Windows 服务
  DeepSeekHarness，NSSM 托管，Web UI 127.0.0.1:3080）；
  用户配置在 C:\Users\郭永涛\.dsh\settings.yaml（llm-pi-ai.providers 段）。
- 现状：原仅 local-gateway 一个 provider 且 disabled:true（用户此前停用），
  agent-default-model 仍指向 local-gateway/deepseek-opencode（未改动）。
- 改动：settings.yaml 新增 provider deepseek-official——
  api: openai-completions、baseURL: https://api.deepseek.com/v1、
  Authorization Bearer <DEEPSEEK_API_KEY>（密钥取自用户级环境变量，
  写入文件未在会话回显）。local-gateway 原样保留。
- 验证：
  · YAML 合法（pyyaml 解析通过），providers = [deepseek-official, local-gateway]；
  · 官方 GET /v1/models 实测返回 ['deepseek-flash','deepseek-v4-pro']，
    与网关 deepseek 渠道登记一致，模型 ID 权威确认。
- 未完成：服务重启需管理员（NSSM 服务跑在 SYSTEM 下，本会话无提权，
  Stop-Service 报 cannot open）。配置是否热加载未验证；
  若新会话未出现 provider，用户需提权重启：
  管理员 PowerShell 执行 Restart-Service DeepSeekHarness -Force
  （或 services.msc → DeepSeekHarness → 重启）。
- 备份：C:\Users\郭永涛\.dsh\settings.yaml.bak-20260918-b4official

## 2026-09-19 3100 网关 · 智谱 GLM 渠道接入与 DeepSeek Harness 官方配置修复
- 智谱接入：
  · 写入 API Key（POST /api/channels/zhipu/key）；
  · 清空 channels.json 中的 hidden_channels 隐藏名单，渠道重新上线；
  · 官方定价核对：确认 GLM-4.7-Flash 为官方真免费（200K 上下文），GLM-5.3-Flash 等为收费模型；
  · 路由挂载：通过 POST /api/model-overrides/custom 将 glm-4.7-flash 挂载至 zhipu 渠道，形成 5 渠道多活备用链。
- DeepSeek Harness 配置排障：
  · 定位设置页“无可用模型”真因：catalog.ts 第 843 行禁止 modelOverrides 与 models 共存，移除冲突的 overrides 声明；
  · provider 重命名为 DeepSeek，明确声明 deepseek-flash（DeepSeek V4.1 Flash）与 deepseek-v4-pro 模型规格；
  · 默认模型平滑切换至 DeepSeek/deepseek-flash。
- 备份：C:\Users\郭永涛\.dsh\settings.yaml.bak-20260919-fixoverlap

## 2026-09-19 3100 控制台 · 全局排版与字阶提档重构（用户反馈：字体太小）
- 用户反馈：网关页面字体大小设计不合理，字体实在是太小，严重影响肉眼阅读。
- 根因审计：
  · 历史“黑白建筑极简”设计过度套用平面印刷比例，硬编码散落了 75+ 处低于 10px 的微字体；
  · 甚至出现了 6.5px（.member-state）、7px（.topology-caption / .kpi-no-series）、7.5px（.member-model）、8.5px（.data-sheet th）等极端微缩值；
  · 数据表格正文仅 11.5px，表头仅 8.5px，按钮仅 11px，导致 1440px 分辨率下无法舒适阅读。
- 重构与落地：
  · 引入统一排版设计令牌：--text-xs (11px)、--text-sm (12px)、--text-base (13.5px)、--text-md (15px)、--text-lg (18px)；
  · 彻底消灭所有 < 11px 的极端微字体（全页面实测最低起步提升至 11px）；
  · 表格体系提档：表头 th 提至 12px（加粗并加字距），单元格 td 提至 13.5px，行高 1.6，代码 code 提至 12.5px；
  · 交互控件提档：按钮、输入框、下拉框字体提至 13px~13.5px，控件高度微调至 38px，增大点击热区；
  · 拓扑流向图提档：节点与状态文本全部恢复至 11.5px~13.5px。
- 验证：
  · CSS 括号平衡校验（815/815）PASS；
  · 内嵌脚本 node --check 校验 PASS；
  · opencli 联动真实 Chrome 运行时断言：页面全量元素最低计算字号从 6.5px 整体跃升至 11px，表格与按钮清晰呈现。
- 备份：services/web/_bak_api_page_20260919_b4fontscale.html

## 2026-09-19 3100 控制台 · 渠道注册分类重构（按免费/付费严格分流，移除冷宫）
- 用户需求：
  · 取消原“打入冷宫”分类；
  · 严格按照“免费”与“付费”重组渠道注册表：
    1. 付费渠道：只要该渠道有可能会路由到付费模型，即属于付费渠道；
    2. 免费渠道：完全是免费、没有付费的。
- 规则审计与分类落地（共 21 渠道）：
  · 🟢 免费渠道（11 个）：openrouter, zhipu, nvidia, cloudflare, longcat, agnes, xiaohongshu, mistral, sensetime, groq, modelscope（完全零扣费风险/纯免费配额）；
  · 🔴 付费渠道（10 个）：deepseek（官方按量）, opencode（Go订阅/按量）, tokenrhythm（全付费）, zscc（按量）, gmi（MiniMax限免过期按量）, ark-coding（Coding Plan订阅）, bai（仅2款限免其余扣费）, siliconflow（含DeepSeek-V3/R1按量模型）, ark（含豆包Pro/Seedance按量模型）, zenmux（多模型聚合器平台，包含商业与付费模型，非100%全免费）。
- 界面重构与交互：
  · 移除冷宫压暗灰阶（.cold）与冷宫操作按钮（coldToggleBtn）；
  · 新增两级全宽区块分隔标题：`🟢 免费渠道 (11)` 与 `🔴 付费渠道 (10)`，包含描述与计数 Pill；
  · 渠道卡片统一打标：`🟢 免费` / `🔴 付费` 醒目标签，卡片底部替换为直观计费口径说明；
  · 新增顶部快捷筛选器：`全部 (21)` / `🟢 免费渠道 (11)` / `🔴 付费渠道 (10)`，支持按组一键聚焦；
  · 修复抽屉详情报错：补齐 `promoTag(cid)` 兼容别名映射至 `channelBillingTag`，修复展开渠道详情时报 `promoTag is not defined` 的白屏问题。
- 验证：
  · CSS 括号平衡校验（823/823）PASS；
  · 内嵌脚本 node --check 校验 PASS；
  · opencli 联动真实 Chrome 运行时断言：双组分别精准挂载 11 / 10 渠道，Tab 点击切换即时响应，点击 ZenMux 等渠道卡片详情抽屉渲染正常且标为付费。
- 备份：services/web/_bak_api_page_20260919_b4billingclassify.html

## 2026-09-20 免费渠道模型策展重构（以 GLM-4.7-Plus 为能力斩杀线）
- 用户需求：
  · 在 11 个免费渠道中全面筛选模型；
  · 设定能力红线：必须大于以 GLM-4.7-Plus 为基准线；
  · 凡比此基准更弱的免费模型一律剔除，不得作为可选模型。
- 弱模型清理与斩杀（移出可选名单）：
  · `sensetime`：移出 `sensenova-u1.5-lite`；
  · `cloudflare`：移出 `@cf/qwen/qwen3-30b-a3b-fp8`（激活仅3B）与 `@cf/openai/gpt-oss-20b`；
  · `mistral`：移出 `mistral-medium` 全系过度档（3/3.5/3-5），收窄至 large 与 codestral；
  · `agnes`：移出 `agnes-3.0-flash` 轻量文本模型，纯保留图像/视频专用多模态；
  · `zhipu`：移出 `glm-4.7-flash`（官方无其他免费大模型，高阶 5.x 全按量收费），清空 `model_overrides.json` 中历史注入项，清空选定列表防付费外溢。
- 优质大模型策展落地（入选 channel_models.json）：
  · `modelscope`：入选 DeepSeek-V4-Pro、DeepSeek-V4.1-Flash、GLM-5.2、Qwen3.8-Flash-Next、Qwen3.5-397B-A17B、MiniMax-M3、Mistral-Large-Instruct-2407；
  · `nvidia`：入选 Nemotron-3-Ultra-550B、Nemotron-4-340B、Llama-3.3-70B-Instruct、Mistral-Large-2、Gemma-4-31B、Kimi-K3、GLM-5.3、DeepSeek-V4-Flash；
  · `openrouter`：严格锁定 6 款优质免费节点（dots-3-note-preview:free、glm-5.2:free、nemotron-3-ultra-550b:free、qwen3.8-27b:free、gemma-4-31b-it:free、deepseek-v4-flash:free）；
  · `cloudflare`：补选 70B 标杆 `@cf/meta/llama-3.3-70b-instruct-fp8-fast`，保留 `@cf/openai/gpt-oss-120b`；
  · `groq`：保留 `qwen/qwen3.8-27b`，补充 `openai/gpt-oss-120b`；
  · `mistral`：锁定 `mistral-large-latest/2512` 与 `codestral-latest/2508`；
  · `xiaohongshu`：锁定 `dots3-note-prev`（T0 顶级）；
  · `longcat`：保留 `LongCat-2.0`（美团 128k 免费档）；
  · `sensetime`：锁定 `deepseek-v4-pro`、`glm-5.2`、`kimi-k3`、`deepseek-v4-flash`。
- 底层机制加固：
  · `channels.py`：修复渠道策展空列表 `[]` 仍泄漏全量目录的边界，支持显式空选定（0 个暴露）；
  · `api_page.html`：空选定展示文案优化为直观的“未选定模型（0 个模型 · 不参与路由暴露）”。
- 验证：
  · 真实 /v1/models 接口实测：所有被斩杀弱模型均已消失，免费渠道暴露总量从历史数百款收敛为 32 款真正大模型；
  · 浏览器交互实测通过。
- 备份：
  · `data/_channel_models_bak_20260920_b4clean.json`
  · `services/_bak_channels_20260920_b4curate.py`
  · `data/_model_overrides_bak_20260920.json`

## 2026-09-20 AGNES 渠道全量 12 模型审计与配置补全
- 问题：
  · 控制台历史上显示 AGNES 仅有 3 个可用模型（原因是 channels.py 早期硬编码了 3 个初始示例兜底）。
- 官方接口现场实测：
  · 调通 `https://apihub.agnes-ai.com/v1/models`，证实实际全量包含 12 个可用模型（6 文本 + 3 生图 + 3 生视频）；
  · 实测文本模型：`agnes-2.5-pro` 成功返回高品质逻辑推理回答（量子纠缠）；
- 落地：
  · `channels.py`：默认静态模型补全为完整的 12 款，避免网络重试降级为 3 款；
  · `channel_models.json`：锁定三大王牌 `agnes-2.5-pro`（高阶文本）、`agnes-image-2.5-flash`（生图）、`agnes-video-2.5-flash`（生视频）；
  · 控制台验证：抽屉展示“全部可用模型 · 12”，已选模型精确置顶 3 款旗舰。

## 2026-09-20 选定模型基准线校正为 GLM-4.7-Flash · 全部模型全量展示
- 用户明确指示：
  · 选定模型按照 `GLM-4.7-Flash` 的标准筛选（即 >= GLM-4.7-Flash 的模型均入选，弱于其的小参数/老旧模型剔除）；
  · 渠道详情中的“全部可用模型”必须全量完整展示，不做过滤隐匿。
- 全量目录补齐（全部模型全展示）：
  · `zhipu`：解决智谱官方 /models 不返回免费模型的问题，在探测探针与静态清单中双向补全 `glm-4.7-flash`，全量目录达 12 款；
  · `agnes`：静态与动态全面维持 12 款全量展示（6 文本 + 3 生图 + 3 生视频）；
  · 所有 11 个免费渠道的“全部可用模型”在抽屉中 100% 完整展示（NVIDIA 82款、Mistral 46款、ModelScope 35款、OpenRouter 447款等）。
- 选定模型校准落地（channel_models.json）：
  · `zhipu`：正式挂载 `glm-4.7-flash`（100% 免费且精准契合基准线）；
  · `agnes`：选定 `agnes-2.5-pro`、`agnes-3.0-flash`、`agnes-image-2.5-flash`、`agnes-video-2.5-flash`；
  · `cloudflare`：选定 `@cf/zai-org/glm-4.7-flash`、`@cf/meta/llama-3.3-70b-instruct-fp8-fast`、`@cf/openai/gpt-oss-120b`、`@cf/openai/gpt-oss-20b`、`@cf/qwen/qwen3-30b-a3b-fp8`；
  · `sensetime`：选定 `deepseek-v4-pro`、`glm-5.2`、`kimi-k3`、`deepseek-v4-flash`、`sensenova-6.8-flash-lite`；
  · `groq`：选定 `openai/gpt-oss-120b`、`openai/gpt-oss-20b`、`qwen/qwen3.8-27b`；
  · `mistral`：选定 `mistral-large-latest/2512`、`codestral-latest/2508`、`mistral-medium-latest/3.5`；
  · `modelscope`：用户指令仅选定展示 `deepseek-ai/DeepSeek-V4.1-Flash` 这一个模型（全部可用模型仍全量展示 35 款）。
- 验证：
  · 重启 3100 进程（PID 23128），/healthz 与 /api/channels/zhipu/models 探测实测通过；
  · opencli 联动 Chrome 验证：智谱与 AGNES 抽屉均展示“全部可用模型 · 12”；魔塔社区抽屉“全部可用模型 · 35”，选定置顶模型严格仅 `deepseek-ai/DeepSeek-V4.1-Flash` 一款。





## 2026-09-20 ~13:45 — 二三级页面半透明渐变重构与独立商业工程规范交付

### 1. 视觉层重构：二三级页面白色背景向左渐变半透明淡出
- 针对用户截图反馈的二三级页面白色形状生硬切块问题，重构 pi_page.html 中 .view:not(#view-dashboard)::before：
  · 背景由陡峭硬渐变改为 linear-gradient(to left, rgba(243,242,238,.92) 0%, rgba(243,242,238,.86) 25%, rgba(243,242,238,.65) 50%, rgba(243,242,238,.35) 75%, rgba(243,242,238,.12) 90%, rgba(243,242,238,0) 100%)；
  · 暗色模式同步对齐适配深灰主题；
  · 实现从右到左透明度平滑递增（向左自然透明淡出），完美融入底层山水建筑纹理；
  · 完成 CSS 括号平衡严格语法校验。

### 2. 网关后台长效常驻加固
- 修复 start_api_gateway_3100.ps1 中的 Python 解释器路径为系统常驻 uv cpython 3.11；
- 基于 WMI 脱离会话守护启动网关（PID 5136，端口 3100），验证 /healthz 响应 {"ok": true} 保持稳定在线；
- 贯彻用户硬约束：后台进程不随意杀死，确保服务持续存活。

### 3. 项目独立平级迁移与商业工程结构落地
- 项目从 D:\Work\AI平台\apps\api-gateway 独立提拔为 D:\Work\api-gateway，与 work 仓库下各子项目完全平级；
- 落地**双层视图架构**：
  · **Agent 专属机器视图**：AGENTS.md 纯短链映射，直达核心源码与运行时；
  · **用户专属交互可视化视图**：isual-hub/index.html 独立交互大屏，开箱即用呈现架构、路线图、WBS 任务看板与渠道矩阵；
  · **四大核心工程文档规范**：架构全景（SYSTEM_OVERVIEW）、产品路线图（PRODUCT_ROADMAP）、任务拆分（TASK_BREAKDOWN）、进展跟踪（PROGRESS_TRACKER）。
