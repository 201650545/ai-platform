# api-gateway 文档

本项目专属文档**自包含**在本目录（`apps/api-gateway/docs/`），与代码同一子文件夹，项目看全貌不需跨目录：

- `api_gateway.md` —— 服务身份 / 运行 / 配置三拆 / 主题
- `主题设置.md` + `玄白_MONO_GATEWAY_预览图.png` —— 玄白·黑白建筑极简（MONO GATEWAY）主题定义与预览图

跨网关**共享约定**不自包含，仅引用公共区：
- ADR-002（配置三拆与显式备用链）、ADR-003（统一模型名聚合同模多渠道成员池）位于 `apps/search-gateway/docs/ADR/`，两应用共用，不复制。
- 运行手册 / 任务卡等全局知识在父库 `docs/`（Obsidian），此处不复制。